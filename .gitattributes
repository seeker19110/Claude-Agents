# Nhiều phiên agent chạy song song đều thêm một dòng vào cùng khối "Chưa phát hành".
# `union` gộp cả hai bên thay vì dừng rebase — đúng ý muốn: không dòng nào được mất.
# Sau khi gộp, đọc lại khối để bỏ dòng trùng nếu có (git không sắp xếp giúp).
CHANGELOG.md merge=union
docs/archive/DAC-TA-NANG-CAP-2026-09.md merge=union

# Máy phát triển chính là Windows. Một file `.sh` lọt vào repo với CRLF thì bash trên CI Linux báo
# `\r: command not found` — và với hook `.claude/hooks/*` thì nó không đỏ ở đâu cả, chỉ lặng lẽ hết canh.
# `platform/console/tests/test_cong_khung.py` canh dòng này bằng `git check-attr`.
*.sh text eol=lf
