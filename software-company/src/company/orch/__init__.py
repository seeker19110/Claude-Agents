"""Máy trạng thái của orchestrator, tách theo trách nhiệm (ADR-0034). Re-exported nguyên vẹn từ
`company.orchestrator` — import từ đây khi viết module mới trong gói này, import từ `company.orchestrator`
khi ở ngoài (giữ tương thích test/module cũ)."""
