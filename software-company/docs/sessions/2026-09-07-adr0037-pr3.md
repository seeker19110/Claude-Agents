# Phiên 2026-09-07 — ADR-0037 PR-3 (skill theo pha)

PR: **#155, đã merge** (squash, 24/24 check xanh). Phạm vi: **đúng một PR** — §4 của
`docs/DAC-TA-TRIEN-KHAI-ADR-0037.md`. Nhánh `claude/trienkhai-pr-3-e2r5kk` đã rebase lên `main` có PR-1
(#154) trước khi mở PR: xung đột duy nhất là dòng số ca/file test trong `README.md`, giải bằng cách đo lại
từ đĩa (1001 ca / 58 file) rồi chạy lại đủ ba lệnh CI trên bản đã rebase.

Đã làm (mã):
- `registry.py`: `Phase`, `AgentSpec.phases`/`_phase_text`/`owned_skills`, `system_prompt(phase)`, `_load_phases`.
  Cấm trùng trong CÙNG một cấp; trùng giữa hai cấp thì **pha thắng** (bản rút gọn cấp agent biến mất).
  `check_owners` tính trên hợp cấp agent ∪ mọi pha.
- `orch/routes.py`: `Route.phase`, `phase_for(r, spec, inp)` (route khai sẵn, hoặc `stack` của ticket với
  `tools="rw"`), `check_routes` bắt route khai pha agent không có.
- `events.py` + `topics/schemas/tasks.json`: `Task.stack`; `AuditLog.phase` + `audit-log.json`.
- `runner.py`: `phase` xuống `generate`/`generate_in_workspace`/`author_tests`/`run_context`; prompt và `fit` theo
  đúng pha; `cache_key = <id>[<pha>]`; audit `produced:*` ghi `phase`; payload đầu ra mang `_phase` do CODE ghi.
- `assetscan.py`: `agent_weights` đo theo từng pha (`agent[pha]`), ngưỡng 50% áp cho từng dòng.

Bằng chứng: 992 test / coverage 100%, ruff + mypy sạch; 9 đột biến đo hai chiều (xem commit message).

Người sau không được quên:
- **Chưa agent nào khai `phases`** — PR này chỉ dựng cơ chế. `tests/golden/` và `.claude/agents/` không đổi một
  byte, nên PR-3 không cần 7 bước `CONTRIBUTING.md` §3; PR-5a..5e thì cần đủ.
- `Task.stack` đã có trong schema nhưng **chưa agent nào điền**; `phase_for` tạm lấy `assignee`. Kiểm
  `"<id> thiếu stack"` trong `_check_plan` (đặc tả §4.2) để lại cho PR sinh ticket có `stack` (PR-5e), vì bắt buộc
  từ bây giờ sẽ chặn mọi plan trong khi không agent nào biết ghi trường đó.
- Guard `_from_phase` (đặc tả §1.2) đọc `_phase` mà runner vừa ghi — chưa viết, thuộc PR-4/PR-5.
- Eval theo pha (đặc tả §11) chưa làm: `evals.py` chưa có trường `phase`.
- `make eval-replay` có 4 ca FAIL — đã có trên `main` trước PR này (kiểm bằng `git stash`), không phải hồi quy.
- Luật bắt buộc **10** của `AGENTS.md` ra đời từ phiên này: dòng CHANGELOG của PR-3 lúc merge còn thiếu
  `(#155)` vì số PR chỉ có sau khi tạo PR. Từ nay điền số rồi commit tiếp vào **chính PR đó**.
  Luật 10 và phần vá thông tin của PR-3 đi trong PR **#157**, và PR đó tự áp luật 10 cho chính nó.
