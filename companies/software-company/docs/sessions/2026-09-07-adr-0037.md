# Phiên 2026-09-07 — ADR-0037 (21 agent → 5, 2 gate)

Nhánh: `claude/software-company-agents-stages-no7cre`. Chưa có PR mở; chưa đổi dòng code nào.

Đã làm:
- `docs/adr/0037-gop-21-agent-thanh-5-hai-gate.md` — quyết định ranh giới vai và số gate (trạng thái Đề xuất).
- `docs/DAC-TA-TRIEN-KHAI-ADR-0037.md` — đặc tả từng PR (PR-1 … PR-6), front matter 5 agent, bảng route đích.
- README (gốc + package) cập nhật "ADR 0001–0037" vì `test_readme_khop_so_lieu_that` đếm ADR từ đĩa.

Người sau không được quên:
- ADR chưa được chấp nhận; PR-1 (`_check_plan` chặn hết) là bước duy nhất làm được mà không cần model thật.
- Mỗi PR-5x cần `make eval-record` bằng model thật (hoặc workflow Actions `eval-record`).
- `supervisor` vẫn là một file trong `agents/` → `load_agents()` trả 6, không phải 5 (đặc tả §6 ghi rõ).
