# Audit toàn dự án 2026-09-12 — tám phép đo A1–A8

Lần đầu chạy theo khuôn mới (`docs/TASK-PACK.md` §"Gói việc thường trực: audit toàn dự án", mở ra từ nghi thức
tự kiểm cũ ở PR #264). Đo trên nhánh `docs/audit-toan-du-an` @`3fa2254`, `uv sync` sẵn, Windows 11. Mọi số "đo
được" là output của lệnh trong cột cuối, chạy trong chính phiên này. Số nào không tái hiện được thì **sửa tài
liệu, không sửa phép đo**.

## A1 — Số liệu tài liệu gốc

| Tài liệu | Claim | Đo được | Chênh | Lệnh |
|---|---|---|---|---|
| `README.md` software-company | ADR 0001–0039, 1100 test | 1152 test | +52 | `cd companies/software-company && uv run pytest --collect-only -q` |
| `README.md` console | ADR 0001–0003 | ADR 0001–0004 | +1 | `ls platform/console/docs/adr` |
| `companies/software-company/README.md:68` | 1151 ca | **1152** | +1 | như trên |
| `companies/software-company/README.md:250` | 1097 ca | **1152** | +55 | như trên — hai dòng tự mâu thuẫn nhau trong cùng file |
| `platform/gateway/README.md` | 216 ca | 251 ca | +35 | `cd platform/gateway && uv run pytest --collect-only -q` |
| `companies/keeper/README.md` | 5 ca (dòng ví dụ, không phải tổng) | không lệch — không phải claim tổng | 0 | — |

Đã sửa cả 6 dòng trong PR #272 (commit A1+A2).

## A2 — README gốc so README con

Đã có cổng canh cho software-company (`test_so_agent_khop_dia`, `test_so_adr_khop_dia`), nhưng README gốc còn
**ba dãy ADR khác** (console, gateway, xagents-core) không cổng nào canh — đúng dòng treo từ 2026-09-07. Đo:
console lệch (xem A1), gateway/xagents-core khớp. Mở rộng `test_readme_goc.py` thêm
`test_so_adr_cac_day_khac_khop_dia` (parametrize hai dãy console+gateway); xagents-core không có mục ADR ở dạng
`ADR NNNN–NNNN` trong README gốc nên không đưa vào (nó ghi rời "ADR gốc 0001, 0003, 0004").

## A3 — Hoá thạch sau khi cấu trúc đổi

`grep -rn "package" --include="*.md" --include="*.yml" --include="*.toml" . | grep -iE "năm|sáu|bảy|bốn|[0-9]+ package"`
sau khi #271 dọn 10 chỗ: còn sót đúng 1 chỗ (`docs/HUONG-DAN-VAN-HANH.md:38` "cả bốn package") — đã sửa trong
commit A3 của PR này.

**Phát hiện lớn hơn phạm vi grep gốc**: 35 chỗ nhắc `Studio-creators` trong `docs/HUONG-DAN-VAN-HANH.md` (cả §3.3
34 dòng và §6 "Vận hành Studio-creators" 85 dòng — company đã tách sang repo riêng ở #259, hai tuần trước) và 3
chỗ trong `docs/DIEU-PHOI-MODEL.md` (một mục tier riêng 24 dòng + hai dẫn ADR chết). Grep theo mẫu A3 gốc
(`năm|sáu|bảy|[0-9]+ package`) không bắt được loại hoá thạch này vì nó không nhắc "package" — nó nhắc tên riêng
của công ty đã xoá. **Đề nghị mở rộng A3**: thêm một lượt `grep -rln "Studio-creators\|studio\." docs/ *.md` sau
mỗi lần một package rời workspace, không chỉ grep theo số đếm.

Đã dọn hết 38 chỗ (35 + 3) trong commit A3.

## A4 — ADR "được chấp nhận" so mã thật

Tiếp nối vòng trước (2/11 ADR gốc). Vòng này (2026-09-13) đọc **hết phần còn lại**: 10 ADR gốc + 39 ADR của
`companies/software-company/docs/adr/` — tổng **50/50 ADR trong repo đã đối chiếu** (11 gốc lúc bắt đầu vòng này
đã lên 12 vì ADR-0012 gốc mới thêm 2026-09-12; cộng 39 của software-company = 51 file, trừ `docs/adr/README.md`
không phải ADR = 50 ADR thật). Đọc trạng thái tự khai + mục "Quyết định", rồi grep/đọc mã nguồn thật để đối
chiếu. Số nào không tái hiện được thì sửa tài liệu, không sửa phép đo (khuôn A4 gốc).

### ADR gốc (`docs/adr/`) — 12/12

| ADR | Tên ngắn | Trạng thái tự khai | Khớp mã? | Lệnh kiểm chính | Ghi chú |
|---|---|---|---|---|---|
| 0001 | lõi chung `xagents-core` | được chấp nhận | ✅ | `grep -rn "publish\|subscribe" .../bus.py`, cấu trúc 7 bước K3 | |
| 0002 | `gate.request` tin `env.actor` | (không có dòng riêng) | ✅ | `grep -n created_by .../gate_cli.py` → `kw = {..., "created_by": env.actor}` | |
| 0003 | đối chiếu `ruvnet/ruflo` | được chấp nhận | ✅ | `grep -n "tu-kiem\|claim.*đo được" docs/TASK-PACK.md` → mục tự-kiểm đã có | |
| 0004 | đối chiếu `neo4j-labs/agent-memory` | được chấp nhận | ⚠️ một phần | `test -f docs/NGON-NGU.md` ✅; `grep -rn lessons_for src` → 0 kết quả | quyết định 1 (NGON-NGU.md) xong; quyết định 2 (`lessons_for(ticket)` cắt ≤5 bài học) CHƯA cài — ADR tự ghi rõ "không làm trước K3.6", và K3.6b (blackboard vào core) đã xong nhưng phần lọc `lessons_for` cho prompt runner vẫn chưa thấy trong mã; đã có trong sổ "việc để lại" của `docs/reports/2026-09-07-tu-kiem.md:40` từ trước — không phải phát hiện mới, không tự sửa (không phải một dòng trạng thái, mà một tính năng còn thiếu) |
| 0005 | `HumanGate.request()` từ chối `created_by` rỗng | (không có dòng riêng) | ✅ | `grep -n "req.created_by).strip()" .../gates.py` → có kiểm + `PermissionError` | |
| 0006 | công ty con `keeper` | (không có dòng riêng) | ✅ | `ls companies/keeper/src/keeper/` → `risk.py`, `budget.py`, `gates.py`... đủ 6 khối | |
| 0007 | tỉa `role=tool` cũ trong vòng tool | được chấp nhận | ✅ | `grep -n "_prune" .../context.py .../runner.py` → hàm `_prune` + gọi trong `_turns` | |
| 0008 | allowlist vai được tạo gate | (không có dòng riêng) | ⚠️ đã sửa | `grep -n REQUEST_ACTORS .../gate_cli.py` → cơ chế khớp | mục "Liên quan" trỏ `Studio-creators/src/studio/gate_cli.py` — đường dẫn đó không còn tồn tại (studio tách repo riêng từ #259); đã sửa dòng tham chiếu, nội dung Quyết định/Hệ quả không đổi |
| 0009 | quan sát được bằng span | được chấp nhận | ✅ | `grep -n "class Span\|def span" .../observe.py` → có | |
| 0010 | domain allowlist mạng sandbox | đề xuất, chưa cài đặt | ✅ (đã kiểm vòng trước) | — | |
| 0011 | hợp nhất một công ty + cải tổ thư mục | Chấp nhận | ✅ (đã kiểm vòng trước) | — | |
| 0012 | PR theo hạng mục lớn | Chấp nhận | ✅ | `grep -n "hạng mục" docs/KHUON-THI-HANH.md` → §3.6 đã đổi đúng như Quyết định | |

### ADR `companies/software-company/docs/adr/` — 39/39

| ADR | Tên ngắn | Trạng thái tự khai | Khớp mã? | Lệnh/bằng chứng chính |
|---|---|---|---|---|
| 0001 | event-driven | Accepted | ✅ | `bus.py` publish/subscribe |
| 0002 | block-structure | Accepted, đính chính bởi 0009 | ✅ | `orch/*.py` dùng khối |
| 0003 | expand-to-22-agents | Accepted, đính chính bởi 0009 | ✅ | lịch sử đúng, đã đính chính tiếp bởi 0037 (xem dưới) |
| 0004 | prompt-as-code | Accepted | ✅ | `agents/*/*.md` là nguồn duy nhất |
| 0005 | provider-neutral runner | Accepted | ✅ | `llm.py` `Backend`/`RoutingClient` |
| 0006 | research-merge-acceptance-staging | Accepted, sửa 0002/0003 | ✅ | trạng thái ticket `merged` có trong `events.py` |
| 0007 | orchestrator | Accepted | ✅ | `orchestrator.py` tồn tại, khớp mô tả |
| 0008 | skill-tiering | Accepted | ✅ | `agents/*/*.md` có `tier` |
| 0009 | ux-designer-not-split | Accepted, sửa 0003 | ✅ | không có agent `ux-designer` riêng trong `agents/` |
| 0010 | tool-boundary-eval-replay | Accepted | ✅ | `evals.py` có `RecordingClient`/`ReplayClient` |
| 0011 | integration-branch | Accepted | ✅ | `orchestrator.py:13` nhánh `company/integration` |
| 0012 | content-context-cost-parallel | Accepted | ✅ | 8 khoảng trống đều có cơ chế tương ứng hiện nay (blackboard nội dung thật, `context.py` giới hạn cửa sổ, retry, `pricing`, tool nghiên cứu, `worktree`, audit-log) |
| 0013 | per-stack-checks | Accepted, bổ sung 0010 | ✅ | `runner.py:552` `ws.stack().test` |
| 0014 | schema-as-source-of-truth | Accepted, bổ sung 0001 | ✅ | `topics/schemas/*.json` 19 file, `events.py` đồng bộ |
| 0015 | eval-recordings-required | Accepted, bổ sung 0010 | ✅ | `Makefile:39` `eval-record` bắt buộc |
| 0016 | skill-must-have-owner | Accepted, bổ sung 0008 | ✅ | `agents/*/*.md` có `owner` |
| 0017 | acceptance-is-a-real-gate | Accepted, bổ sung 0001 | ✅ | `gates.py:50` `decided_by != created_by` |
| 0018 | project-scoped-blackboard | Accepted | ✅ | `core.py:68` `global_namespaces` |
| 0019 | subscription-routing | Accepted | ✅ | `xagents_core/routing.py` `class RoutingClient` |
| 0020 | context-by-role | (không có dòng riêng) | ✅ | `subagents.py:241` "trần kích thước ... ADR-0020" |
| 0021 | review-lean | Accepted | ✅ | `delivery.py` `required_reviews`, ghi chú ADR-0037 đè lên (qa hợp nhất reviewer+qa-debugger) |
| 0022 | quet-tai-san-prompt | (không có dòng riêng) | ✅ | `assetscan.py`, `assetscan-waivers.txt`, `test_assetscan.py` |
| 0023 | claude-code-cli-tools | (không có dòng riêng) | ✅ | `llm.py:97` `cli_tools` |
| 0024 | cau-mcp-cho-claude-code | (không có dòng riêng) | ✅ | `llm.py:100` `mcp_tools`, `mcp_bridge` |
| 0025 | repo-theo-tung-du-an | (không có dòng riêng) | ✅ | `orchestrator.py` `self.repo` theo từng lệnh, `gate_brief.py` dùng `integ.repo` theo dự án |
| 0026 | khong-thay-harness-va-effort | (không có dòng riêng) | ✅ | `xagents_core.llm.cli_effort_args` |
| 0027 | giao-hang-that | (không có dòng riêng) | ✅ | `github_pr.py`, `orchestrator.py:42` tag + `company/release` |
| 0028 | test-author-doc-lap | Chấp nhận (đã cài) | ✅ | tự khai đã cài, khớp |
| 0029 | smoke-do-orchestrator-chay | (không có dòng riêng) | ✅ | `gate_brief.py` `verified_by: "orchestrator"` |
| 0030 | so-ruling | (không có dòng riêng) | ✅ | `ruling` xuất hiện ở `blackboard.py`/`events.py`/`gate_brief.py` |
| 0031 | gate-spec-doi-runtime | (không có dòng riêng) | ✅ | `gate_checklists.py:71-76` `spec.runtime`, `smoke.parse_runtime` |
| 0032 | no-kien-truc-treo | (không có dòng riêng) | ✅ | `supervisor.py` `debt_table()`, `orchestrator.py:491` `architecture_debt` |
| 0033 | dod-san-pham-chay-duoc-va-anh-chup | chấp nhận | ✅ | `events.py:133` `screenshots[]`, `gate_brief.py` `verified_by` |
| 0034 | tach-may-trang-thai | chấp nhận | ✅ | `orch/ticket_fsm.py`, `orch/release_fsm.py` tồn tại tách riêng |
| 0035 | sandbox-tien-trinh | chấp nhận | ✅ | `workspace.py` worktree cô lập theo ticket |
| 0036 | unverified-khong-phai-trung-lap | được chấp nhận | ✅ | `gate_brief.py:394-399` `unverified` kèm lý do |
| 0037 | gop-21-agent-thanh-5-hai-gate | **Đề xuất** (trước khi sửa) | ⚠️ **đã sửa** | `ls agents/*/*.md` → đúng 5 agent (product/builder/qa/security/ops) + supervisor; `gates.py` `GateKind = spec\|release\|acceptance\|escalation` (đã bỏ `plan`) — khớp NGUYÊN VĂN mục "Quyết định" §2; `companies/software-company/CLAUDE.md` gốc tự khai "6 agent... 3 human gate + escalation". PR-1..PR-5e đều đã merge (`docs/sessions/2026-09-07-adr-0037.md`, `...-pr3.md`) nhưng dòng Trạng thái vẫn ghi "Đề xuất" và banner "Chưa có dòng code nào đổi cùng ADR" — **lệch rõ ràng, đã sửa Trạng thái thành "Chấp nhận (đã cài đặt)"** kèm chú thích ngày/bằng chứng, không đổi nội dung Quyết định |
| 0038 | pr-that-cho-khach-review-truoc-uat | Accepted | ✅ | `github_pr.py` gọi `gh pr create` |
| 0039 | deploy-that-bang-compose | Accepted | ✅ | `deploy.py`, `smoke.py`, README docker-compose |

### Tổng kết A4

**50/50 ADR đã đối chiếu** (12 gốc + 39 software-company — kể cả 2 ADR gốc đã kiểm ở vòng trước 2026-09-12, không
đếm lặp là 12; A4 lúc bắt đầu nói "11 ADR gốc" vì khi đó ADR-0012 gốc chưa tồn tại).

- **2 ADR đã sửa Trạng thái/tham chiếu cho khớp thực tế** (không đổi quyết định kiến trúc):
  - `companies/software-company/docs/adr/0037-gop-21-agent-thanh-5-hai-gate.md`: "Đề xuất" → "Chấp nhận
    (đã cài đặt)" — agent + gate đã đúng như Quyết định mô tả từ nhiều PR trước, chỉ dòng trạng thái quên cập nhật.
  - `docs/adr/0008-allowlist-vai-duoc-tao-gate.md`: sửa dòng "Liên quan" trỏ file `Studio-creators/...` đã không
    còn tồn tại (studio tách repo riêng #259) — nội dung quyết định không đổi.
- **1 mục lệch nghiêm trọng hơn một dòng, chưa tự sửa, cần người quyết**: ADR-0004 quyết định 2 (`lessons_for`,
  lọc bài học theo `assignee`/`risk_tags`/`retry>0`, cắt ≤5 bản mới nhất, đưa vào prompt runner) chưa có trong mã —
  đây không phải lỗi Trạng thái (ADR không tự khai "đã cài") mà một tính năng đã hoãn từ 2026-09-07, đã ghi trong
  `docs/reports/2026-09-07-tu-kiem.md:40`, tới giờ (K3.6 blackboard-vào-core đã xong) vẫn chưa làm. Cần người
  quyết: làm tiếp `lessons_for` (việc code, cần ADR/ticket riêng, không phải việc của audit A4) hay đóng lại vì
  đã có `supervisor.lessons()`/`calibration()` đủ dùng.
- **Không có ADR nào bị lệch tới mức phải đổi hẳn kiến trúc.**
- Nhiều ADR (≈20/50) không có dòng "Trạng thái:" tường minh ở đầu file — không phải một lỗi cụ thể để sửa (không
  có claim sai), nhưng đáng ghi lại: khuôn ADR không bắt buộc trường này, nên không đồng nhất giữa các ADR. Không
  tự thêm trong phiên này (thêm 20 dòng "Trạng thái: Chấp nhận" đồng loạt là việc lớn hơn phạm vi audit A4, có
  thể là một ticket riêng của keeper's `drift-detector`).

## A5 — Sổ miễn trừ, bốn lối thoát hợp lệ khỏi cổng

Đã có trần cứng từ #271 (`platform/console/tests/test_cong_repo.py`). Đo lại xem còn khớp không:

| Lối thoát | Trần (#271) | Đo lại 2026-09-12 | Khớp? |
|---|---|---|---|
| `pragma: no cover` | 21 (core 6, gw 0, console 2, sc 10, keeper 3) | 21 | ✅ |
| `skip`/`xfail` | 6 (gw 3, console 1, sc 2) | 6 | ✅ |
| `omit` | 2 | 2 | ✅ |
| `assetscan-waivers.txt` | không có trần cứng (tự dọn qua `waiver-unused`) | 3 dòng | cổng riêng đã có, không cần thêm |

Không lệch — cổng #271 mới hai giờ tuổi, đúng như kỳ vọng. Giá trị thật của A5 sẽ thấy ở lần audit **sau**, khi
so trần này với số đo lúc đó.

## A6 — Độ sâu phép đo (`branch = true`)

| Package | `branch = true` | Ghi chú |
|---|---|---|
| `companies/keeper` | ✅ | |
| `platform/xagents-core` | ✅ | |
| `companies/software-company` | ❌ (comment, `pyproject.toml`) | phiên 2026-09-13 bù xong 64/67 nhánh (đợt 1: gate_cli, orch/gates_flow, orch/rehydrate, probe, supervisor; đợt 2: 11/12 file còn lại đạt 100% nhánh) — còn ĐÚNG 3 nhánh bế tắc thật (`orchestrator.py:332`, `orch/scheduler.py:56`/`:250` — return một dòng trong `with`, coverage.py không ghi nhận được dù test đúng nhánh; xem `pyproject.toml`) trước khi bật thật |
| `platform/console` | ❌ (không nhắc) | |
| `platform/gateway` | ❌ (không nhắc) | |

Không đổi trong phiên này (bật là sửa cấu hình cổng, sẽ lòi nhánh cần test — đúng lý do đã ghi trong sổ treo từ
2026-09-10, cần PR riêng có thể nhiều PR).

## A7 — Cổng còn hiệu lực

`companies/keeper` chưa qua canary (BT8): `README.md:5` tự khai "chạy được, chưa qua canary". Hệ quả đo được:
`golden-check (companies/keeper)` vừa nối vào CI thật ở #271 (trước đó không chạy), nhưng `drift-check` — tầng
audit tự động phát hiện bản dẫn xuất lệch — theo `README.md:95` "đường ghi hẹp cho canary còn là một quyết định"
chưa chốt. Không phải việc của phiên đo (đã ghi trong sổ treo 2026-09-10).

## A8 — Hạn dùng của bằng chứng

- Bản ghi eval `companies/keeper/evals/recordings/*.json` (3 file: keeper-supervisor, release-clerk,
  security-auditor) — khoá bằng hash `(system_prompt, user_message)` (`TRAPS.md:115`), nên tự hết hạn đúng lúc
  prompt đổi mà quên ghi lại; cổng `eval-replay --strict` đã canh việc này ở CI (`keeper-eval-replay` job). Không
  cần hạn đáo thủ công thêm.
- Sổ nợ keeper: không tìm thấy file sổ nợ chuyên biệt (`companies/keeper/*.md` không có tên khớp `no*`/`debt`) —
  nợ được ghi trực tiếp vào ticket trên bus (`keeper.sqlite`), không phải file tĩnh. Không có gì để đo hạn dùng ở
  cấp tài liệu.

## Ranh giới với cổng máy

17+ job CI đã canh (xem `docs/TASK-PACK.md` §"Ranh giới với cổng máy"); phiên này không đo lại phần đó.

## Việc để lại đang treo — cập nhật

Đóng trong PR #272 (A1, A2, A3): số liệu README gốc/con, dãy ADR console/gateway, 38 chỗ Studio-creators, câu
khai sai coverage 90%. Còn treo, chuyển sang `docs/TASK-PACK.md`:

- A4: **xong** 2026-09-13 — 50/50 ADR đối chiếu, 2 sửa trạng thái/tham chiếu, 1 mục cần người quyết (ADR-0004
  `lessons_for` chưa cài) — xem bảng đầy đủ ở mục A4 phía trên.
- A6: bật `branch = true` cho `companies/software-company` — phiên 2026-09-13 (PR test(company): bù nhánh
  coverage gate_cli/gates_flow/rehydrate/probe/supervisor) bù xong 16/67 nhánh; còn 51/67 nhánh ở 12 file
  (orch/{release_fsm,routes,scheduler,ticket_fsm,verify,worktree_flow}, orchestrator, runner, subagents, tools,
  web, workspace) trước khi bật `branch = true` thật — tiếp tục nhiều PR nhỏ theo cụm file.
- A7: canary BT8 của keeper — gói việc riêng.
- Đề xuất mở rộng phạm vi A3 sang tên riêng công ty đã xoá, không chỉ số đếm package (xem mục A3 ở trên).
