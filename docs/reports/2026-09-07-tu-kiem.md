# Tự kiểm 2026-09-07 — số liệu trong tài liệu gốc so với đĩa

Lần đầu chạy nghi thức của ADR-0003 (`docs/TASK-PACK.md` §"Gói việc thường trực: tự kiểm"). Đo tại nhánh
`claude/ruflo-repo-review-try5w6` @`1b4b349`, `uv sync` sẵn, Linux. Mọi số "đo được" là output của lệnh trong cột
cuối, chạy trong chính phiên này. Số nào không tái hiện được thì **sửa tài liệu, không sửa phép đo**.

## Bảng claim · đo được · chênh

| Tài liệu | Claim | Đo được | Chênh | Lệnh |
|---|---|---|---|---|
| `README.md:17` software-company | 850 test | **1001** | +151 | `cd software-company && uv run pytest --collect-only -q` |
| `README.md:18` Studio-creators | 424 test | **469** | +45 | `cd Studio-creators && uv run pytest --collect-only -q` |
| `README.md:19` gateway | 206 test | **225** | +19 | `cd gateway && uv run pytest --collect-only -q` |
| `README.md:20` console | ADR 0001–0002 | **0001–0003** (`platform/console/docs/adr/` có 3 file) | +1 | `ls platform/console/docs/adr` |
| `README.md:21` xagents-core | ADR gốc 0001 | 0001 **và 0003** (0002 đặt trước cho K4) | +1 | `ls docs/adr` |
| `README.md:17` software-company | 21 agent, 45 skill, 19 topic, 14 template | 21 / 45 / 19 / 14 | 0 | `ls agents/*/*.md \| wc -l` … |
| `README.md:17` software-company | ADR 0001–0037 | 37 file | 0 | `ls companies/software-company/docs/adr \| wc -l` |
| `README.md:17` software-company | 26 subagent | 26 | 0 | `ls .claude/agents/sc-*.md \| wc -l` |
| `README.md:18` Studio-creators | 14 agent, 24 skill, 19 topic, 7 template, ADR 0001–0009 | 14 / 24 / 19 / 7 / 9 | 0 | như trên |
| `README.md:111` | `fail_under` 100 ×5 | 100 ×5 | 0 | `grep -n fail_under */pyproject.toml` |
| console (đo thêm) | — | 232 test | — | `cd console && uv run pytest --collect-only -q` |
| xagents-core (đo thêm) | — | 109 test | — | `cd xagents-core && uv run pytest --collect-only -q` |

## Kết luận

- **5 dòng lệch, 5 dòng khớp.** Mọi dòng lệch đều lệch **một chiều** (tài liệu nói ít hơn thực tế) — dấu hiệu của
  số liệu điền một lần rồi không ai đo lại, không phải dấu hiệu nói quá.
- **Vì sao 5 dòng khớp lại khớp:** số agent và số ADR của software-company có test CI canh
  (`platform/console/tests/test_readme_goc.py`); số test của README **từng package** có `test_readme_khop_so_lieu_that`.
  Số test và số ADR platform/console/xagents-core trong README **gốc** thì **không có gì canh** — đúng những dòng lệch.
- Đã sửa cả 5 dòng README trong cùng PR với báo cáo này. Hai test canh README gốc/package chạy lại xanh (output
  trong thân PR).
- **Việc để lại (không làm trong PR này, tránh sửa code cạnh bên — `AGENTS.md` luật cấm 7):** mở rộng
  `platform/console/tests/test_readme_goc.py` canh thêm số test và số ADR của platform/console/xagents-core trong README gốc, theo
  cùng quy ước "khoảng [số hàm test, gấp đôi]" của các package. Có test canh thì lần tự kiểm sau không phải đo lại
  năm dòng này bằng tay.
- **Việc để lại 2 (ADR-0004, đo thêm sau khi báo cáo lập):** `companies/software-company/agents/supervisor/supervisor.md:28`
  nói "bài học được runner đưa vào ngữ cảnh mọi agent qua blackboard" — thực tế `snapshot()` chỉ đưa **một** JSON
  của ticket đóng gần nhất (`blackboard.py::_latest` giữ bản mới nhất mỗi namespace; `_record_lessons` ghi mỗi
  ticket một version). Sửa câu đó cùng lúc với K3.6 (quyết định 2 của ADR-0004), theo đủ bảy bước
  `CONTRIBUTING.md` §3 — không sửa lẻ prompt trong PR tài liệu.
