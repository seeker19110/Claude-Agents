# 2026-09-25 — đánh giá #335, đặc tả Product Excellence v2

## Việc đã làm

- Chủ dự án yêu cầu đánh giá "PR 355". Repo không có PR hay issue 355 (API trả 404; số cao nhất là #335), nên phiên này đánh giá #335.
- Đo trên `main@6d74ba3`: bốn file test mới của company có 186 passed, hai file test core có 25 passed. Grep `src/`: không có caller nào của `commit_quality_result`/`ExecutionJournal` ngoài `quality_execution.py`.
- PR #336. Bản đánh giá nằm ở `docs/reports/2026-09-25-danh-gia-pr335.md`. Kế hoạch theo khuôn A–F nằm ở `docs/thi-hanh/pe2.md`. PR thuần tài liệu, không đổi code.

## Người tiếp quản

- Thi hành bằng `/thi-hanh pe2` **sau khi** PR chứa kế hoạch merge. Thứ tự hạng mục: pe2-quytrinh → pe2-cung → pe2-ky → pe2-noi.
- Có hai chỗ cần người quyết: dependency chữ ký (ADR K1, dự kiến số 0020) và ADR nối orchestrator (N1, dự kiến số 0021).
- Số dòng trong bảng A2 là của `6d74ba3`. Đọc lại hàm trước khi sửa.

## Thi hành pe2 — đợt 1 (P0, hạng mục pe2-quytrinh)

- PR #337. `/thi-hanh pe2`: lượt trước không để lại gì (không PR mở, bảng B toàn `chưa`), nên lượt này bắt đầu từ P0.
- P0 giao subagent mức C1. Phiên chính tự chạy lại: `dev-task.sh gate console` được 467 passed, 100% coverage. Đo chiều ngược: tắt nhánh phát hiện thì 7/15 ca đỏ, gồm ca fixture #335.
- Phiên chính sửa hai chỗ trong bản của subagent: comment workflow dẫn nhầm "ADR-0034 kịch bản C" (không tồn tại), và `AGENTS.md` làm rơi ý "không che `/goal` native".
- Từ PR này trở đi, template PR để nguyên ô DoD `- [ ]` là đỏ ở `metadata`. Tick ô, xoá ô không áp dụng, hoặc ghi `(sau merge)`.
- Repo nhánh đơn trong môi trường cloud: các hạng mục sau dùng lại nhánh `seeker/magical-johnson-2qemj8`, dựng lại từ `origin/main` sau khi PR trước merge.
- CI #337 lần đầu đỏ ở `console-unit (windows-latest)`: script in ô tiếng Việt ra stderr cp1252 và test gọi subprocess với env tối giản. Đã sửa bằng cách ép stderr UTF-8 trong script, và test tái hiện trên Linux bằng `PYTHONIOENCODING=ascii`. Bài học: test subprocess có tiếng Việt phải đọc bằng `encoding="utf-8"` và giữ `os.environ`.
- Đợt 2 phát triển song song trong worktree riêng: P1 (`wt/pe2-p1` e85753a) và P2 (`wt/pe2-p23` 628ffc8) đã commit cục bộ, P3 đang chạy. PR pe2-cung chỉ mở sau khi #337 merge.

## Thi hành pe2 — đợt 2 (P1, P2, P3, hạng mục pe2-cung)

- #337 (pe2-quytrinh) merge lúc 05:10 UTC, nên P0 = xong #337.
- P1 và P2 phát triển song song trong hai worktree, P3 làm sau P2 trên cùng worktree. Mỗi gói có ca đỏ trước code và đo chiều ngược. Phiên chính tự chạy lại chiều ngược của P1: đếm lại ngay trước transition thì 2 ca đỏ.
- `sc-qa`: mọi ca bắt buộc ở mục 7 đều có mặt. `sc-security` nêu ba chỗ, đã sửa trong commit riêng: principal so khớp phân biệt hoa/thường và khoảng trắng; docstring cũ vẫn nói Ready "không xác thực"; cổng chưa bắt `_append_unchecked`.
- Còn mở, ghi lại chứ không sửa: `artifact_matches` có khe TOCTOU giữa resolve và open (hash vẫn phải khớp); `earliest_evidence` lấy min trên mọi receipt, kể cả receipt chưa xác thực, nên receipt giả chỉ gây chặn nhầm; trần 64 MB của spec artifact là hằng riêng.
- PR #338.
