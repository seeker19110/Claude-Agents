# 2026-09-25 — đánh giá #335, đặc tả Product Excellence v2

## Việc đã làm

- Chủ dự án yêu cầu đánh giá "PR 355". Repo không có PR hay issue 355 (API trả 404; số cao nhất là #335), nên phiên này đánh giá #335.
- Đo trên `main@6d74ba3`: bốn file test mới của company có 186 passed, hai file test core có 25 passed. Grep `src/`: không có caller nào của `commit_quality_result`/`ExecutionJournal` ngoài `quality_execution.py`.
- PR #336. Bản đánh giá nằm ở `docs/reports/2026-09-25-danh-gia-pr335.md`. Kế hoạch theo khuôn A–F nằm ở `docs/thi-hanh/pe2.md`. PR thuần tài liệu, không đổi code.

## Người tiếp quản

- Thi hành bằng `/thi-hanh pe2` **sau khi** PR chứa kế hoạch merge. Thứ tự hạng mục: pe2-quytrinh → pe2-cung → pe2-ky → pe2-noi.
- Có hai chỗ cần người quyết: dependency chữ ký (ADR K1, dự kiến số 0020) và ADR nối orchestrator (N1, dự kiến số 0021).
- Số dòng trong bảng A2 là của `6d74ba3`. Đọc lại hàm trước khi sửa.

## Thi hành pe2 — đợt 1 (P0, hạng mục pe2-quytrinh)

- PR #337. `/thi-hanh pe2`: lượt trước không để lại gì (không PR mở, bảng B toàn `chưa`), nên lượt này bắt đầu từ P0.
- P0 giao subagent mức C1. Phiên chính tự chạy lại: `dev-task.sh gate console` được 467 passed, 100% coverage. Đo chiều ngược: tắt nhánh phát hiện thì 7/15 ca đỏ, gồm ca fixture #335.
- Phiên chính sửa hai chỗ trong bản của subagent: comment workflow dẫn nhầm "ADR-0034 kịch bản C" (không tồn tại), và `AGENTS.md` làm rơi ý "không che `/goal` native".
- Từ PR này trở đi, template PR để nguyên ô DoD `- [ ]` là đỏ ở `metadata`. Tick ô, xoá ô không áp dụng, hoặc ghi `(sau merge)`.
- Repo nhánh đơn trong môi trường cloud: các hạng mục sau dùng lại nhánh `seeker/magical-johnson-2qemj8`, dựng lại từ `origin/main` sau khi PR trước merge.
- CI #337 lần đầu đỏ ở `console-unit (windows-latest)`: script in ô tiếng Việt ra stderr cp1252 và test gọi subprocess với env tối giản. Đã sửa bằng cách ép stderr UTF-8 trong script, và test tái hiện trên Linux bằng `PYTHONIOENCODING=ascii`. Bài học: test subprocess có tiếng Việt phải đọc bằng `encoding="utf-8"` và giữ `os.environ`.
- Đợt 2 phát triển song song trong worktree riêng: P1 (`wt/pe2-p1` e85753a) và P2 (`wt/pe2-p23` 628ffc8) đã commit cục bộ, P3 đang chạy. PR pe2-cung chỉ mở sau khi #337 merge.

## Thi hành pe2 — đợt 2 (P1, P2, P3, hạng mục pe2-cung)

- #337 (pe2-quytrinh) merge lúc 05:10 UTC, nên P0 = xong #337.
- P1 và P2 phát triển song song trong hai worktree, P3 làm sau P2 trên cùng worktree. Mỗi gói có ca đỏ trước code và đo chiều ngược. Phiên chính tự chạy lại chiều ngược của P1: đếm lại ngay trước transition thì 2 ca đỏ.
- `sc-qa`: mọi ca bắt buộc ở mục 7 đều có mặt. `sc-security` nêu ba chỗ, đã sửa trong commit riêng: principal so khớp phân biệt hoa/thường và khoảng trắng; docstring cũ vẫn nói Ready "không xác thực"; cổng chưa bắt `_append_unchecked`.
- Còn mở, ghi lại chứ không sửa: `artifact_matches` có khe TOCTOU giữa resolve và open (hash vẫn phải khớp); `earliest_evidence` lấy min trên mọi receipt, kể cả receipt chưa xác thực, nên receipt giả chỉ gây chặn nhầm; trần 64 MB của spec artifact là hằng riêng.
- PR #PR.

## Thi hành pe2 — đợt 3 (K1, K2, hạng mục pe2-ky), làm cục bộ

- Force-push nhánh designated (thay các commit cũ của #337 đã squash) bị quyền phiên từ chối, dù chủ dự án chọn phương án này. Vì vậy pe2-cung nằm ở `seeker/magical-johnson-2qemj8` cục bộ @22bf757, chưa có PR. Người cần chạy `git push --force-with-lease=seeker/magical-johnson-2qemj8:9fedd0b… -u origin seeker/magical-johnson-2qemj8`.
- K1: ADR-0020 Accepted sau khi chủ dự án đồng ý `cryptography`. K2 làm trên `wt/pe2-ky`, xếp chồng lên pe2-cung.
- `sc-security` chấm K2 và nêu thiếu test tamper cho Ed25519; đã bổ sung. Cửa sổ ≤ max_age sau `not_after` (evidence lùi `created_at`) được ADR §4 chấp nhận và có test ghim; muốn thu hồi ngay thì gỡ key khỏi registry.
- Còn mở, chuyển sang N1: profile legacy chỉ bị chặn ở CLI `register`, còn `ExecutionJournal.register` gọi thẳng qua API thì chưa chặn.
- `uv add` để uv 0.8.17 thêm marker cho `h11`/`truststore` trong `uv.lock` (drift do resolver, `uv lock --check` vẫn sạch).
- ADR-0021 (nối orchestrator, gói N1) ở trạng thái Proposed trên `wt/pe2-noi`, còn năm câu hỏi chờ người.
- #338 (pe2-cung) merge lúc 06:41 UTC. Nhánh remote bị xoá sau merge, nên pe2-ky được rebase lên `main` và push thường, không cần force. PR #339.

## Thi hành pe2 — đợt 4 (N1, hạng mục pe2-noi)

- #339 (pe2-ky) merge; K1, K2 = xong #339.
- Chủ dự án giao "chốt phương án chất lượng cao" cho năm câu hỏi của ADR-0021, và ADR chuyển sang Accepted. Chốt được: một run cho mỗi plan; profile ký kèm spec; ticket ngoài run bị R6; candidate là sha đã staged; sha đổi sau SUCCEEDED thì R6 chặn. Nghiệm thu lại trên sha mới là mã N5, cần ADR core riêng.
- Lượt N1 đầu bị cắt vì giới hạn phiên (reset 09:40 UTC) và không để lại gì trên đĩa; lượt hai làm xong.
- `sc-builder` và `sc-security` nêu bảy chỗ: lỗi ngoài danh sách bắt làm vỡ `_mark`, escalate gom theo lớp lỗi, driver chạy lại vô hạn, không thoát sớm, `pid`/`run_id` ghép thẳng vào đường dẫn, console lách được quyết định 2, CLI commit ghi FAILED vĩnh viễn. Cả bảy đã sửa kèm test đỏ trước.
- Hỏi đáp chưa đóng: agent không tự đặt được `actor="human:…"`, vì actor do code gán theo `ROLE.*`. `approval_lookup` thật và CLI `--quality-trust` của orchestrator chưa có. Khi hàng đợi rỗng sau restart, attempt chờ tới event kế tiếp, đúng thiết kế ADR §c.
- N2 (gap R6) và N3 (console) phát triển song song trong hai worktree. `sc-gate-release` nêu R6 thiếu blocker; đã sửa, kèm test restart khoá thứ tự rehydrate. N4 là tài liệu, phiên chính tự làm (C1).
- Còn lại sau pe2-noi: N5 (nghiệm thu lại khi sha đổi sau SUCCEEDED, cần ADR core). Ngoài ra `approval_lookup` thật và cờ `--quality-trust` cho CLI orchestrator chưa có. Thiếu driver thì kết quả chỉ vào qua `quality_execution commit --trust`.

## Kết `/thi-hanh pe2`

- Đã merge: #337 (P0), #338 (P1–P3), #339 (K1–K2), #340 (N1–N4). Mọi PR có CI xanh trên head cuối.
- N5: ADR-0022 (Proposed) đề xuất `TASK_REOPENED` cho task `reopenable`. Đã đo: core hiện không có đường nào ra khỏi `SUCCEEDED`; `quality_flow.py` ở 399/400 dòng nên code N5 phải nằm ở module mới.

### Người sau KHÔNG được quên

- **chờ người — N5:** duyệt ADR-0022 và trả lời bốn câu hỏi: chọn (a); có migrate run cũ không; tự mở lại hay mở bằng lệnh; có trần số lần mở lại không. Trong lúc chờ, R6 chặn release khi sha đổi sau SUCCEEDED (hỏng thì đóng).
- `ApprovalLookup` thật và cờ `--quality-trust` cho CLI orchestrator chưa có. Thiếu driver thì kết quả chỉ vào qua `quality_execution commit --trust`.
- Force-push lên nhánh designated bị quyền phiên từ chối. Sau mỗi merge, nhánh remote bị xoá, nên push thường tạo lại nhánh là được.

## Thi hành pe2 — đợt 5 (N5, hạng mục pe2-noi2)

- #340 (pe2-noi) đã merge; N1–N4 = xong #340. PR #342. Không có PR mở, lượt này làm N5 trên nhánh `claude/continue-upgrade-refinement-c60xhz`.
- ADR-0022 (Accepted): core có event `task.reopened`, chỉ cho task sink, bắt buộc có lý do. Nó sửa §4 của ADR-0017. Phiên chính chốt theo quyết định 5 của ADR-0021, người đã giao "chốt phương án chất lượng cao".
- Test đỏ trước khi viết code. Core: 8 ca đỏ vì `TASK_REOPENED` chưa có. Company: 4 ca đỏ, gồm ca tái hiện `quality:accept` kẹt `READY` khi candidate quay về RC đã chấm, đúng họ lỗi của đường `FAILED → RETRIED` có sẵn.
- Đo chiều ngược:
  - Core: tắt từng điều kiện (sink, lý do, trạng thái run, task SUCCEEDED) thì ca tương ứng đỏ.
  - Company: tắt nhánh mở lại thì 3/4 đỏ; tắt hậu tố `~n` thì 2/4 đỏ.
- `orch/quality_flow.py` chạm trần 400 dòng (`test_orch_khuon_loi`), nên phần định danh attempt chuyển sang `quality_execution.py`. Retry `quality:accept` giờ cũng mang `reason` trong payload.
- Môi trường cloud thiếu `pytest-xdist` cho tới khi chạy `uv sync --all-packages --all-groups`.
- Còn lại ngoài pe2: `approval_lookup` thật và cờ `--quality-trust` cho CLI orchestrator.
- `sc-security` chấm N5 và nêu ba chỗ:
  - (1, trung bình) `runs_for_release` đọc trạng thái và candidate ở hai lần khác nhau. Một lần mở lại chen vào giữa sẽ ghép `succeeded` cũ với sha mới, và R6 cho qua. Đã tái hiện bằng test rồi sửa: đọc một snapshot, và chuyển `runs_for_release` sang `orch/quality_release.py`.
  - (2, thấp) cache `_quality_done` cũ nuốt lần mở lại khi candidate quay về. Đã sửa kèm test đỏ trước.
  - (3, thấp, hỏng thì đóng, có từ N1) kết quả CLI `commit` đến muộn cho attempt cũ bị chấm vào attempt mới thành FAILED. Chủ dự án yêu cầu sửa luôn trong #342: `commit_quality_result` từ chối khi `result.attempt_id` khác attempt đang chạy. Test đỏ trước ở mức unit và qua CLI thật; gỡ phần kiểm thì cả hai đỏ lại.
