# 2026-09-09

## `/thi-hanh 4l` — tiếp đợt 3 (4L-6) sau khi K3.5c merge

- Người dùng báo K3.5c đã merge, xác nhận bằng `git log origin/main` (#187 `refactor(core): K3.5c — sqlite_bus
  lên xagents_core`). Điều kiện vào đợt 3 (4L-6) và đợt 4 (4L-7) đạt — cả hai cần K3.5c, nhưng cùng chạm
  `studio/orchestrator.py` nên làm tuần tự theo bảng D (4L-6 trước, 4L-7 sau).
- 4L-6 tách hai tiểu gói theo đúng task pack (`_rework`+`_publish_video` ∥ `_transient`+`trusted_decision`)
  nhưng chạy **tuần tự trong cùng worktree** thay vì song song thật — cả hai chạm cùng file
  `studio/orchestrator.py`, hai subagent Opus sửa đồng thời cùng file trên đĩa sẽ giẫm chân nhau.
- **`sc-security` chấm gói phát hiện lỗ hổng thật, NGHIÊM TRỌNG**: `trusted_decision()` mà 4L-6b viết là
  blocklist lỏng (mặc định TIN, chỉ CHẶN actor hình người mà `evidence.by` lệch) thay vì allowlist mặc định từ
  chối như bản mẫu `company/gate_cli.py`. Hệ quả đo được: actor `"orchestrator"` (hoặc bất kỳ chuỗi không phải
  `human:x`) kèm `evidence.by="human:editor"` giả — qua được, và ca test 4L-6b tự viết (`test_bus_sqlite_cu_...`)
  vô tình khẳng định đúng hành vi lỗ hổng đó là "tương thích ngược". `PersistentGate.apply` và `_rehydrate`
  cũng tin thẳng evidence thô, cùng lỗ.
- Vá cả ba điểm (không giao lại subagent — sửa trực tiếp, nhỏ, rõ ràng): `trusted_decision` → allowlist
  `is_human(actor) and actor == by`; `apply()` gọi `trusted_decision` trước `super().decide`; `_rehydrate` dùng
  `trusted_decision(env)` thay vì đọc `d` thô cho nhánh `gate.decide approve` (ảnh hưởng dispatch plan thật khi
  mở lại bus). Sửa lại ca test cũ thành đúng ý (tương thích ngược = actor CHÍNH LÀ người ký, không phải actor hệ
  thống giả danh) + thêm ca `test_actor_orchestrator_gia_lam_nguoi_ky_bi_tu_choi` chứng minh lỗ đã đóng.
- Các phát hiện CAO/TRUNG BÌNH còn lại của `sc-security` (thiếu kiểm approver/four-eyes, gate có đang `pending`
  không, `SQLiteBus.replay` không validate payload, thiếu `Lease` giữa hai tiến trình) — **cố ý không vá**:
  chính review cũng nhận đó là bất biến ADR-0002 rộng hơn phạm vi "validate trước rẽ nhánh" của gói, cần ADR
  riêng nếu làm. Ghi rõ trong PR để không ai tưởng đã đóng hết.
- 4L-6 xong → PR #190. `make demo` chạy hết end-to-end (duyệt publish thật `human:editor` → lên lịch → phân
  tích) để nghiệm thu, không chỉ dựa vào test đơn vị.

## Gom docs tháng 9 đã bị `DAC-TA-KICH-BAN-B.md` bọc vào `docs/archive/`

- Phạm vi: chỉ tài liệu. Chuyển `docs/DAC-TA-NANG-CAP-2026-09.md` và `docs/DANH-GIA-VA-TAM-NHIN-2026-09.md`
  sang `docs/archive/` (`git mv`, không đổi nội dung ngoài một ghi chú "đã lưu trữ" thêm đầu mỗi file), sửa
  dẫn chiếu còn sống trỏ đường dẫn mới: `docs/DAC-TA-KICH-BAN-B.md`, `docs/KIEN-TRUC-4-LOP.md:264`,
  `docs/NGON-NGU.md`, `.claude/commands/thi-hanh.md`, `.gitattributes`.
- **Cố ý không đụng**: `docs/DAC-TA-KICH-BAN-B.md` và `docs/DAC-TA-TRIEN-KHAI-KICH-BAN-B.md` (bảng theo dõi
  đang sống, PR tới #187) — hai file này không trùng lặp nội dung với nhau (một mức epic, một mức PR-theo-PR),
  gộp chúng sẽ tái tạo đúng lỗi `KHUON-THI-HANH.md` §6 cảnh báo ("một file thay ba"). Cũng không đụng
  session log/ADR/CHANGELOG cũ có nhắc hai đường dẫn cũ — đó là bản ghi lịch sử, sửa lại là viết lại quá khứ.
- Rebase lên `main` sau khi #190 (4L-6) và #191 (K3.6c) đã merge; `docs/KIEN-TRUC-4-LOP.md` và
  `docs/DAC-TA-KICH-BAN-B.md` đã trôi theo hai PR đó — chỉ gộp lại đoạn "đã lưu trữ", không sửa nội dung khác
  của họ.

---

# K3.6d2 — `AgentRunner` lên `xagents_core`; K3.6 XONG (#195)

## Bài học lớn nhất: đo hai chiều đã bắt được một lỗ hổng của chính bộ test

Đột biến hai quyết định hợp nhất — bật `wants_content` và ép `_new_envelope` dùng `inp.child()` — làm **ba ca
của core đỏ**, nhưng **cả 557 ca của Studio-creators vẫn xanh**. Nghĩa là: nếu một phiên sau thấy hai hook ấy
"thừa" và dọn đi cho gọn, studio sẽ ghi một audit rác **mỗi lần** ghi context và đổi hình dạng event trên bus,
mà không có gì trong suite studio đỏ.

Ca ở core chỉ chứng minh *cơ chế chạy đúng*. Nó **không** chứng minh *công ty B vẫn giữ hành vi của mình* —
muốn thế thì ca phải nằm ở công ty B. Đã thêm `Studio-creators/tests/test_runner_core.py` (3 ca), đo lại thì cả
hai đột biến làm studio đỏ đúng chỗ.

Đây là lần thứ ba trong chuỗi K3.6 mà bước "đo hai chiều" trả về thứ không ai đoán trước (K3.6b: ca đồng thời
xanh cả khi bỏ khoá; K3.6c: seam `monkeypatch` mất tác dụng và test ghi đè bản ghi eval thật). **Ba lần, ba
loại lỗi khác nhau, không lần nào phát hiện được bằng đọc code.**

## Nguyên tắc của d2, khác các bước trước

Bốn bước đầu của K3.6 nâng studio lên bản company và **ghi CHANGELOG** khi đổi hành vi. d2 thì không: ở đây
*hành vi quan sát được của mỗi công ty giữ nguyên từng byte*, mọi chỗ lệch thành hook. Lý do: hai chỗ lệch của
d2 không phải "studio thiếu", mà là **hợp đồng đầu ra** (`context_writes` có `content` không) và **dữ liệu trên
bus** (event có cha không). Nâng chúng lên không phải cải tiến âm thầm được — nó là đổi dữ liệu.

## Chỗ đặc tả đoán sai

- "`MAX_TOOL_TURNS` studio → `CoreConfig.max_tool_turns`": **không cần**. Vòng lặp tool ở lại từng công ty (nó
  dựng `tools_prompt`, tức chạm prompt), nên hằng ấy ở lại cùng nó.
- "hook `toolbox_for(spec, ctx) -> ToolBox` do package cung cấp": studio đã có `toolbox_factory` từ trước và
  company cấp tool theo đường khác; không cần thêm một hook chung ở core.

## Nghiệm thu đã chạy

`evals all --replay` **0 FAIL** cả hai công ty (ràng buộc prompt của d1 còn nguyên) · `make demo` hai công ty
EXIT=0 · company 1057 + 100% · studio 560 + 5 skipped + 100% · core 340 + 100% · console 237 · gateway 251 ·
`ruff check .` sạch · mypy **chạy trong thư mục từng package** đều Success. **Không sửa một dòng test nào** của
hai công ty.

## Còn lại của kịch bản B

- **K3.7** (`gates`, `gate_cli`, `supervisor` hợp nhất hai chiều) — bước cuối của K3.
- **K3.e chưa đóng**: "chạy thật company ≥ 5 ngày lịch sau K3.5". Mốc của NGƯỜI; test xanh không đóng được.
- **Studio vẫn chưa gọi `Blackboard.rehydrate()`** (nợ từ K3.6b): mở lại `studio.sqlite` là blackboard rỗng.

---

## `/thi-hanh 4l` — đợt 4 (4L-7): trace studio + hẹn hoãn bền

- Task pack 4L-7 viết ngày 2026-09-08, lệch với code thật ở **một điểm đáng kể**: nó nói `_defer` "chỉ giữ
  hẹn trong RAM, cần ghi thêm `event_id` vào audit-log". Thực tế `_defer` ĐÃ ghi `defer.until` kèm `event_id`
  từ K3.3d; thứ thiếu là đầu bên kia — không có ai đọc bản ghi ấy lúc mở lại. Nên gói này chỉ thêm
  `Orchestrator._nap_lai_hen` (đối xứng `company/orch/rehydrate.py:_nap_lai_hen`), không đổi hình `defer.until`.
- `xagents_core/trace.py` (MỚI) giữ phần chung: `base_row`/`audit_row` (tên action của audit là chung hai công
  ty vì cùng `AgentRunner`/`PersistentGate` sinh ra), `_gop_lap`, `summarize`, `render`, `run`, `_hms`/`_wait`.
  Hai thứ ở lại từng miền: **chủ thể là gì + event nào thuộc về nó** (core chỉ nhận envelope đã lọc sẵn, không
  biết tên topic nào) và **dòng của topic riêng miền** (hook `domain`).
- `studio/trace.py` (MỚI): `python -m studio.trace <VIDEO|PLAN-…|CHANNEL>`, mở SQLite `mode=ro`.
- `last_sync` KHÔNG đụng: nó là cache "lần kéo số liệu gần nhất" của `_sync`, mất đi chỉ làm kéo sớm một nhịp
  — khác họ với hẹn hoãn (mất đi là đập vào backend đang cạn quota).
- Rebase lên `main` xung đột ở README (số ca/file test — K3.6d2 xen giữa lúc dispatch) và session log (hai
  mục cùng ngày chèn cạnh nhau) — không xung đột code thật, cả hai nội dung ghép nối theo đúng thứ tự thời
  gian. 4L-7 xong → PR #196; CHANGELOG dòng 4L-7 ban đầu xếp SAI VỊ TRÍ (giữa các dòng K3.6 cũ) — luật 10 đòi
  xếp theo thời điểm MERGE chứ không phải lúc soạn, PR #196 merge sau #195 nên phải nằm trên đầu "Chưa phát
  hành" — đã sửa lại trước khi push.
- **Bảng B `docs/KIEN-TRUC-4-LOP.md`**: 1a `xong #182` · 1b `chờ người` (cần key) · 2 `xong #183` · 3
  `xong #184` · 4 `chờ người` (K3.6 đã xong toàn bộ #187-#195, đủ điều kiện vào đợt 5) · 5 `xong #185` ·
  6 `xong #190` · 7 `xong #196` · 8 `xong #181`.

## Kiểm 4L-4 (đợt 5) — vẫn `chờ người`, lý do khác lần trước

- Điều kiện vào đợt "K3.6, 4L-2, 4L-3, ngày có key" nay chỉ còn thiếu **key**: K3.6 xong (#187-#196), 4L-2
  (#183) và 4L-3 (#184) đã merge từ trước.
- Đọc lại khối 7 mục 4L-4: mục 2 nói rõ "bản ghi hai công ty ghi lại (hash user message đổi) — nói rõ trong
  PR, không phải bỏ bước". `_prune(msgs, keep_turns=3)` cắt tool output cũ hơn 3 lượt TRƯỚC khi gửi cho model
  — với ticket có > 3 lượt tool, `user_message` (dùng làm khoá bản ghi eval `hash(system, user_message)`) ở
  lượt 4 trở đi sẽ đổi nội dung, làm bản ghi cũ không còn khớp hash. Bản ghi lại đòi **model thật**
  (`make eval-record`, đủ 7 bước `CONTRIBUTING.md` §3) — cùng ràng buộc với 4L-1b.
- Xác nhận phiên remote này không có key: `env | grep API_KEY` rỗng, không có `llm.yaml`/`media.yaml`. Không
  thể hoàn tất 4L-4 an toàn (viết `_prune` mà không ghi lại được bản ghi thật để xác nhận sẽ để lại eval đỏ
  hoặc một PR không nghiệm thu được đúng cách) — giữ nguyên `chờ người`, không đoán liều.
- `make test` gốc (cả năm package, `main@4b51f16` — chạy đúng trong worktree dựa trên `origin/main` mới nhất,
  KHÔNG phải nhánh gốc phiên `claude/thi-hanh-4l-jl6rpu` đã cũ): **xanh** — console 237, gateway 251,
  company 1057, studio 569 + 5 skip, xagents-core 364, tất cả exit 0.

## Kết `/thi-hanh 4l` (lần hai, sau khi K3.5c/K3.6 merge)

Bảy trong tám mã của bảng B đã `xong #n` (#181-#185, #190, #196 — tám PR merge trong phiên này tính cả lần
soạn #180-#181 và các đợt sau). Còn lại: **4L-1b** và **4L-4**, cả hai `chờ người` vì cùng một lý do — cần
máy có key model thật để chạy `make eval-record` đủ 7 bước `CONTRIBUTING.md` §3. Không có gì trong repo chặn
hai việc này ngoài việc đó; người có key chạy lại `/thi-hanh 4l` (đọc bảng B, idempotent qua file) để hoàn tất.

## `/thi-hanh k3.7` — gates/gate_cli/supervisor hợp nhất, bước cuối kịch bản B

- Người dùng yêu cầu "thi hành toàn bộ việc không bị chặn" sau khi phiên trước chốt còn lại K3.7 (không cần
  key) và một nghi ngờ nợ `Blackboard.rehydrate()` cho studio. Khảo sát lại bằng ba `Explore` song song
  (company/studio/xagents-core) trước khi soạn: nghi ngờ rehydrate là **false lead** — cả company lẫn studio
  đều không gọi method đó, cả hai cùng inline `_on(env)` trong một vòng replay log duy nhất (đối xứng, không
  phải nợ riêng của studio) — bỏ khỏi việc cần làm, không sửa.
- Soạn `docs/thi-hanh/k3.7.md` (mục A-F), giao một subagent Opus thực thi trong chính working directory của
  phiên (không tạo worktree/branch mới — phiên đã ở branch được giao `claude/viec-con-lai-vz2vdk`). Kết quả:
  `xagents_core/{gates,gate_cli,supervisor,ticket_model}.py` mới, company/studio kế thừa, giữ tên method khác
  miền. `COMPANY_GATE_APPROVERS` mới (mặc định tắt = hành vi cũ).
- Tự chạy lại CI bốn package (không chỉ tin báo cáo subagent) — khớp: xagents-core 408, company 1059→1060 (sau
  khi thêm ca), studio 569+5 skip, console 237→238 (sau khi thêm ca), tất cả 100% coverage.
- Gọi `sc-qa` + `sc-security` chấm gói song song. `sc-qa`: không có mâu thuẫn. `sc-security`: xác nhận logic
  bảo mật giữ đúng, nhưng phát hiện **2 lỗ hổng CAO do chính tính năng `COMPANY_GATE_APPROVERS` mới sinh ra**:
  (1) `console/decide.py` mở gate company không truyền `approvers` — allowlist bị bỏ qua trên đúng đường
  console; (2) `_close_acceptance_gate` thiếu `enforce=False` — bật allowlist làm gate nghiệm thu khách
  (`UAT-*`) không đóng được vì chữ ký khách không phải người duyệt nội bộ, lỗi bị nuốt thành `handler_error`,
  ticket đóng băng vĩnh viễn. Vá cả hai, thêm ca test đo hai chiều cho từng cái (bỏ sửa → đỏ, dán output, khôi
  phục → xanh) — chính tay kiểm lại, không giao lại subagent (nhỏ, rõ ràng, giống tiền lệ 2026-09-09).
- Hai phát hiện còn lại của `sc-security` (NGHIÊM TRỌNG: `gate.request` không kiểm actor; TRUNG BÌNH: studio
  actor lạ đóng gate rồi mất khi replay) — đối chiếu `git show main@34c4952` xác nhận **pre-existing từ trước
  K3.7 ở cả hai công ty**, không phải K3.7 sinh ra. Ghi rõ trong k3.7.md mục G, không vá — cần gói ACL riêng
  theo ADR-0002, ngoài phạm vi "hợp nhất module" của gói này.
- K3.7 xong → PR #198, auto-merge squash bật, đang theo dõi CI qua `subscribe_pr_activity`.

**Đang mở**: PR #198 (K3.7) — chờ CI + auto-merge.
**Người sau KHÔNG được quên**: 4L-1b và 4L-4 vẫn `chờ người` (cần key model thật). Gói ACL cho `gate.request`
(không kiểm actor, pre-existing) chưa có mã việc — cần mở nếu muốn đóng bất biến ADR-0002 rộng hơn.

## Vá lỗ hổng bypass four-eyes ở `HumanGate.request()` (nối tiếp phát hiện `sc-security` ở K3.7)

- Việc đúng như dòng "Người sau KHÔNG được quên" ở trên: lỗ hổng `gate.request()` không kiểm `created_by`
  rỗng/None (làm ngắn mạch four-eyes ở `decide()`) đã có mã việc và được xử lý ngay trong phiên này, không
  chờ gói ACL riêng — vì phạm vi đủ nhỏ (một hàm, một bất biến) để không cần hoãn.
- ADR trước khi sửa (luật bắt buộc #2): `docs/adr/0005-gate-request-tu-choi-created-by-rong.md` — bối cảnh,
  quyết định (allowlist mặc định từ chối ở `request()`, không phải vá lại điều kiện ở `decide()`), hệ quả.
- Worktree riêng `../Claude-Agents-wt-gate-acl`, nhánh `fix/gate-request-actor-acl` từ `origin/main` mới nhất
  (không đụng ba worktree khác đang mở: `loving-kirch-836ab2`, `Claude-Agents-dev`, `Claude-Agents-wt-sd`).
- Grep `GateRequest(` toàn `software-company/src`, `Studio-creators/src`, `xagents-core/src`: mọi call site
  sản phẩm đã truyền `created_by` hợp lệ — không có bug thật nào cần lộ ra thêm.
- Sửa `xagents_core/gates.py::HumanGate.request()`: raise `PermissionError` nếu `created_by` rỗng/None/toàn
  khoảng trắng sau `.strip()`.
- Test hai chiều (`xagents-core/tests/test_gates.py`): tạm revert bản vá → 3 ca `created_by=None/""/"   "`
  ĐỎ đúng chỗ (`DID NOT RAISE PermissionError`); khôi phục → 15/15 xanh, toàn `xagents-core` 412/412 xanh,
  coverage 100%. `software-company` phát hiện 1 test thiếu `created_by`
  (`test_delivery_and_gates.py::test_gate_timeouts`) — sửa call site đó (không nới lỏng lỗ hổng), rồi 1059
  passed/1 skipped, coverage 100%. `Studio-creators` 574 passed không đụng gì. `ruff check .` và `mypy` ba
  package: sạch.
- PR #199 `fix(core): HumanGate.request() từ chối created_by rỗng/None`, auto-merge squash, tất cả CI job xanh
  (`gh pr checks --watch`), merged lúc 2026-09-09T04:25:24Z. CHANGELOG cập nhật, worktree đã dọn.

**Người sau KHÔNG được quên**: dòng "gói ACL cho `gate.request`" ở trên đã đóng — bất biến ADR-0002 (four-eyes)
nay được bảo vệ cả ở `request()` lẫn `decide()`. Không còn việc `chờ người` mới từ phiên này.

## Đợt sau: ADR-0006 — công ty con bảo trì `keeper`

Người dùng hỏi "GitHub có agent bảo trì dự án không", rồi yêu cầu **gộp thành một công ty con bảo trì toàn
dự án, nhiều tính năng nâng cao**. Đổi kiến trúc ≥ 2 package → ADR trước (`AGENTS.md` bắt buộc §2).

- Viết `docs/adr/0006-cong-ty-bao-tri-keeper.md` (#201): package thứ sáu `keeper/` (dist
  `keeper`, package Python `keeper`), 6 khối / 8 agent / human gate `keeper` mới; xây thẳng trên
  `xagents_core`, không sao chép từ `company`/`studio`.
- Ý chính: **không viết lại** Renovate/CodeQL/Scorecard mà tiêu thụ đầu ra của chúng làm tín hiệu; phần khác
  biệt là biến luật repo (`AGENTS.md` §4 đo hai chiều, §5 rà cả họ lỗi, §10 tài liệu đi cùng PR,
  `CONTRIBUTING.md` §3 bảy bước) thành **cổng máy** thay vì lời nhắc.
- Ngân sách thay đổi giữ đúng luật một-PR-mở (`docs/QUY-TRINH-GIT.md` §2c) — chống chính công ty này gây bão PR.
- Khách hàng số 0 là chính X-Agents (canary), chỉ mở cho repo khách sau một chu kỳ.

**Việc dở / người sau không được quên**: ADR-0006 mới là quyết định, **chưa có một dòng mã nào**. Lộ trình 5
PR ghi ở mục "Hệ quả" của ADR; PR (1) khung package + `pyproject.toml` + CI là bước kế tiếp, và phải đạt
`fail_under = 100` như năm package kia ngay từ PR đầu.

## Audit toàn diện (tính năng · quy trình · code) và đợt vá theo audit

Người dùng yêu cầu audit toàn diện rồi "lên kế hoạch và fix toàn bộ". Bảy phát hiện, xếp theo mức nghiêm trọng;
PR này (#202) đóng hai phát hiện đầu.

**Cách audit** (không tin lời khai, luật cấm #8): `git fetch` trước — local đang tụt 2 commit và
`docs/adr/0005-*.md` nằm untracked **y hệt** bản trên origin (dấu vết phiên trước chưa dọn). `make` KHÔNG có
trên máy này: `make test` trả exit 127, tức là mọi lần ai đó báo "make test xanh" trên Windows đều là lời khai
rỗng — phải `cd <pkg> && uv run pytest`. Chạy thật cả năm package, rồi mở **dữ liệu chạy thật** thay vì chỉ đọc
code: chính bước đó lộ ra hai lỗi nghiêm trọng nhất, không test nào trong 2535 ca bắt được.

**Phát hiện** (chi tiết + bằng chứng trong CHANGELOG và mô tả PR):
1. NGHIÊM TRỌNG — DB QLKH không rehydrate được (`assignee` thế hệ cũ). **Vá trong #202.**
2. CAO — `integration.skipped` chiếm 78% audit-log. **Vá trong #202.**
3. CAO — bộ điều khiển chi phí đo 0 USD trên 137 triệu token: `llm.yaml` không có bảng `prices` cho model đi
   qua gateway, nên `cost_usd` luôn 0.0 và `budget_usd`/`project_budget_usd` không bao giờ chạm trần. Cờ
   `unpriced` CÓ được đếm (`supervisor.py:112`) nhưng không hành động — guardrail ngân sách là no-op im lặng.
4. CAO — `_the_he` (scheduler.py) định danh thế hệ gate bằng `created_at.isoformat()`: `datetime.now(UTC)` trên
   Windows có bước ~15,6 ms, đo được `timedelta(0)` giữa hai lần gọi liên tiếp. Hai gate cùng `subject_id` mở
   cách nhau <16 ms dùng chung khoá `once` ⇒ lần quá hạn của gate thứ hai bị nuốt — đúng TRAPS §1 khuôn 3 mà
   `_the_he` sinh ra để chặn. Làm đỏ `test_khuon3_gate_the_he_hai_van_duoc_escalate` và
   `test_overdue_khong_truyen_now_thi_lay_bay_gio`, không phải mọi lần chạy (máy chậm thì lọt).
5. TRUNG BÌNH — **vá trong #205** — CI không có chân Windows cho `core-unit`/`gateway-unit`/`console-unit`; K3.1–K3.7 vừa dồn toàn
   bộ cơ chế chung vào `xagents-core` nên package rủi ro cao nhất đang có phạm vi nền tảng hẹp nhất.
6. TRUNG BÌNH — README gốc lệch: vẫn viết "Còn K3.5c rồi K3.6–K3.7" (cả ba đã merge #187–#198); số test
   xagents-core 236 / thực 412, Studio 514 / thực 574. `software-company/README.md` thì KHÔNG lệch vì có test
   canh (`test_readme_khop_so_lieu_that`) — bài học: chỗ nào có bánh cóc thì chỗ đó không trôi.
7. THẤP — `protection-guard` dùng `actions/checkout@v4` (mọi job khác `@v7`; **vá trong #205** cùng `pr-policy.yml`, chỗ cuối còn v4 — nó đang sinh cảnh báo *Node.js 20 is deprecated* thật, đọc được trong annotation của run #203); `docs/adr/0002` trống chỗ mà
   `docs/adr/README.md` không nói; 4 worktree cũ còn treo; `company.sqlite` rỗng ở gốc repo.

**Đã học được khi vá**: bánh cóc của repo hoạt động đúng như thiết kế — bản vá làm đỏ
`test_k63_banh_coc_dict_any_trong_orch_khong_duoc_tang` (27 → 28, đã sửa kèm lý do) và
`test_readme_khop_so_lieu_that` (số ca/file test). Cả hai đều là cổng ĐÚNG, không phải phiền toái.

**Người sau KHÔNG được quên**: phát hiện 3–7 chưa vá, đi theo thứ tự 4 → 5 → 3 → 6 → 7, mỗi cái một PR (luật
một PR mở tại một thời điểm). Phát hiện 4 phải sửa bằng BỘ ĐẾM đơn điệu trên `HumanGate`, không phải bằng
`time.monotonic_ns()` — thế hệ phải sống sót qua restart, mà đồng hồ monotonic thì không.

## Đợt sau: BT0 — công ty bảo trì đổi tên `keeper`, có đặc tả triển khai

Người dùng chốt tên **`keeper`** và yêu cầu đặc tả chi tiết để triển khai, lưu trong `keeper/` trên repo.

- Đổi tên ADR-0006 (`git mv` + nội dung): `Upkeep-crew` → `keeper`, `upkeep-supervisor` → `keeper-supervisor`,
  gate `upkeep` → `keeper`; lộ trình 5 PR → 9 PR trỏ đặc tả mới.
- `keeper/docs/DAC-TA-KEEPER.md` (#207): BT0–BT8, mỗi mục có bảng file, test **đo hai chiều**, lệnh nghiệm
  thu, cạm bẫy đã biết. §0 là 7 bất biến, mỗi cái kèm cột "ép bằng" — I1 (không quyền ghi) thành
  `FORBIDDEN_ARGS` trong `github.py`, I2 (bằng chứng hai chiều) thành `require_two_way()`, I3 (một PR mở)
  thành `budget.can_open_pr()` hỏi GitHub thật.
- Bẫy đã nạp sẵn vào đặc tả từ bài học cũ: `verified_by` không được nhận từ JSON model (lời khai); khoá
  chống-trùng phải mang thế hệ; `gate_cli approve` là reopen; không `reset --hard` checkout chung; không
  dựng cổng đúng-sai theo nền tảng.

**Việc dở / người sau không được quên**: vẫn **chưa có một dòng mã** nào của `keeper`. Bước kế tiếp là BT1
(khung package + `pyproject.toml` gốc + hai job CI), phải đạt `fail_under = 100` ngay từ PR đầu.
Ngoài ra: có file chưa theo dõi `docs/adr/0006-tia-tool-output-cu-trong-vong-tool.md` của phiên khác —
**số 0006 đã bị ADR công ty bảo trì lấy** (#201), phiên đó phải đổi thành 0007.

---

## 4L-1b + 4L-4 chạy bằng gói subscription CLI (`claude-liendv`), không cần API key — kịch bản B 4 lớp 8/8 XONG

- Người dùng hỏi "4L-1b, 4L-4 chạy cli tài khoản sub" sau khi hai việc này bị đóng băng ở `chờ người` (cần
  "máy có key model thật"). Kiểm lại: `software-company/llm.yaml` (thật, gitignore) đã có sẵn backend
  `claude-liendv` (`provider: claude-code`, `config_dir: ~/.claude-liendv`) — CLI `claude -p` với tài khoản
  subscription Pro/Max đang đăng nhập, KHÔNG trả tiền theo token. Test gọi thật: `claude -p "..."` trả lời
  đúng. Vậy "cần key" ở các phiên trước là hiểu nhầm phạm vi (đúng ra là "cần MỘT NGUỒN MODEL THẬT nào đó", gói
  sub cũng đủ, không nhất thiết API key trả theo token) — sửa lại hiểu biết này cho các lần sau.
- **4L-1b** (`evals/security.yaml` 2→10 ca, ghi lại `make eval-record` bằng model thật) → PR **#204**
  (`chore(company): 4L-1b — security ≥ 10 ca eval`), auto-merge, CI xanh (`1059 passed, 1 skipped,
  coverage 100%`). Bảng B dòng 1b: `chờ người` → `xong #204`.
- **4L-4** (tỉa tool output cũ trong vòng tool) — theo đúng thứ tự bảng D "ADR trước, code sau":
  - ADR: `docs/adr/0007-tia-tool-output-cu-trong-vong-tool.md` (đặt số 0007 vì 0006 đã bị ADR công ty
    `keeper`/Upkeep-crew chiếm — #201) → PR **#206** (`docs: ADR-0007 — tỉa tool output cũ trong vòng tool`),
    merge.
  - Code: `_prune(msgs, keep_turns=3)` thuần ở `xagents_core.context` (không phải sẵn có ở core như đặc tả
    giả định — `_turns`/`_tool_loop` vẫn là hai vòng tool riêng của `company/runner.py` và
    `studio/runner.py`, chưa hợp nhất dù K3.6d2 đã đưa `AgentRunner` lên core; hợp nhất trọn vòng tool ngoài
    ngân sách gói này, ghi rõ "không thuộc phạm vi" trong ADR). Placeholder cắt có hash, không tỉa `msgs[0]`,
    giữ `tool_calls`. Bẫy biên đã vá: `turn_starts[-keep_turns]` với `keep_turns=0` bị Python đọc thành
    `turn_starts[-0] == turn_starts[0]` ("giữ mọi lượt" thay vì "tỉa hết") — thêm nhánh `if keep_turns > 0`.
  - Đo hai chiều: `test_khong_tia_thi_vuot_3x` (monkeypatch `_prune` no-op) → lượt cuối VƯỢT 3× mốc ổn định
    (đo thật: 15 lượt × 6k/lượt, không tỉa ≈ 4.7×, có tỉa ≈ 1.0×) — đỏ đúng hướng khi tắt, xanh khi bật.
  - `evals all --replay --strict` **cả hai công ty**: mọi bản ghi hiện có PASS KHÔNG ĐỔI — khoá bản ghi eval
    là `hash(system, USER MESSAGE ĐẦU)`, `_prune` chỉ sửa `msgs` bên trong vòng (từ lượt 4), không sửa `user`
    gốc, và bộ eval hiện có (company 58 ca/6 agent, studio 28 ca/14 agent) không ca nào chạm ≥ 4 lượt tool để
    `_prune` từng kích hoạt — nên không cần ghi lại toàn bộ eval như đặc tả gốc lo ngại.
  - Studio: eval-record bằng model thật lỗi CLI `error_max_turns` (studio không có `llm.yaml` thật ở máy
    này) — KHÔNG phải lỗi của `_prune`; đã revert file ghi hỏng, không commit dữ liệu thất bại. Không bắt
    buộc cho gói này vì replay không đổi; cần người cấu hình `llm.yaml` studio + gỡ lỗi CLI riêng nếu muốn
    ghi lại thật sau này.
  - CI ba package xanh 100% coverage: xagents-core 417, software-company 1072+1 skip, Studio-creators 575.
    `make demo` ×2 sạch. → PR **#208** (`feat(core): 4L-4 — tỉa tool output cũ trong vòng tool (ADR-0007)`),
    auto-merge, merge. Bảng B dòng 4: `chờ người` → `xong #208`.
- **Kẹt luật một-PR-một-lúc nhiều lần**: cả #204, #206, #208 đều phải chờ PR của phiên khác (#203, #205 —
  không liên quan) merge trước mới mở được, đúng luật 2b `docs/QUY-TRINH-GIT.md`. Một agent nền tự động đã
  poll ~55 phút liên tục chờ hàng đợi trống rồi tự dừng báo "chờ người" đúng lúc — phiên chính tiếp quản, đẩy
  hai nhánh đã commit sẵn cục bộ (`docs/4l-4-adr-0006`, `feat/4l-4-prune`) và mở nốt hai PR khi hàng đợi
  trống. Một agent khác (chạy song song, không rõ nguồn) cũng tự mở PR #206 — trùng nỗ lực nhưng không hại vì
  chỉ một PR merge, cần chú ý dấu hiệu "hai agent cùng làm một việc" ở các lần sau.
- `metadata` CI check (đòi dòng CHANGELOG.md) chặn PR docs-only hai lần (#200, #206) — nhãn `no-changelog`
  KHÔNG tự kích lại check cũ, phải `gh run rerun <id> --failed` hoặc thêm dòng CHANGELOG thật để job chạy lại.
- **Kịch bản B 4 lớp — ĐỦ 8/8 mã đã `xong #n`**: 1a #182 · 1b #204 · 2 #183 · 3 #184 · 4 #206+#208 · 5 #185 ·
  6 #190 · 7 #196 · 8 #181. Không còn dòng nào `chờ người` trong bảng B.
- **Còn treo, ngoài phạm vi kịch bản B**: gói ACL cho `gate.request` không kiểm actor thô (khác lỗ hổng
  `created_by` rỗng đã vá ở PR #199 — đây là phát hiện NGHIÊM TRỌNG còn lại của `sc-security` từ K3.7, cần
  ADR-0002 riêng); studio chưa ghi được eval bằng model thật (thiếu `llm.yaml` cấu hình + lỗi CLI
  `error_max_turns` chưa gỡ).

## /thi-hanh keeper — BT1: khung package thứ sáu (#210)

- Đo hiện trạng bằng 4 subagent `Explore` song song (lõi `xagents_core` · khuôn package mới + CI · cơ chế nợ/`once`/worktree
  của company · console + tài liệu + script sinh `sc-*`). Kế hoạch, bảng theo dõi và mục F ở `docs/thi-hanh/keeper.md`.
- **Năm chỗ đặc tả sai**, đo được chứ không suy: (1) BT1 nói 9 schema "sinh từ pydantic bằng script, như hai công ty
  kia" — repo **không có generator nào**, 19 file JSON của studio là artefact commit tay ép khớp bằng
  `Studio-creators/tests/test_bus.py:46`; (2) `SqliteBus` tên thật là `SQLiteBus` (`xagents_core/sqlite_bus.py:57`);
  (3) BT4 bảo "dùng lại `debt_due` của company, đọc `company/orch/`" — nó ở lõi (`xagents_core/supervisor.py:82`) và
  đo **chuỗi review liên tiếp** (`streak`/`fired`), không đo ngày, nên `DebtEntry(due_at=…)` là cơ chế MỚI;
  (4) cạm bẫy §9 nói `approve` là reopen — sai: `gates.py:87 decide()` chỉ đóng gate và đẩy vào `history`, reopen là
  nghĩa riêng của vài escalation do orchestrator quyết (`orch/gates_flow.py:110-117`); (5) `Topic` có 10 giá trị
  (8 topic riêng + `audit-log` + `shared-context`), đặc tả đếm 9. Hai chỗ đầu đã sửa trong `DAC-TA-KEEPER.md`;
  ba chỗ còn lại sửa ở PR của BT tương ứng.
- BT1 chạm nhiều hơn bảng đặc tả liệt kê: cấu hình Makefile `MEMBERS`, `ci.yml` `needs` của job `quality`, `uv.lock`, CODEOWNERS, `.gitignore`.
- **Việc dở / người sau không được quên**: BT2–BT8 còn nguyên. BT7 sẽ `chờ người` ở `make eval-record` (cần key model thật); BT8 canary `chờ người` (cần `gh auth login` + người merge vì bất biến I1 cấm keeper tự merge).

## /thi-hanh keeper — BT2: cầu `gh` chỉ đọc (#211)

- `GitHubReader` sao khuôn `software-company/src/company/github_pr.py:62` (`encoding="utf-8"`, `env=clean_env()`,
  bắt `FileNotFoundError`/`TimeoutExpired`, không bao giờ ném lỗi tiến trình) — sao khuôn, không import chéo công ty.
- **Lỗ bất biến I1 trong chính đặc tả**, phát hiện khi đọc lại diff chứ không phải khi chạy test: bảng
  `FORBIDDEN_ARGS` của BT0 chặn `-X`/`--method` và bốn subcommand ghi, nhưng `gh api` đổi phương thức sang POST
  ngay khi có **bất kỳ trường nào** (`-f`, `-F`, `--field`, `--raw-field`, `--input`) — không cần `-X`. Nghĩa là
  `gh api repos/o/r/pulls/1/merge -f merge_method=squash` là một lời gọi **ghi** đi lọt: token `merge` nằm trong
  đường dẫn `repos/.../merge` nên phép so khớp tuyệt đối không thấy. Đã thêm năm cờ, 5 ca chặn + 1 ca chiều ngược.
  Bài học: một bảng chặn viết theo *danh sách lệnh nguy hiểm* luôn hụt so với bảng viết theo *cơ chế gây nguy hiểm*.
- `_VALUE_FLAGS` bỏ qua token ngay sau cờ nhận giá trị, để repo tên `delete-me` hay nhánh `edit/foo` không bị
  chặn nhầm — có test riêng cho ca này.

**Việc dở**: các model `DependabotAlert`/`CodeScanningAlert` chưa đối chiếu với `gh` thật lần nào (test dùng
`FakeGitHub`, I1 cấm chạm mạng trong CI). Cần một lần soi tay khi BT3 tiêu thụ chúng.

## Đóng nốt phát hiện NGHIÊM TRỌNG của `sc-security` — ADR-0002 (#212)

- Phát hiện còn lại từ khi chấm K3.7 ("`gate.request` không kiểm actor thô") nay có ADR và bản vá.
  **Khác ADR-0005** (`created_by` rỗng → gate không có chủ): ở đây gate có chủ nhưng chủ là **bịa**.
  Đường khai thác đo được: publish `gate.request` với `actor="human:evil"`, `evidence.created_by="human:a"`
  → gate vào `pending` mang chủ `human:a` → `human:evil` gọi `decide(by="human:evil")`, four-eyes so với
  `human:a` nên qua. `COMPANY_GATE_APPROVERS` không cứu: kẻ khai thác phải nằm trong danh sách mới làm được,
  mà đó đúng là kịch bản four-eyes sinh ra để chặn.
- Vá: `PersistentGate.apply()` lấy `created_by` từ `env.actor`, bỏ `created_by` trong evidence — cùng lý lẽ
  `trusted_decision` (evidence là lời khai; `env.actor` là thứ bus kiểm). **Ghi đè chứ không từ chối bản ghi
  lệch**: từ chối sẽ đánh rơi gate cũ ghi trước ADR-0005 (`created_by=None`, `actor="human"`) khỏi replay.
- Đo hai chiều: bỏ bản sửa → `test_created_by_lay_tu_actor_that_khong_phai_evidence_tu_khai` đỏ ở
  `assert 'human:a' == 'human:evil'`; khôi phục → 23/23 xanh, core 418, studio 575, console 236+2 skip.
- **Còn mở, cố ý không làm ở đây**: ACL producer cho `gate.request` theo vai (chặn hẳn gate ma — `gate.request`
  là việc hợp lệ của agent nên phải là allowlist theo vai, gói riêng); phát hiện TRUNG BÌNH của studio (actor
  lạ đóng gate rồi mất khi replay); hai phát hiện THẤP về ACL `orchestrator` — tất cả pre-existing.
- **Studio chưa ghi lại được eval bằng model thật** trên máy này: thiếu `Studio-creators/llm.yaml` (chỉ có
  `*.example.yaml`), `make eval-record` không chạy được và CLI báo `error_max_turns` chưa gỡ. Không chặn 4L-4
  (replay không đổi) nhưng **người sau cần biết**: bất kỳ việc nào cần `make eval-record AGENT=…` của studio
  vẫn ở trạng thái `chờ người` cho tới khi có key model thật.

## /thi-hanh keeper — BT3: khối watch (#213)

- Ba phép so của `drift` đều **thuần cục bộ**, không chạm mạng; `drift` chỉ phát signal, không xoá gì (I5).
- **Hai chỗ đặc tả sai, đo được**: (a) §5 nói so "hash nguồn ghi trong front matter" của `sc-*.md` — thật ra là số
  `version` trong HTML comment SAU front matter (`software-company/src/company/subagents.py:131`); (b) `WorkflowRun`
  của BT2 xin sai tên trường thời gian — `gh run list --json` dùng `startedAt`/`updatedAt`.
- Mốc chặn dưới cho phép so CHANGELOG **đo chứ không đoán**: `git log -p --follow -- AGENTS.md` → commit thêm luật
  §10 là `a82a804`, `2026-09-07T19:41:01+07:00`. Đã kiểm lại bằng `git show -s --format=%aI`. Không dùng danh sách
  số PR — danh sách mục ngay tuần sau.
- **Một phần bị bỏ dở rồi giao lại**: `health.py` ban đầu bỏ p50/p95 với lý do `WorkflowRun` thiếu trường thời gian.
  Đó là chỗ *thiếu trường*, không phải chỗ *không làm được* — thu hẹp phạm vi không phải quyết định của gói. Đã làm
  nốt; mẫu rỗng trả `None` chứ không phải `0.0` (0.0 là "CI nhanh vô hạn", `None` là "chưa đo được").
- **`sc-qa` bắt hai chỗ**: một ca dán nhãn "chiều ngược" mà chỉ đếm fixture đầu vào, không chạm `dedupe` — đúng
  khuôn lỗi BT1 đã mắc, đã thay bằng ca đo thật; và `semver_jump("1.2.3", "1.2.3rc1")` trả `"patch"` cho một bump
  không hề xảy ra. Chỗ thứ hai không chỉ sai tên: `risk_tier` (BT4) cho patch **tự merge**, nên một pre-release bị
  gọi nhầm là patch sẽ đi thẳng vào nhánh không cần người duyệt. Nay trả `None` — không phân loại còn hơn phân loại sai.
## Studio ghi eval bằng model thật — gỡ `error_max_turns` (#214)

- Người dùng cho phép dùng gateway thật. `cp llm.claude-gateway.yaml llm.yaml` (gateway đang chạy, 3 tài khoản
  trong pool; `gateway models --check` xanh cả 6 dòng) → studio gọi được model thật. **Thiếu `llm.yaml` chỉ là
  nửa vấn đề**: có nó rồi vẫn chết `error_max_turns` ở `seo-optimizer/kich-ban-phai-ra-metadata-dung-gioi-han`.
- Root cause đo được, KHÔNG đoán: đường **không tool** của `studio.llm.ClaudeCodeClient` truyền `--max-turns 1`,
  mà `--json-schema` (ADR-0026) ép JSON bằng một lượt CLI nữa → lượt ép bị cắt. Company đã đo đúng lỗi này
  **2026-09-05** và vá bằng `CLI_NO_TOOL_TURNS = 6`; studio không được port suốt từ đó. Đây là lý do bản ghi eval
  của studio chưa từng chạy lại được bằng model thật, không phải "thiếu key".
- Đo hai chiều bằng chính model thật: trước sửa `seo-optimizer: 1/2 pass`; sau sửa `2/2 pass`
  (13717 tok + 31225 tok). Test cũ `test_claude_code_delegates_web_tools_to_cli` **khẳng định đúng hành vi lỗi**
  (`--max-turns` phải `== "1"`) — sửa thành `== CLI_NO_TOOL_TURNS > 1`; đặt lại `= 1` thì đỏ `assert 1 > 1`.
- Rà cả họ lỗi (luật 5): grep `--max-turns` toàn repo — chỉ hai adapter `claude-code` (company, studio) có; core,
  console, keeper không có. Company đã đúng. **Bài học vào `Studio-creators/TRAPS.md`**: vá lỗi provider ở một
  công ty thì grep công ty kia ngay, hai adapter vẫn là hai bản.
- **Không commit bản ghi eval mới**: prompt không đổi nên bản ghi cũ vẫn hợp lệ; `evals all --replay --strict`
  14/14 agent PASS, 0 FAIL. Ghi đè bản ghi chỉ để "chạy lại cho vui" là đưa dao động điểm chấm vào diff.
- `Studio-creators/llm.yaml` nằm ngoài git (`.gitignore`) — đúng luật cấm 3, không có gì bị commit nhầm.

## /thi-hanh keeper — BT4: triage, bậc rủi ro, sổ nợ, ngân sách (#215)

- **Quyết định C3 của gói**: §6 bảo "dùng lại cơ chế `debt_due` của company, đọc `software-company/src/company/orch/`".
  Sai hai lần, đo được: cơ chế nằm ở **lõi** (`xagents_core/supervisor.py:82`, company chỉ tiêu thụ ở
  `orch/gates_flow.py:88`), và nó đếm **chuỗi review liên tiếp** (`streak`/`fired`, `supervisor.py:102-123`) chứ
  **không đọc đồng hồ**. `DebtEntry(due_at)` là hạn theo lịch ⇒ cơ chế MỚI. Đặt tên khác hẳn (`due_at`/`is_overdue`/
  `Ledger.overdue`) để không ai tưởng hai thứ là một; không định danh nào trong `keeper/src/` tên `debt_due`.
- Thế hệ khoá chống-trùng = **số vòng đời ticket đã ĐÓNG của chủ thể**, orchestrator đếm và truyền vào. Không dùng
  `GateRequest.seq` (keeper chưa có gate lúc triage) hay `Task.retry` (chưa có `Task`), nhưng giữ nguyên tắc của cả
  hai: thế hệ tăng khi vòng đời trước KẾT THÚC. Thế hệ theo vòng quét thì mỗi vòng watch ra một ticket mới —
  chống-trùng thành vô nghĩa; không thế hệ thì nuốt signal thứ hai hợp lệ (đúng bug `once=` đã mắc).
- **Một lỗ hợp đồng không cổng nào bắt được, phát hiện khi soi diff**: `Signal.severity` thêm vào model mà
  `topics/schemas/maintenance-signals.json` không có. Bus vẫn nhận vì `payload.additionalProperties` là `true` (cố ý,
  để nguồn ngoài thêm trường không làm vỡ bus), và test khớp topic ↔ file chỉ so TÊN FILE. Đã thêm trường vào schema
  **và** thêm cổng máy: trường có trong model mà thiếu trong schema là CI đỏ. Đo hai chiều: xoá `severity` khỏi
  schema → đỏ; trả lại → 184 passed.
- `sc-supervisor` chấm gói: không phát hiện chặn nào.
## ACL vai được tạo gate — ADR-0008 (#216)

- Đóng nốt phần ADR-0002 hoãn lại. `gate.decide` chỉ NGƯỜI được ghi (một dòng ở `company/bus.py`), nhưng
  `gate.request` là việc **hợp lệ của agent** nên không cấm được agent — chỉ liệt kê được vai. Rà từ chính call
  site: company 4 vai (`product`, `supervisor`, `ops`, `delivery-lead`), studio 4 vai (`channel-strategist`,
  `desk`, `community-manager`, `supervisor`).
- **Đo trước khi siết, và phép đo đã cứu 33 gate**: quét `software-company/company.sqlite` thật (18293 event)
  cho ra actor của `gate.request` là `supervisor` 104, `delivery-lead` 40, `release-engineer` 19,
  `account-manager` 13, `human:lead-repair` 4, `spec-writer` 1. Ba tên giữa là vai **trước ADR-0037**. Nếu chỉ
  allowlist tên hôm nay thì mỗi lần mở bus, console và `gate_cli list` mất trắng 33 gate lịch sử — im lặng,
  không lỗi nào báo. Thêm `roles.LEGACY_GATE_ACTORS`. Bài học lặp lại: **allowlist phải đo trên dữ liệu đã có,
  không suy từ code đang chạy** — code đang chạy chỉ biết hiện tại, bus thì nhớ cả quá khứ.
- **Một fixture bịa bị lộ**: `console/tests/conftest.py` dựng gate `publish` với `created_by="publisher"`, mà
  `publisher` chưa từng mở gate nào — grep lịch sử: tên ấy chỉ xuất hiện đúng ở commit tạo fixture (#91). Sửa
  fixture về `desk` (đúng `studio/orchestrator.py`) thay vì nới allowlist để chiều nó.
- Hai chiều khác nhau có chủ ý: `request()` ném `PermissionError` (quên thêm vai mới thì nổ ngay ở call site),
  `apply()` im lặng bỏ qua (một dòng log xấu không được làm sập replay cả sổ gate). Cả hai CLI bắt lỗi trả mã 3.
- **Còn mở**: phát hiện TRUNG BÌNH của studio (actor lạ đóng gate rồi mất khi replay) và hai phát hiện THẤP về
  ACL `orchestrator` — đều pre-existing, chưa có mã việc.

## /thi-hanh keeper — BT5: patcher trên worktree riêng (#217)

- Chốt checkout chung **hỏi git, không đoán tên thư mục**: `rev-parse --absolute-git-dir` khác `--git-common-dir`
  thì là worktree phụ; bằng nhau hoặc không phải repo → từ chối (fail-closed).
- Chốt `make subagents` đứng **trước** khi chạy, không sau: chạy rồi mới từ chối thì file đã ghi ra `.claude/agents/`
  và `patcher` không được xoá (I5) nên rác nằm lại. Ca đo là `calls == []` — bằng chứng lệnh KHÔNG hề chạy.
- **`sc-security` bắt 4 phát hiện chặn**, tất cả là loại bộ test tự nó không bao giờ bắt được:
  1. Chốt chỉ bảo vệ hai hàm DỌN, không bảo vệ đường GHI; `cli.py --root` mặc định `"."` nên gõ `keeper run` trong
     checkout chung là ghi thẳng `CHANGELOG.md` của nó. Nay `--root` bắt buộc và mọi đường ghi qua chốt.
  2. `_changed_files` không kiểm `returncode` → `git status` lỗi thì trả `[]` → vòng `check_path` chạy 0 lần →
     `regen_derived` "thành công" với `files=[]`. Khuôn "số xanh vì rỗng". **Và một ca test đang pass NHỜ lỗi này**
     (nó truyền `tmp_path` không phải repo git) — đã viết lại thành ca đo thiệt hại thật trên đĩa.
  3. Chốt `fail_under` so danh sách DÒNG nên đổi `[tool.coverage.report]` thành `[tool.coverage.paths]` mà giữ
     nguyên dòng là lọt. Nay parse `tomllib` và so GIÁ TRỊ; `--cov-fail-under` được chốt ở mọi file khác.
  4. `.github/workflows/` không nằm trong bảng cấm — `keeper` ghi được `ci.yml`, tức gỡ chính cổng ép `fail_under`
     và gitleaks. Bảng cũ đúng chữ đặc tả nên đây là **gap của đặc tả**, đã sửa cả bảng lẫn §7.
- Bài học chung của gói: một chốt bảo vệ *một số hàm* không phải là một bất biến. Bất biến là thứ mọi đường vào
  đều phải đi qua — và "đường vào" gồm cả cờ dòng lệnh có giá trị mặc định tiện tay.

## /thi-hanh keeper — BT6: bằng chứng hai chiều, họ lỗi, security-auditor (#218)

- **Lỗ thủng thẳng bất biến I2, `sc-security` tìm ra.** `SELF_CLAIM_FIELDS` chỉ liệt kê `verified_by`; `before`
  và `after` cũng là trường CODE tự đo nhưng không nằm trong bộ lọc, mà thứ tự hợp nhất lại là
  `{**measured, **payload}` — payload thắng. Nghĩa là một model chỉ cần khai `before.exit_code=1,
  after.exit_code=0` là dựng được một `VerificationReport` hợp lệ **mà không lệnh CI nào chạy**, và
  `require_two_way` đi chấm chính lời khai đó. Đúng thứ cả gói được viết ra để chống.
  Sửa bằng hai lớp cố ý thừa nhau: (1) bộ lọc bằng ĐÚNG tập khoá của `_measured()`, khoá bằng test nên thêm một
  trường đo mới mà quên lọc là CI đỏ; (2) hợp nhất số đo SAU, nên kể cả khi lớp (1) bị nới, lời khai vẫn không
  với tới. Ca chiều ngược dựng lại đúng đường cũ để chứng minh nó thủng.
- **Bài học**: "chặn lời khai" không phải một danh sách trường, mà là một bất biến "mọi trường code tự đo đều
  phải do code thắng". Danh sách viết tay đứng cạnh chỗ đo sẽ lệch ngay lần thêm trường đầu tiên.
- `collect_two_way` từ chối checkout chung: `git stash push --include-untracked` cất TOÀN BỘ worktree chứ không
  chỉ diff của patch, nên chạy nhầm chỗ là nuốt việc chưa commit của phiên khác. Chốt dùng lại
  `refuse_shared_checkout` của BT5 (nối được sau khi #217 merge). Ca đo cả thiệt hại: file untracked của "phiên
  khác" phải CÒN NGUYÊN và runner không hề được gọi.
- **Một test đang đi lọt nhờ trạng thái git thật**: các ca `collect_two_way` truyền `Path(".")`, và chúng xanh
  chỉ vì phiên này tình cờ ngồi trong một worktree phụ — chạy trong clone thường thì đỏ. Đã thay bằng repo chính
  + worktree phụ dựng thật trong `tmp_path`.
- `family_safe` nay mang theo LÝ DO tới tận `topics/schemas/verification-reports.json`: trước đó `SafeSite` bắt
  buộc `reason` nhưng payload lên bus chỉ là `list[str]`, tức lý do bị vứt đúng lúc dữ liệu rời tiến trình — thứ
  đi lên bus lại đúng hình dạng "lời khai" mà `family.py` sinh ra để chống (`TRAPS.md` §1).

## /thi-hanh p3 — soạn đặc tả ba việc production (#219)

- **Đề bài sai hai phần ba, và biết được điều đó là nhờ đo.** Phiên chính chấm repo theo trục
  "Agents · Swarms · Production" rồi đề xuất ba việc: OTel span, "thêm prompt caching", "thêm cổng eval hồi quy".
  Hai việc sau đã tồn tại trong repo. `grep cache_control` ra `llm.py:550`; `evals/thresholds.yaml` có sẵn 6 agent
  ở `min_pass_ratio: 0.95` và CI chạy `--strict`. Chẩn đoán từ trí nhớ về chính repo mình vừa đọc vẫn sai —
  `TRAPS.md` §2 áp cho cả khâu lập kế hoạch, không chỉ khâu sửa lỗi.
- **Lỗi thiết kế trong chính đề xuất**: "điểm trung bình n lần chạy → CI đỏ" không đặt được ở chỗ định đặt.
  Replay tất định (khoá `hash(system,user)` → `text` đã ghi), chạy lại 100 lần ra đúng một số. Dao động điểm
  sinh ra lúc `--record`. Nên n-lần phải nằm ở `RecordingClient`, và bản ghi phải có trường điểm — hiện `cases.*`
  chỉ có `{text, model, input_tokens, output_tokens}` trên cả 20 file.
- **Ba lỗ hổng thật, không đoán được từ xa**: (1) studio không có `thresholds.yaml`; `_one` khi `--replay` chỉ
  trả `not any(errored)`, tức 14/20 agent có ca chấm sai nội dung mà CI vẫn xanh; (2) `save()` cố ý gộp không dọn
  → `product.json` 32 khoá cho 16 ca, `qa.json` 15/8, `supervisor.json` 8/3; (3) `make eval-replay` của company
  thiếu `--strict`, lệnh local không giống CI.
- **Bẫy đã ghi vào gói việc để subagent không mắc lại**: `ModelClient` là Protocol với wrapper lồng nhau
  (routing → retry → recording → replay) — chèn span ở tầng client sẽ đếm trùng, bắt buộc chèn phía gọi
  (`company/runner.py:248`). Chế độ `cli` (ADR-0023) không qua `ToolBox` nên span tool rỗng là ĐÚNG, không phải
  lỗi. Nhận diện bản ghi lệch đang dựa vào substring tiếng Việt "bản ghi" (`evals.py:282-285`) — đổi thông điệp
  lỗi là gãy im lặng.
- Người sau KHÔNG được quên: hai gói `chờ người` (`p3.2d` đo cache qua gateway Antigravity cần tài khoản thật;
  `p3.3e` ghi lại eval bằng model thật sau khi đổi schema bản ghi). Lệnh cụ thể ở mục F của `docs/thi-hanh/p3.md`.

## /thi-hanh p3 — đợt 1: ADR-0009 quan sát được bằng span (#220)

- **Số ADR đặt trước là bẫy, lần thứ hai.** Đặc tả `p3.md` viết "ADR-0008" ở ba chỗ vì phiên chính liệt kê
  `docs/adr/` trong checkout chung đang ở `main` **cũ** (`ca4f6af`), nơi chưa có ADR-0008 của #216. `origin/main`
  (`a79927b`) đã có `0008-allowlist-vai-duoc-tao-gate.md`. Bài học #168 lặp lại nguyên hình: số ADR chỉ đúng khi
  đếm trên `origin/main` vừa fetch, không phải trên cây làm việc. Sửa thành 0009 trong chính PR này.
- **`sc-supervisor` chặn đúng một lỗi mà người đọc lướt sẽ bỏ qua**: ADR khai phân bố `def complete` là
  "core llm.py ×6" trong khi thực tế là 5 (dòng 474 là **khai báo** `Protocol`, bốn cái 545/618/675/792 mới là
  hiện thực). Tổng 12 vẫn đúng nên bảng không "trông sai". Nhưng chính phân bố ấy là bằng chứng của quyết định 3
  (chèn span phía gọi vì client lồng nhau) — sai bằng chứng của một quyết định thì quyết định đó thành lời khai.
  Đã sửa và nói rõ cái nào là Protocol, cái nào là hiện thực.
- **Subagent tự sửa lại phiên chính một chỗ, đúng**: `ToolBox.call` **đã** đo `ms` sẵn (`tools.py:101`), nên span
  `tool.call` không thêm số đo mới. Nó thêm *liên kết cha* (lời gọi tool này thuộc lượt model nào, bước nào) và
  *đường phát tại chỗ* — `self.calls` là bộ đệm phẳng chỉ đọc được sau khi vòng kết thúc. Gói việc ban đầu của
  phiên chính nói như thể tool chưa có số đo; ADR viết lại cho đúng.
- Ràng buộc chốt trong ADR mà `p3.1` không được vi phạm: **không đổi một byte nào của `system`/`user`/`messages`**.
  Nếu vi phạm thì mọi bản ghi eval lệch và PR span biến thành PR ghi lại eval — đó là việc của `p3.2`, tách PR.
- Bẫy thao tác: `git fetch` chạy đồng thời ở hai worktree báo `cannot lock ref 'refs/remotes/origin/main'`; chạy
  lại là xong, nhưng đừng đọc nó thành "rebase hỏng". Và `python - <<EOF` in tiếng Việt ra stdout vỡ mã hoá nếu
  quên `PYTHONIOENCODING=utf-8` — file vẫn ghi đúng, chỉ dòng `print` chết, dễ tưởng cả script hỏng.

## /thi-hanh keeper — BT7: gate, release-clerk, vòng lặp orchestrator (#221)

- `orchestrator.pr_blockers()` là **nơi duy nhất** ghép bốn cổng, theo thứ tự: `human-only` (chạm
  `agents/`/`skills/` — khoá vĩnh viễn, gate approved cũng không mở), `evidence` (I2), `gate` (tier `high` phải
  approved), `budget` (I3, hỏi `gh` mỗi lần). Một cổng một chỗ; không có đường vòng thứ hai.
- **Ba chỗ đặc tả §9 sai**, đã sửa theo phép đo: `SqliteBus` → `SQLiteBus` (`sqlite_bus.py:57`); `HumanGate`
  không có `approve()`/`reject()`, chỉ `decide(...)`, thế hệ là `GateRequest.seq` (`gates.py:84,87`); và cạm bẫy
  "`approve` là reopen" viết sai — `decide()` ĐÓNG gate, reopen là nghĩa riêng của vài escalation do orchestrator
  tự phát `supervisor-actions{resume}` (`orch/gates_flow.py:110-117`). Câu cấm viết sai thì dạy sai cho agent sau.
- **Lỗi ẩn từ BT1 lộ ra ở đây**: `topics/schemas/verification-reports.json` đặt `$defs` ở gốc file, nhưng bus dựng
  validator payload lấy `properties.payload` làm gốc riêng (`bus.py:74`) → `PointerToNowhere`. Chưa lộ suốt sáu gói
  vì **chưa có mã nào publish topic đó** — đúng loại lỗi chỉ hiện khi có người dùng thật.
- **ADR-0006 đếm sai quân số**: ghi "8 agent" nhưng chính bảng của nó liệt kê 10 (khối `watch` một mình có ba).
  Đã sửa ADR, README và đặc tả, ghi rõ là lỗi đếm chứ không phải đổi phạm vi.
- `assetscan`/`assetbudget` nay quét cả `../keeper` ở **bốn** chỗ. Một phát hiện buồn cười: chuỗi ví dụ
  "ignore previous instructions" viết trong prompt `dependency-scout` để dạy nó cảnh giác lại làm chính máy quét
  injection báo động — đã diễn đạt lại thay vì thêm miễn trừ.

**Chờ người**: bước `make eval-record AGENT=<id>` của `CONTRIBUTING.md` §3 cần model thật. `keeper/evals/` **cố ý
chưa dựng** — thư mục rỗng sẽ trông như bước đã chạy.

### BT7 — bốn phát hiện chặn, đều là "cổng có mặt nhưng không chặn"

1. **Thế hệ khoá chống-trùng không bao giờ tăng.** `_generation` đếm ticket `status == "closed"`, nhưng không
   chỗ nào trong toàn bộ `keeper/src/` đặt trạng thái ấy — `Ticket.status` chỉ có mặc định `"open"`. Thế hệ
   vĩnh viễn 0 ⇒ một tín hiệu hợp lệ lặp lại sau khi đã xong một vòng bị nuốt **vĩnh viễn**. Đúng bug `once=`
   §6 cảnh báo, ở dạng nặng hơn. Sửa lần một (đếm vòng đã có `release-notes`) làm hỏng test resume: khoá phải
   đồng thời *idempotent khi replay* và *không nuốt signal mới*. Bỏ hẳn trục "thế hệ", chống trùng theo
   `Envelope.event_id` — replay cho lại đúng id đó, còn signal mới là envelope mới.
2. **Cổng bằng chứng bị vòng qua ở đường TIÊU THỤ.** `require_two_way` đứng ở hàm DỰNG báo cáo, nhưng `_apply`
   nạp mọi event `verification-reports` vào `verified` mà không kiểm lại — một event từ tiến trình khác hoặc từ
   replay với `before.exit_code=0` vẫn mở cổng. Bài học: chốt đặt ở chỗ SINH dữ liệu không bảo vệ được chỗ NHẬN
   dữ liệu, khi dữ liệu đi qua một cái bus.
3. **I3 thủng trong cùng một nhịp**, không cần đa luồng: `open_pr()` không tạo PR thật nên `gh.open_prs()` vẫn
   trả 0 ở ticket kế, và bộ đệm 60s giữ nguyên câu trả lời. Nay `budget` cộng cả "ý định mở PR chưa thấy trên
   GitHub", dựng lại được từ bus.
4. **Không có đường cho người duyệt gate.** Ticket `risk_tier=high` bị chặn ở cổng `gate` và không gì mở được —
   công ty tự khoá chính mình. Thêm `keeper gate` CLI; quyết định chỉ nhận khi `env.actor` là người.
## /thi-hanh p3 — đợt 2: span vào runtime (#222)

- **Ràng buộc của phiên chính mâu thuẫn với code thật, subagent nói ra thay vì lách.** Gói việc đòi cây span lồng
  nhau theo thời gian: `tool.call` là con của `llm.complete`. Nhưng trong `_turns`, model trả `tool_calls` rồi
  runner mới chạy tool — span `llm.complete` **đã đóng** trước khi `tool.call` mở. Hai lối ra: giữ span LLM mở tới
  hết vòng tool (phá đúng cái latency span sinh ra để đo), hoặc gắn cha tường minh. Chọn cái sau. Hệ quả phải biết
  khi đọc trace: `tool.call` có timestamp **sau** `end_ns` của cha nó. Bài học: ràng buộc viết trong gói việc cũng
  là giả thuyết, cũng phải chịu đo.
- **Hai trợ lý chấm độc lập hội tụ vào một chỗ, mỗi bên thấy một nửa.** `sc-qa` (góc test) thấy đường MCP không có
  test cây span và ADR không liệt nó vào giới hạn đã biết. `sc-builder` (góc mã) tìm ra **cơ chế**: `ToolBridge`
  dùng `ThreadingTCPServer` và gọi `toolbox.call` trên thread handler (`mcp_bridge.py:93-96,120-123`), thread mới
  bắt đầu với `contextvars` Context RỖNG → `parent=None`. Một mình mỗi báo cáo là một nghi ngờ; hai báo cáo là một
  sự thật. Đây là lý do bảng D giao **hai** `sc-*` cho gói C3 chứ không một.
- **Characterization test không đỏ được trước khi sửa — phải chứng minh sức nặng theo chiều ngược.** Test khẳng
  định `parent is None` qua cầu MCP mô tả hành vi *hiện tại*, nên nó xanh cả trước lẫn sau. Subagent chứng minh nó
  không phải trang trí bằng cách monkeypatch cho bridge chạy dưới `copy_context()` → test **đỏ** với
  `parent=Span(name='llm.complete')`. Khuôn dùng lại được: test đặc tả hành vi thì đo bằng "sửa cho đúng thì nó
  đỏ", không phải "tắt bản sửa thì nó đỏ".
- `sc-builder` bắt `_TURN_SPAN.set(sp)` chạy vô điều kiện: khi sink tắt, `sp is None` nhưng `ContextVar.set` vẫn
  cấp phát một `Token` — lệch quyết định 5 của ADR ("no-op không tốn một chút nào"). Nhỏ, nhưng đúng là chỗ một
  quyết định ADR bị bào mòn lặng lẽ nếu không ai đọc.
- Ghi lại một mutation cố ý thay vì sửa nó: `tools.sink = self.sink` gán vào `ToolBox` của người gọi và không hoàn
  lại. Hôm nay vô hại vì cả 5 chỗ dựng `ToolBox` đều dựng mới mỗi bước — nhưng không có gì trong mã giữ điều đó
  đúng về sau, nên bẫy được viết cạnh dòng gán.
- Nghiệm thu rẻ mà gói nào chạm prompt cũng phải chạy: `company.evals all --replay --strict` và
  `studio.evals all --replay` — 0 FAIL cả hai, tức không byte nào của prompt đổi và không phải ghi lại eval.

## /thi-hanh p3 — đợt 2b: cổng điểm eval cho studio (#224)

- **Một test đang đóng đinh chính lỗ hổng mà gói này bịt.** `test_replay_exit_code_ignores_grading_but_not_stale_recordings`
  (studio) dựng bản ghi cho `publisher` chấm 1/2 rồi khẳng định exit 0. Nó không sai lúc viết — nó đo đúng cổng
  **bản ghi** (thiếu/lệch prompt), và câu "ca chấm không đạt không làm CI đỏ" là chính sách có thật của
  `CONTRIBUTING.md` §3. Nhưng chính sách ấy chỉ đúng khi **có** một cổng điểm riêng ở chỗ khác — company có, studio
  không. Cách xử lý: thêm cờ `--no-thresholds`, giữ nguyên hai khẳng định cũ (chúng vẫn đo đúng thứ chúng sinh ra để
  đo), rồi thêm một dòng cho chiều mới. Bài học: một test cản đường không phải lúc nào cũng là test sai — đọc xem nó
  đo gì trước khi nới nó.
- **Thêm `--strict` vào `eval-replay` đẻ ra một target trùng.** Sau khi sửa, `eval-replay` và `eval-thresholds` có
  lệnh **giống hệt**, trong khi chú thích cái sau vẫn nói "như eval-replay --strict". Không xoá được vì
  `CONTRIBUTING.md` §3 dẫn tên đó. Sửa chú thích thành "bí danh" kèm lý do giữ. Phiên chính bắt được lúc đọc diff,
  subagent không nêu — đọc diff không thay thế được bằng đọc báo cáo.
- **Ngưỡng phải đo, không copy.** Không lấy 0.95 của company vì "công ty kia cũng thế": chạy
  `studio.evals all --replay` lấy điểm thật (14/14 agent 2/2 = 1.00) rồi làm tròn xuống 0.05. Với 2 ca/agent thì
  0.95 nghĩa là một ca hỏng = 0.50 → đỏ. `cases: 2` chặn đường né bằng cách xoá bớt ca xấu cho ratio đẹp lên.
- **Phiên chính tự đo hai chiều thay vì tin ca test**: sửa tạm `cases: 2` → `3` cho một agent, chạy
  `studio.evals all --replay --strict` → `FAIL analytics-analyst: bộ ca bị thu nhỏ`, exit **1**; khôi phục → exit 0.
- Bẫy thao tác: `grep FAIL | head` rồi đọc `$?` là đọc exit code của **grep**, không phải của lệnh eval. Phải chạy
  lại lệnh riêng, vứt stdout, mới lấy được exit thật (0 giả vs 1 thật).
- Cổng README của company đếm số **file** test từ đĩa, không chỉ số ca: thêm một file test là đỏ, kể cả khi số ca
  vẫn nằm trong khoảng cho phép. Số cuối sau khi gộp `p3.1` + `p3.3`: `1085 ca / 69 file`.

## Audit sâu toàn repo (#226, phiên remote, nhánh `claude/audit-sau-lsr1iu`)

Đề bài chỉ có hai chữ "audit sâu". Chạy thật `make lint` / `make test` / `make cov` và **sinh lại file dẫn xuất
rồi `git status`** thay vì đọc code đoán. Nền tảng lành mạnh thật: lint+mypy sạch cả sáu, **16.285 dòng phủ 100%,
0 miss**, `make golden` + `make subagents` cho diff **rỗng**, không bí mật nào bị commit, `shell=True`/`eval`/
`pickle` **0 chỗ**, và 100% ấy không mua bằng thủ thuật (chỉ 2 file `omit`, cả 23 `pragma: no cover` đều là
`__main__`/stub Protocol/`ImportError`).

- **Phát hiện chính: repo có công cụ tự soi mà không ai bật.** `keeper.drift` phủ 100% test, đặc tả ở
  `DAC-TA-KEEPER.md` §drift hứa đúng ba phép — nhưng `grep drift .github/workflows/` = **0**. Hệ quả đo được:
  **#192, #208, #209 merge thiếu dòng CHANGELOG**, #208 còn nguyên chữ `(#PENDING)`. Bài học chung: một cổng
  chỉ có giá trị bằng thứ chạy nó; viết bộ dò rồi phủ 100% test cho bộ dò **không** phải là đã canh.
- **Cách tìm ra đáng giữ**: grep tay ra ba PR trước, rồi mới chạy `keeper.drift` — hai đường độc lập cho **cùng
  ba số**. Nếu chỉ chạy công cụ thì không biết công cụ có đúng không; nếu chỉ grep thì không biết công cụ có tồn tại.
- **`block` KHÔNG phải tên thư mục.** `subagents.py` ghép `agents/{spec.block}/{id}.md`; `supervisor.md` khai
  `block: supervision` mà nằm ở `agents/supervisor/`. Năm agent kia đúng chỉ vì tên trùng **tình cờ**. Bản dẫn
  xuất vì thế bảo người ta "sửa nguồn" ở một đường dẫn trống, và phép (a) của drift mù hẳn với supervisor. Sửa
  gốc: `AgentSpec.source_rel` do `load_agents` đặt bằng `p.relative_to` — đường dẫn THẬT, không ghép lại từ nhãn.
- **Fail-open làm lỗi đội lốt lỗi khác.** `_source_version` thiếu file → trả `1`, nên bug đường dẫn hiện ra là
  "nguồn hiện version=1"; người đọc sẽ đi tìm một lần tăng version không hề tồn tại. Trả `None` + thông điệp
  riêng. Chi phí của fail-open không phải "bỏ sót", mà là **dẫn người sau đi sai hướng**.
- **Một test đang đóng đinh chính cái fail-open ấy**: `test_sc_agent_drift_nguon_khong_ton_tai` đòi **im lặng**
  khi nguồn không tồn tại. Lần thứ hai trong ngày gặp mẫu này (xem mục `p3.3` phía trên) — đọc xem test đo gì
  trước khi nới, nhưng ở đây nó đo một hành vi **sai**, nên đổi chiều chứ không nới.
- **`fetch-depth: 0` là điều kiện đúng/sai, không phải tinh chỉnh.** `actions/checkout` mặc định depth 1 → `git
  log` chỉ có một commit → phép (c) không soi PR nào → cổng **xanh giả**, tệ hơn không có cổng vì nó tạo cảm
  giác đã được canh.
- **Lại dính đúng bẫy đã ghi trong chính file này**: đọc `$?` sau `lệnh | tail -3` là đọc exit code của `tail`.
  Suýt báo "exit=0" cho một ca đang phải đỏ. Đã chạy lại riêng, vứt stdout, mới lấy được mã thật.
- Số liệu README đã trôi: company 1044→**1087**, studio 514→**580**, core 236→**450**; và README còn nói core
  "còn K3.5c rồi K3.6–K3.7" trong khi K3.1–K3.7 **xong hết** từ #187/#195/#198. Suýt ghi K3.5c là #200 theo trí
  nhớ — `git log` nói **#187**. Số nào cũng phải hỏi `git log`, kể cả số mình vừa nghĩ ra.
- **Chưa làm, để lại cho người sau**: phủ 100% hiện là **dòng**, không phải **nhánh** — không package nào đặt
  `branch = true`. Với repo mà sản phẩm chính là logic quyết định (guard, gate, fail-closed), `fail_under = 100`
  đang bảo vệ ít hơn con số gợi ý. Đây là quyết định chính sách (bật lên là một loạt cổng đỏ cùng lúc), nên nêu
  chứ không tự bật.
- Ghi để khỏi ai đi lại: gateway **không có xác thực client** và `guard_middleware` **tự tắt** khi bind ngoài
  loopback (`GATEWAY_HOST` là env var, `0.0.0.0` là mẫu Docker thường gặp). Đã soi kỹ: đây là **quyết định có
  ghi chép**, nêu thẳng trong docstring và cảnh báo bằng `warn_if_public_host` — không phải lỗ hổng bị giấu.
- **Cổng mới bắt được thứ chính người viết ra nó không thấy — ngay lần chạy đầu.** `drift-check` đỏ trên CI với
  **PR #161** (ADR-0038, 886 dòng thêm), trong khi `keeper drift` chạy tay ở phiên này báo *sạch*. Lý do: clone
  của phiên remote là **shallow** — 52 commit, cũ nhất 2026-09-08, mà #161 merge 2026-09-07. Tôi mù đúng cái kiểu
  mù mà tôi viết `fetch-depth: 0` để chống, và không hề biết cho tới khi CI nói. Hai bài học: (a) `git rev-list
  --count HEAD` + `test -f .git/shallow` phải là bước **đầu** của bất kỳ phép đo nào đọc `git log`, không phải
  bước điều tra sau khi lệch; (b) "chạy tay thấy sạch" và "cổng thấy sạch" là **hai phát biểu khác nhau** khi môi
  trường khác nhau — đây chính là lý do cổng phải sống trong CI chứ không sống trong tay người.
- #161 hoá ra **đã có dòng CHANGELOG** (dòng 607, tả đúng ADR-0038) nhưng **chưa bao giờ được điền số PR** — cùng
  khuôn với #192. Ba trong bốn ca là *quên điền số*, không phải *quên viết dòng*: chỗ hỏng nằm ở bước "điền `(#n)`
  sau khi có số PR", không phải ở ý thức ghi CHANGELOG. Ai muốn giảm tái phát thì sửa ở đó.
- `git fetch --unshallow` để phiên sau đo đúng như CI.

## Xử lý nốt phát hiện audit (#227, phiên remote, tiếp)

- **Mẫu thật của lỗi chỉ lộ ra khi có đủ mẫu.** Ba ca đầu (#192/#208/#209) trông như "quên ghi CHANGELOG". Ca thứ
  tư (#161) làm rõ: **ba trong bốn là quên ĐIỀN SỐ**, dòng mô tả đã viết đủ. Chỗ hỏng là bước "điền `(#n)` sau khi
  có số PR", không phải ý thức ghi CHANGELOG — nên phép (d) dò *placeholder* chứ không nhắc người ta ghi chép.
- **Phép (d) không phụ thuộc độ sâu clone**, khác phép (c). Đó không phải tình cờ mà là điểm mạnh chọn có chủ ý:
  đúng chỗ clone shallow đã làm phép (c) mù ở #226.
- **Bộ dò báo động vì tài liệu MÔ TẢ nó là bộ dò sẽ bị tắt.** Bản đầu của phép (d) đỏ ngay trên chính CHANGELOG,
  vì file này *kể lại* các ca placeholder bằng văn xuôi (`đổi (#208) về (#PENDING)`). Lọc code span trước khi dò.
  Bài học: mọi bộ dò chạy trên tài liệu phải phân biệt "dùng" với "nhắc tới".
- **`fail_under = 100` đang đo DÒNG.** Đo được **198/4966 nhánh chưa phủ** trong khi cả sáu package xanh 100%.
  Đã bật `branch = true` cho `xagents-core` (9) và `keeper` (3) — chọn hai cái này vì khoảng cách nhỏ nhất, và vì
  core là lõi cả sáu dùng chung. Vá bằng **test thật**, không nới ngưỡng; đúng một `pragma: no cover` cho stub
  `Protocol` (thân `...` không có đường chạy tới — miễn trừ cho thứ *không thực thi được*, không phải cho thứ
  *chưa được test*).
- **Đọc số dòng của báo cáo coverage phải đối chiếu file, đừng suy.** Tôi đoán `50->52` là "dict rỗng" và viết một
  test đúng-nhưng-vô-dụng; đọc `awk` ra số dòng thật thì đó là "giá trị không phải str/dict/list". Tương tự
  `203->206` không thuộc `SubprocessSandbox` mà thuộc `ContainerSandbox`. Hai lần suy sai liên tiếp từ cùng một
  thói quen.
- **Một test có thể xanh mà không chạm nhánh cần.** Ca `_strings` đầu đặt `limit=10_000` nên `trim_payload` thoát
  ở vòng đầu và `_strings` **không hề chạy** — test xanh, nhánh vẫn hở. Luôn đo lại coverage sau mỗi test mới,
  đừng tin "test pass ⇒ đã phủ".
- **Còn lại, đã đo, chưa làm**: console 12 nhánh, gateway 27, Studio-creators 60, software-company 87 (tổng 186).

## Kết `/thi-hanh keeper` — tám PR merge, hai việc chờ người

BT1 #210 · BT2 #211 · BT3 #213 · BT4 #215 · BT5 #217 · BT6 #218 · BT7 #221 · BT8 #223.

Nghiệm thu chạy trên chính `origin/main@64a9414` trong lượt này: xagents-core 438 · keeper 462 · gateway 248 ·
console 256 · Studio-creators 576 · software-company 1083 = **3 063 test pass**, cả sáu package `fail_under = 100`.

**Người sau KHÔNG được quên — hai việc `chờ người`:**

1. `make eval-record AGENT=<id>` (`CONTRIBUTING.md` §3 bước 3) cần **model thật**. `keeper/evals/` cố ý chưa dựng:
   thư mục rỗng sẽ trông như bước đã chạy.
2. **Canary của BT8 chưa chạy.** Đặc tả §10 nói thẳng *"không có PR thật thì BT8 chưa xong"*: `keeper` phải tự mở
   đúng một PR bảo trì có bằng chứng đo hai chiều và PR đó **merge**. Ba điều kiện: `gh auth login` sẵn trên máy;
   bước merge là của NGƯỜI (I1 cấm `keeper` tự merge); và `GitHubReader` hiện thuần chỉ-đọc — khảo sát cho thấy
   `gh pr create` KHÔNG bị bảng cấm chặn, nhưng thêm một adapter GHI là nới ranh giới bảo mật của cả công ty,
   nên để người quyết.

**Ba khuôn lỗi lặp lại suốt tám gói** (đáng đưa vào `TRAPS.md`):

- *Cổng có mặt nhưng không chặn.* Bốn phát hiện chặn của BT7 đều thuộc loại này, và cả bốn đều xanh trong bộ test
  của chính gói. Câu hỏi kiểm phải là "đường vào NÀO không đi qua cổng này", không phải "cổng có chạy không".
- *Ca chiều ngược giả.* Sáu gói có ít nhất một ca tự nhận là đo hai chiều mà xanh vĩnh viễn: tính sai đường dẫn,
  assert hằng đúng, chỉ đếm fixture đầu vào, assert biến đếm giả, và một ca xanh chỉ vì trạng thái git thật của
  repo đang chạy. Viết một dòng `assert` cạnh chữ "chiều ngược" là cách rẻ nhất để một gói TRÔNG như đã đo.
- *Danh sách viết tay đứng cạnh cơ chế.* `SELF_CLAIM_FIELDS` thiếu `before`/`after`; `_VALUE_FLAGS` thiếu cờ của
  `gh pr create`; `FORBIDDEN_ARGS` thiếu `-f/-F/--field`. Cả ba đều là "bảng liệt kê thứ nguy hiểm" thay vì "bảng
  suy ra từ cơ chế gây nguy hiểm" — lệch ngay lần thêm phần tử đầu tiên.

**Việc ngoài phạm vi, ghi để không ai tưởng đã sửa**: `CHANGELOG.md:674` còn một chỗ ghi `(#PRNUM)` chưa thay số
(dòng cũ của phiên khác); `.github/dependabot.yml` thiếu khối cho `xagents-core`; `CONTRIBUTING.md:59` còn số
coverage lạc hậu 90/84/73. Cả ba đúng là loại tín hiệu mà chính `keeper` sinh ra để bắt.

## keeper: hạ tầng eval + ghi bản ghi bằng gói subscription CLI (#PR)

Gỡ khoá việc `chờ người` số 1 của `/thi-hanh keeper`. Người dùng hỏi "dùng tk sub" — và đúng như lần 4L-1b,
"cần model thật" KHÔNG có nghĩa "cần API key trả theo token": `~/.claude-liendv` là `CLAUDE_CONFIG_DIR` của một
tài khoản gói Pro/Max đang đăng nhập, provider `claude-code` gọi CLI `claude -p`. Đã kiểm trước khi dựng gì.

**Model thật bắt hai lỗi prompt mà 462 unit test không thấy:**

1. `triager` xếp `semver_jump: null` (pre-release) thành `low` — **hai lần liên tiếp**, nên không phải dao động.
   Prompt chưa bao giờ nói ra luật của `risk.py`. Đây đúng là ca suýt cho **tự merge một bản pre-release**, thứ
   đã được vá ở tầng mã từ BT3/BT4 nhưng agent thì không biết. v1 FAIL 2/2 → v2 PASS.
2. `release-clerk` viết một báo cáo bằng chứng **một chiều** thành "đã xác minh", và không có khuôn dòng
   CHANGELOG. v2 → v3, hai ca hỏng nay pass.

**Một ca bị BỎ vì agent đúng còn ca sai**: `regression-guard` chấm 1/5, nhưng 4 lần trượt là nó **từ chối chép**
`before/after` từ trường `summary` do `patcher` kể, trả `exit_code: -1` kèm "đây là lời khai của patcher, không
phải output tôi chạy". Mọi ca đòi nó điền số đo đều là đòi một lời khai — đúng thứ bất biến I2 sinh ra để chặn.
Thay bằng `keeper-supervisor`. Bài học: **một bộ eval có thể yêu cầu agent vi phạm chính luật của hệ**; điểm thấp
là tín hiệu để đọc lại ca, không phải để sửa agent cho vừa ca.

Ngưỡng `min_pass_ratio: 0.95` (đo được 1.00, làm tròn xuống theo `CONTRIBUTING.md` §3). Replay `--strict` chạy
offline: 21/21 pass. CI: 532 test, 100% dòng **và** 100% nhánh.
