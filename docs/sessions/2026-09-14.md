# 2026-09-14

## `COMPANY_DEPLOY=process` cho QLKH (ADR-0040) — PR #289

- Người dùng: "pull và khởi động công ty chạy hoàn thành nốt dự án qlkh". Pull `main` (`5c5aa19`), khởi động lại
  `orchestrator run --watch` trong worktree `qlkh-cong-ty-tu-sua` (đúng CLAUDE.md "bật console thì bật ngay
  watch") — không việc gì tự trôi thêm: 5 gate `escalation` (REL-001/005/006/007/008) vẫn kẹt đúng như
  `docs/sessions/2026-09-13.md` đã ghi, vì `deploy.py` chỉ hỗ trợ `docker compose` còn QLKH staging chạy tiến
  trình trần trên WSL (ADR-0013 của khách). Kiểm trong WSL: tiến trình staging cũ (pid ghi ở `var/devserver.pid`)
  đã chết.
- Đây là quyết định kiến trúc (cách bắc cầu Windows→WSL) + ảnh hưởng gate release production → hỏi người dùng
  trước khi tự vá (đúng AGENTS.md "khi bối rối"), đưa ba lựa chọn. Người dùng chọn xây `COMPANY_DEPLOY=process`
  đầy đủ.
- Vào worktree riêng (`EnterWorktree`, nhánh `worktree-deploy-process-wsl`) theo đúng luật 2. Viết
  `docs/adr/0040-deploy-process-wsl.md` trước (thay đổi kiến trúc → ADR trước, luật bắt buộc 2): mode `process`
  thêm bên cạnh `compose`, giao thức script `up`/`down`, `deployed` là hai phần (không có `ps`), cầu nối
  `wsl.exe --cd . bash` — đã đo thật lệnh này dịch đúng đường dẫn Windows sang `/mnt/<ổ>/...`.
- TDD: `tests/test_deploy_process.py` viết trước (đỏ — `ImportError` vì `DEFAULT_RUNTIME_PROCESS` chưa tồn
  tại), rồi cài `deploy_script`/`_process_prefix`/`_process_argv`/`_process_cmd` + nhánh `mode == "process"`
  trong `deploy()`. 14 ca ban đầu + 2 ca bù coverage (`FileNotFoundError`/`TimeoutExpired` của `_process_cmd`) =
  16 ca, tất cả xanh. CI đầy đủ trong lượt này: `ruff check` xanh, `mypy --ignore-missing-imports` xanh (52
  file), `pytest -q -n auto --cov` → **1247 passed, 1 skipped, 100% dòng lẫn nhánh**. Cập nhật README (số ca/
  file test, ADR mới nhất 0040) và mô tả trường `runtime.deploy` trong `topics/schemas/approved-specs.json`
  (cùng trường, đổi nghĩa theo mode).
- **Còn treo, chưa làm trong lượt này**: (1) viết script thật `up`/`down` phía repo khách
  (`D:\khach\qlkh\infra\staging\wsl\deploy_process.sh`) — KHÔNG dùng `deploy.sh` sẵn có nguyên trạng vì nó tự
  `git clone`/checkout một bản riêng, xung đột với worktree tích hợp company đã checkout đúng sha (xem ADR-0040
  §"Bối cảnh"/"Từ chối"); (2) khai `runtime.deploy` trong spec QLKH trỏ tới script mới; (3) bật
  `COMPANY_DEPLOY=process` cho orchestrator đang chạy và cho nó tự redeploy 5 gate kẹt bằng bằng chứng máy
  sinh thật — chỉ khi đó 5 gate mới đóng đúng luật ADR-0039 (không phải lời khai).
- Người sau: đọc ADR-0040 trước khi viết script phía khách — giao thức `up`/`down` đã cố định, không tự đổi
  tên lệnh con.

## Phiên chiều — đối chiếu `projects-template` (PR #295)

- Người dùng: "thêm những điểm tốt từ repo này bổ sung cho dự án Claude-Agents", rồi "thêm tất cả những cái
  tốt". Không bê cả bộ khung sang: `docs/PROMPT-SHEET.md` §H là luật cho đúng việc này (ba cột, và "chưa có"
  phải ứng với một sự cố ĐÃ XẢY RA ở đây, không thì xếp "chưa cần"). Đã nói rõ với người dùng vì sao lọc, và
  người dùng duyệt mức lọc trước khi tạo nhánh.
- Kết quả: `docs/reports/2026-09-14-doi-chieu-projects-template.md`. 13/15 hạng mục repo này sâu hơn. Bản
  nháp đầu đề nghị lấy 2 thứ; đọc kỹ `docs/TASK-PACK.md` thì **ứng viên số 1 (vòng hội tụ audit) đã có đủ** —
  sổ "Việc để lại đang treo" + ô kiểm mục 2 của gói việc audit đã yêu cầu đúng điều đó. Tự loại, còn 1.
- Thay đổi luật duy nhất: khối BÁO CÁO XÁC THỰC trong `AGENTS.md` luật cấm 8 (bước 4 thành vật thể điền được).
- Xong: PR #295 — tài liệu thuần, không chạm `src/`, `tests/`, `agents/`, `skills/`, `.claude/agents/`.
- Dở: không.
- Đang mở: 3 dòng "Việc để lại đang treo" của 09-12/09-13 **không đổi** — phiên này là phiên đọc repo ngoài,
  không phải phiên audit, nên không đóng dòng nào.
- Bẫy mới: không (bài học "đọc kỹ file đích trước khi kết luận nó thiếu" đã nằm trong §H, không cần mục TRAPS).
- Người sau KHÔNG được quên: từ giờ câu "xong/sẵn sàng merge" phải kèm khối BÁO CÁO XÁC THỰC điền thật.

## Port lớp hàng rào từ `seeker19110/project-template` (#294)

- Người dùng hỏi so sánh repo này với bộ khung `seeker19110/project-template`, rồi "cái nào vận hành tốt hơn",
  rồi "thêm tất cả những cái tốt từ template qua đi". Đã clone template về scratchpad và đọc thật (167 file,
  101 `.md`, 31 `.sh`) thay vì đọc README rồi suy.
- Kết luận so sánh: hai bên mạnh ở hai tầng khác nhau. Repo này chặt hơn ở **tầng bằng chứng** (TDD đỏ-trước
  bắt buộc cho mọi code, `fail_under = 100` × 5 package, ~19 job CI, eval/golden/drift). Template chặt hơn ở
  **tầng thói quen**: luật gần như trùng chữ, nhưng nó **thi hành bằng máy** (hook chặn tại chỗ gõ lệnh,
  `dev-task.sh` một điểm vào, 13 slash command) còn repo này thi hành bằng chữ.
- Đã port: `scripts/dev-task.sh`; `.claude/hooks/{block-dangerous-git,pre-commit-gate,auto-format}.sh` +
  `.claude/settings.json`; `/gate`, `/debug`, `/adr`; `GEMINI.md` + `.cursorrules`/`.windsurfrules`/
  `.clinerules`. TDD: `platform/console/tests/test_cong_khung.py` (73 ca) viết **trước**, đỏ 50/51 lúc chưa có
  gì, xanh 73/73 sau.
- **Cố ý KHÔNG port** (ghi ra để phiên sau khỏi port lại rồi mới phát hiện trùng): `maintenance-sweep.sh` +
  `maintain-cron.sh`/`maintain-run.sh` — `companies/keeper` đã làm đúng việc bảo trì đó ở tầng sản phẩm, thêm
  script song song là dựng nguồn sự thật thứ hai; `spec-compiler.py`, `arch-health-radar.py`, `telemetry-log.py`
  (đo chi phí theo token API, trong khi repo này chạy gói subscription qua gateway); `release.yml`
  (release-please — tag `v*` ở đây là của sản phẩm khách, ADR-0027); 12 template tài liệu (repo đã có
  `docs/TASK-PACK.md` + `docs/KHUON-THI-HANH.md`); `session-guide.sh`/`session-resume.sh`/`usage-guard.sh` của
  template (bám `PROGRESS.md` + model `opusplan`, repo này dùng `docs/sessions/` và cấu hình model khác).
- **Hai lỗi của bản template, phát hiện khi chạy thật, đã sửa trong port**: (1) máy phát triển chính **không có
  `jq`** → cả ba hook bản gốc fail-open **im lặng** suốt phiên, tức hàng rào không tồn tại mà người vẫn tin nó
  canh; đã thêm đường đọc JSON bằng Python. (2) `.sh` commit từ Windows mang CRLF → bash Linux báo
  `\r: command not found`, và hook hỏng kiểu này không đỏ ở đâu cả; đã chốt `*.sh text eol=lf` + test
  `git check-attr` canh.
- Quyết định thiết kế đáng nhớ: cổng trước commit chạy **hẹp theo gói bị đụng**, không chạy cả năm package.
  Cổng toàn workspace mất nhiều phút, và hàng rào nào đắt quá thì agent sẽ tìm đường né — lúc đó nó thành vô
  dụng, tệ hơn không có. File ở gốc (`pyproject.toml`, `Makefile`, `.github/`) vẫn kéo về `all`.
- Người sau: hook chặn oan thì **sửa hook kèm test**, đừng tắt nó; `dev-task.sh` phải luôn khớp `ci.yml` —
  `test_dev_task_test_luon_do_coverage` canh đúng chỗ đó (nó đã bắt được một lần tôi ghi sai lệnh test).
## Vận hành QLKH tiếp: mở khoá phễu release bằng bốn lớp chặn đo được — PR #296

- Điểm xuất phát: engine đã dừng (lock chết), 0 gate chờ, nhưng phễu release đứng vì một câu hỏi clarification
  của `product` (seq 1056, hỏi bật `legacy=true` để bỏ `smoke()`) chưa ai trả lời — và đã lỗi thời sau PR #292.
  Trả lời "không chọn option nào": phát lại `approved-specs` với runtime lấy từ `runtime.yaml` của khách +
  `runtime.deploy = infra/staging/wsl/deploy_process.sh`. `product` làm đúng: spec v6, `security` pass, plan
  sinh QLKH-034.
- Bốn lớp chặn, mỗi lớp chỉ lộ ra sau khi gỡ lớp trước, đều đo bằng lệnh thật chứ không suy từ thông điệp:
  1. **CRLF**: repo khách không có `.gitattributes`, `core.autocrlf=true` → `deploy_process.sh` checkout ra CRLF,
     `wsl bash … up` chết ở dòng 42–43. Đưa vào CR → QLKH-034.
  2. **Vòng lặp `tasks`→`qa` khi bật `--test-author`** (`orch/routes.py:300`): ticket phát lại lần hai đã có
     test trong worktree → qa "không viết file test nào" → escalate → approve → phát lại → qa… (4 vòng, cùng
     khuôn TCK-005 trong `diagnose`). Thoát bằng khởi động lại engine **không** `--test-author`. Lỗi cơ chế
     thật, chưa vá — cần guard "đã có `test-suites` cho thế hệ retry này thì bỏ pha author".
  3. **Docker Desktop tắt** → sandbox container không nối daemon → `local_checks` false. Người bật Docker.
  4. **`--env-file -` không chạy trên Windows** → vẫn false dù Docker đã lên. Vá ở PR này (xagents-core).
- Builder không thể tự làm QLKH-034 vì **thiếu công cụ** (`run` chỉ có tập lệnh cố định, không có
  `git add --renormalize`; `search` đọc text nên không thấy CR). Đi đường `takeover`: sửa tay, chạy bộ test của
  qa (5 pass/0 fail/1 skip), PR dưới tên người, `lint=True tests=True` (takeover dùng subprocess, không dựng
  container). Merge vào `company/integration` `10bd1a4`, REL-011 staged.
- REL-011 `deploy_failed` lần đầu vì worktree tích hợp **đã tồn tại** vẫn giữ byte CRLF cũ — `.gitattributes`
  chỉ tác động lên checkout mới. Ép checkout lại từng script (`rm` + `git checkout -- <file>`, không
  `reset --hard`), chạy tay `up` → healthz 200, `curl` từ Windows 200; duyệt gate với `decision: redeploy`.
- Bẫy thao tác trong phiên (không ghi TRAPS vì thuộc harness, không thuộc repo): (a) monitor kiểm sống bằng
  `ps -W` trong Git Bash **không thấy** tiến trình Windows → báo "orchestrator chết" oan ba lần, để lại 3 cặp
  tiến trình thừa phải nhờ người `Stop-Process`; kiểm bằng `Get-Process -Id`. (b) `comment`/`approve` đều phát
  lại `tasks` — trước khi gõ phải biết `tasks` sẽ rơi vào vai nào theo cờ đang bật. (c) chạy bộ test shell của
  khách bằng git Windows, không trong WSL (git WSL không theo con trỏ `.git` kiểu đường dẫn Windows).
- Còn treo: REL-011 chờ `deploy()` thật; vòng lặp `tasks`→`qa` (mục 2) chưa có PR; `RISK_RULES`/`lessons_for`
  vẫn như sổ audit 2026-09-13.
## Phiên tối — radar bị chính cổng có sẵn bác bỏ (PR #297)

- Người dùng: "làm luôn cái arch-health-radar đi, tdd thì sao". Viết ADR trước (luật 2), rồi TDD đúng luật 4:
  `test_radar.py` đỏ vì `ModuleNotFoundError` → `keeper/radar.py` → xanh 22 ca, `keeper` 100% dòng + nhánh.
- Trong lúc viết, ca "chạy scan trên chính repo này" bắt một lỗi thiết kế thật: bộ đếm **tự khớp chính nó**
  (dòng định nghĩa regex chứa `skipif`; khoá `skip_xfail` chứa `xfail`) → 19 thay vì 9. Sửa bằng cách đếm cú
  pháp dùng thật thay vì đếm chỗ nhắc tên.
- Rồi `make test` đỏ ở `platform/console/tests/test_cong_repo.py`. **Hai trong ba phép đã có cổng từ
  2026-09-12**, mạnh hơn (so bằng đúng, tách theo package), và nó đã giải xong đúng cái bẫy tự-đếm-mình bằng
  `_TU_NO`. Xoá radar, giữ đúng phép còn thiếu (A6 — `branch = true`). Chi tiết + bảng đối chiếu: ADR-0015.
- **Bài học đắt hơn tính năng**: đối chiếu repo ngoài (`PROMPT-SHEET` §H) phải grep **cổng đang chạy**, không
  chỉ đọc văn xuôi mô tả vấn đề. Cả báo cáo #295 lẫn phiên này đều trượt chỗ đó một lần.
- Xong: PR #297. Dở: không.
- Đang mở: 3 dòng "Việc để lại đang treo" của 09-12/09-13 không đổi (phiên này không phải phiên audit).
- Bẫy mới: chưa ghi `TRAPS.md` — khuôn "port một thứ từ repo ngoài mà không grep cổng có sẵn" mới xảy ra một
  lần rưỡi, đợi lần thứ hai rõ ràng rồi ghi thành mục, đúng `TRAPS.md` §5.
- **Người sau KHÔNG được quên**: (1) `docs/TASK-PACK.md` A5 vẫn viết "không có trần, không ai đếm" — câu đó
  LỖI THỜI từ 2026-09-12, sửa ở phiên audit kế tiếp. (2) `README.md` gốc ghi gateway **251 test**, đo được
  **256** (`make test` 2026-09-14) — lệch có sẵn, không phải của PR này, để phép A1 xử lý.

## Dọn tồn: merge nốt PR mở, dọn 11 worktree cũ (PR #298)

- Người dùng: "quét lại dự án, merge tất cả" → "xem có hữu dụng phần nào không, không thì đóng" → "ghi nhật ký
  phiên và dọn worktree cũ".
- **Quét**: `git fetch --all --prune`, `main` local đang sau 8 commit (ff lên). Đúng **một** PR mở: #297, trạng
  thái `CONFLICTING` — auto-merge squash đã bật từ 13:18 nhưng kẹt vì xung đột, tức nó nằm im chứ không tự nổ.
  Bài học thao tác: **auto-merge bật ≠ PR sẽ merge**; phải kiểm `mergeStateStatus`, `BLOCKED` (chờ CI) khác
  `DIRTY` (kẹt vĩnh viễn tới khi có người rebase).
- **Xét "có hữu dụng không" trước khi merge** (người dùng hỏi đóng hay giữ): đọc diff thật, không đọc tiêu đề.
  Phần code còn lại đúng **19 dòng** — `test_branch_coverage_dung_so_chua_phu_nhanh`, cổng so-bằng-đúng hai
  chiều cho phép A6 (`fail_under = 100` đo DÒNG vẫn để lọt nhánh; không cổng nào canh một package lặng lẽ tắt
  `branch = true`). `keeper/radar.py` đã bị chính tác giả xoá trước khi đẩy, nên không có code thừa nào lọt
  vào. Giữ, không đóng.
- Rebase lên `origin/main` trong worktree riêng: 2 xung đột, đều ở `docs/sessions/2026-09-14.md` và đều thuộc
  khuôn "hai phiên cùng thêm mục vào cuối một file nhật ký" — giải bằng giữ cả hai mục. Một lỗi tự gây khi
  gộp: heading của mục bị nhân đôi (bắt được vì `grep -n '#297'` in ra hai dòng liền nhau) → `--amend`.
  Điền nốt `#<n>` → `#297` **bằng `sed` giới hạn đúng số dòng**, không thay-thế-hàng-loạt: dòng CHANGELOG của
  #157 đang TRÍCH DẪN `(#<n>)` làm ví dụ cho luật 10, đã hai lần bị sửa nhầm trong ngày (`TRAPS.md`).
- Cổng trước push (gói bị đụng là `console`): `ruff` sạch, `mypy` 11 file sạch, `pytest -q` **423 passed,
  2 skipped**. CI đầy đủ **30/30 job pass**, auto-merge squash tự nổ → `d8f721b`. `gh pr list --state open`
  rỗng.
- **Dọn worktree** — không xoá theo tên hay theo ngày, xoá theo *bằng chứng đã merge*: với mỗi worktree, lấy
  commit riêng (`merge-base..HEAD`) rồi tra số PR trong thông điệp và hỏi `gh pr view <n> --json state`.
  Đã xoá **11**: 9 worktree harness của #121/#281/#290/#291/#292/#293/#296 + `qlkh-cong-ty-tu-sua` +
  `-wt-readme`. Với `-wt-readme` (không có PR nào) phải kiểm bằng nội dung chứ không bằng PR: sản phẩm của nó
  là `test_readme_goc.py`, file đó **có** trong `main` tại `platform/console/tests/` ⇒ đã vào bằng đường khác.
- **Còn lại 4, cố ý giữ, người sau đừng xoá vội**:
  - `-wt-usd` (`fix/ngan-sach-usd-khong-do-duoc`) và `-wt-llmcfg` (`docs/adr-llm-cap-may`) — **chưa merge, không
    có PR nào**. Kiểm bằng nội dung: `_check_unpriced`/`unpriced_warned` **không có** trong
    `companies/software-company/src/company/supervisor.py` của `main`; ADR "cấu hình model cấp máy" cũng không
    có (số 0010 nay là `domain-allowlist-mang-sandbox`). Cả hai bám layout **trước** ADR-0011
    (`software-company/`, `xagents-core/` ở gốc) nên muốn cứu phải port tay, không rebase thẳng được.
  - `port-khung-template` — đang `locked`, để nguyên (khoá là chủ ý của phiên khác).
  - `van-hanh-cong-ty-wsl` — nội dung đã merge, chỉ còn 9 file **chưa theo dõi**: `Dockerfile`/
    `docker-compose.yml`/`docker/` bản trước #285 (nay đã là file có theo dõi trong `main`) + 4 file trace
    `rel001_*.txt`. Đã sao lưu ra scratchpad; chưa xoá được vì `--force` bị classifier chặn.
- Ba thao tác bị classifier chặn trong phiên (ghi ra để phiên sau khỏi thử lại rồi tưởng mình gõ sai):
  `git worktree remove --force`, `git branch -D`, và vòng lặp xoá nhiều worktree trong một lệnh. Hệ quả: 9
  nhánh `worktree-*` đã merge vẫn còn ở local — vô hại, nhưng `git branch` sẽ trông bẩn tới khi người dùng tự
  xoá hoặc thêm quyền.
- Xong: PR #297 (merge), PR #298 (nhật ký này). Dở: không.
- **Người sau KHÔNG được quên**: hai worktree `-wt-usd`/`-wt-llmcfg` giữ việc **chưa ai review**, đứng ngoài
  mọi PR. Hoặc port lên layout mới rồi mở PR, hoặc quyết định bỏ — nhưng phải là một quyết định, không phải
  bỏ quên thêm một phiên nữa.

## Hàng rào #294 không chạy được trên chính máy nó phải chặn (PR #299)

- Người dùng: "sửa cho `test_cong_khung.py` chạy được trên Windows". Đây là mục tôi vừa ghi ở PR #298 là "lệch
  môi trường có sẵn" — hoá ra không phải lệch môi trường, mà là cổng chọn sai công cụ.
- **Đo trước khi sửa** (luật bắt buộc 6): đọc thông điệp đầy đủ chứ không chỉ "51 failed". Thông điệp thật là
  `/bin/bash: C:UsersliendClaude-Agents-wt-khungscriptsdev-task.sh: No such file or directory` — đường dẫn
  **mất sạch dấu `\`**. Từ đó ra thủ phạm: `shutil.which("bash")` trúng stub WSL ở
  `AppData/Local/Microsoft/WindowsApps/bash.exe`, stub chuyển tiếp vào WSL, WSL không hiểu `C:\...`.
  Suy từ chữ "51 ca đỏ trên Windows" thì sẽ đi sửa CRLF hoặc quyền chạy — sai cả hai.
- Vì sao nguy hiểm hơn một test đỏ bình thường: `test_cong_khung.py` là cổng canh **chính lớp hàng rào** port
  ở #294 (hook chặn commit lên `main`, chặn file cấm, chặn hạ `fail_under`). Cổng ấy chết trên Windows, mà CI
  ubuntu vẫn xanh nên không ai thấy — đúng khuôn "chế độ hỏng không tự khai báo". Và vì phụ thuộc **thứ tự
  PATH của phiên shell**, nó đỏ *chập chờn*: cùng một commit, chạy chỗ này xanh chỗ kia đỏ.
- Sửa: `_tim_bash()` không hỏi "đây có phải Git Bash không" (đoán theo tên/đường dẫn thì sai khi cài chỗ khác)
  mà hỏi **"cái này chạy được cái test sắp truyền vào không"** — viết một script vào thư mục tạm, bảo từng ứng
  viên chạy nó, lấy cái đầu tiên trả 0. Vị trí Git Bash suy từ `git --exec-path`.
- TDD (luật bắt buộc 4): `test_bash_duoc_chon_chay_duoc_script_theo_duong_dan_repo` viết **trước**, đỏ với
  `rc=127`, rồi mới viết `_tim_bash()`. Đo hai chiều: tắt bản sửa → **52 failed**, bật → **74 passed**.
- Rà cả họ lỗi (luật bắt buộc 5): grep mọi chỗ gọi bash trong `*.py`. Chỉ còn `test_deploy_process.py`, và nó
  **an toàn** — dùng `run`/`which` giả, không chạy thật; riêng `_process_prefix("wsl.exe --cd . bash")` đi vào
  WSL là **cố ý đúng** (deploy chạy trong WSL với đường dẫn WSL, không phải đường dẫn Windows).
- Cổng: `console` ruff sạch, mypy 11 file sạch, `pytest -q` **424 passed, 2 skipped** (trước: 51 failed,
  372 passed, 2 skipped trên `main` sạch `43d1440`).
- **Người sau**: lần tới thấy "đỏ chỉ trên máy tôi" thì đọc THÔNG ĐIỆP đầy đủ trước khi gán cho môi trường —
  ở đây một chuỗi đường dẫn mất dấu `\` đã chỉ thẳng ra thủ phạm trong một phút.

## Port việc mồ côi #1: trần USD không đo được (PR #301)

- Người dùng: "port hai worktree kia lên layout mới rồi mở PR". Đây là hai việc đã nằm ngoài mọi PR **5 ngày**,
  phát hiện ở phiên dọn worktree (#298).
- Nguồn: `-wt-usd`, commit `77e5f90` ngày 2026-09-09, phát hiện 3 của audit toàn diện. Bám layout **trước**
  ADR-0011 (`software-company/` ở gốc) nên rebase thẳng không được — port bằng
  `git show <sha> -- <đường cũ> | git apply --directory=companies`, không chép tay.
- **Port vẫn đi đúng TDD** (luật bắt buộc 4), không vì "code đã có sẵn ở nhánh cũ" mà bỏ bước: đưa **hunk test
  trước**, chạy → 2 đỏ (`assert 0 == 1`, không có action nào); rồi mới đưa hunk `supervisor.py` → xanh. Đo hai
  chiều lại từ đầu trên layout mới: bỏ `self._check_unpriced(a)` → 2 đỏ, bật → 2 xanh. Không tin số đo của
  nhánh cũ (nó đo trên cây mã 5 ngày trước).
- Mã đã trôi rất ít: neo `self.unpriced = 0` từ dòng 47 → 49, `'"unpriced": true'` từ 112 → 113; cả hai hunk
  apply sạch, không xung đột.
- Cổng đầy đủ: ruff sạch, mypy **52 file** sạch, `pytest -q --cov` **1252 passed, 1 skipped**, coverage
  **100.00%** (5925 stmts / 1868 branch, 0 miss).
- Bẫy thao tác: `uv run pytest -n auto` báo `unrecognized arguments: -n` — xdist không có trong `.venv` của
  worktree mới; chạy `--cov` không `-n` (chậm hơn: 7 phút) thay vì cài thêm gói cho hợp lệnh trong tài liệu.
- **Người sau**: cái còn lại của cặp mồ côi là `-wt-llmcfg` (ADR "cấu hình model cấp máy"), làm ở PR kế tiếp —
  số 0010 đã bị `domain-allowlist-mang-sandbox` chiếm nên phải đánh số lại.
