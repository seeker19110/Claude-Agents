# Phiên 2026-09-24 — vận hành CAMPUS-UNI-20260922, gỡ kẹt TCK-001

## Đã làm

1. **Backend model.** `claude-sub` (tài khoản `~/.claude` mặc định) trả `403 Your organization has disabled Claude
   subscription access for Claude Code` — chính sách tổ chức, `claude login` lại vô ích. Tài khoản
   `~/.claude-liendv` nay chạy được (probe `claude -p` → OK; `company.probe` → chế độ `mcp`, model trả lời
   `claude-sonnet-5`). Tầng máy `~/.config/xagents/llm.yaml`: tắt `claude-sub`, bật `claude-sub-liendv`
   (Sonnet 5 mọi tier, effort low, `mcp_tools`), thêm `antigravity` (gateway 127.0.0.1:1123) làm dự phòng.
   `companies/software-company/llm.yaml`: `routing.prefer` mọi tier → `claude-sub-liendv`.
   - Bẫy đã mắc: `gateway setup` ghi `llm.yaml` dạng một-provider (không có `backends:`), nhưng tầng máy có
     `backends:` nên nó THẮNG (ADR-0016, gộp theo khoá gốc) — builder vẫn gọi `claude -p` và dính 403 thêm 3 lần.
     Kiểm bằng `uv run python -m company.probe --explain` trước khi tin cấu hình đã đổi.
2. **Chạy thật tiêu chí TCK-001 bằng shell** (builder không có shell, QA chặn vì thiếu bằng chứng chạy thật):
   `/healthz` 200 sau 1,12 s / 0,58 s — đạt; đổi tên CSDL khi server chạy → Windows `Device or resource busy`,
   `/healthz` vẫn 200 — KHÔNG đạt (kết nối của `ensure_database_ready()` lúc import `campus.wsgi` không đóng);
   thêm lỗi `UnicodeEncodeError` khi log tiếng Việt ra console cp1252. Output nguyên văn:
   `<repo khách>/.worktrees/TCK-001/docs/evidence/TCK-001-chay-that-2026-09-24.md`. Duyệt gate với hint cụ thể
   (đóng kết nối, stream utf-8, biến chạy thật thành `tests/test_chay_that.py` chạy được bằng tool `run test`).
3. **Sửa lỗi công ty** (PR này): ticket bị chặn lại sau restart không mở gate escalation vì `_rehydrate` đếm cả
   `gate.decide` chưa xử lý. Đã phải mở gate bằng tay (`gate_cli request escalation TCK-001 --by supervisor`).

4. **Sửa lỗi công ty** (PR thứ hai): handler QA chết `'NoneType' object has no attribute 'strip'` vì `git diff` in byte
   cp1252 — từ chính tệp bằng chứng tôi ghi (log waitress trên console Windows). Mọi lời gọi git/gh của `company`
   nay giải mã `errors="replace"`. Keeper (7 chỗ) + console (`git_truth.py`) còn cùng khuôn, tách việc riêng.

## Người sau không được quên

- Repo khách của CAMPUS-UNI là `C:/Users/liend/campus-company` — lấy từ `repo` trong `research-requests` của dự
  án, thắng `--repo` của tiến trình (`campus-manager` là repo khác, chưa có commit). Worktree ticket ở
  `campus-company/.worktrees/<id>`.
- `claude-sub` (tài khoản mặc định) vẫn 403. Tài khoản còn hạn mức duy nhất cho việc có tool là `claude-sub-liendv`.
- Builder không có shell ngoài `lint`/`test`: tiêu chí nghiệm thu dạng "chạy lệnh X trên máy" phải được biến thành
  test, nếu không QA sẽ chặn mãi.
