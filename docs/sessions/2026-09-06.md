# 2026-09-06

## Phiên 14:30 — đối chiếu skill bên ngoài, smoke cho `deployed`, bộ khung repo
- Xong: #85 (đối chiếu superpowers/frontend-design — merged), #86 (mỗi phiên một worktree — merged),
  #90 (ADR-0029 smoke — merged sau 2 lần rebase + 2 lần vá CI: coverage 100% và ruff), #91 (bộ khung 9 file — merged), PR sổ Ruling ADR-0030 (đang mở).
- Dở: sổ Ruling bước 2 (đổi Quy tắc chung 21 agent — chờ đo sổ có được dùng không); trace một ticket từ intake tới deploy (mục 08 observability), ảnh chụp giao diện
  trong DoD frontend. Mỗi cái một ADR riêng.
- Đang mở: #90 chờ `quality`. Phiên "Tiếp quản công ty phần mềm" vẫn chạy trong `C:\Users\liend\Claude-Agents`
  (nhánh `fix/wip-head-is-pr`) — không dùng clone đó; các worktree `Claude-Agents-wt-*` là của phiên này, dọn bằng
  `git worktree remove` khi PR đã merge.
- Bẫy mới: đổi schema = đổi prompt → eval-record lại 21 agent (~2,5 phút/agent) — `software-company/TRAPS.md`; TRAPS.md §3 "hai phiên chung một clone", "sửa checklists.md mà không make subagents", "thêm file test/ADR
  mà không sửa README".
- Người sau KHÔNG được quên: orchestrator đang chạy `--repo D:/khach/qlkh --deliver`; REL-019 kẹt
  production/pending_human — kích lại bằng request + approve gate release sau CR-OPS-001 (memory
  `project-rel019-pending-human-production`).

## Phiên 16:30 — đánh giá toàn diện + đặc tả nâng cấp
- Xong: pull #90; lint/mypy/test cả 4 package xanh (785/440/213/179); đặc tả `docs/DAC-TA-NANG-CAP-2026-09.md` (PR này).
- Dở: không. Phát hiện: PR #94 CONFLICTING (1 dòng `software-company/README.md`, thuộc worktree `Claude-Agents-dev`);
  CHANGELOG hụt #92 #93; 18 nhánh local đã merge squash chưa xoá; 3 file `llm.yaml.bak-*` không bị gitignore.
- Đang mở: #94 chờ rebase. Nhánh `feat/so-ruling-adr-0030` wip chưa PR.
- Bẫy mới: không.
- Người sau KHÔNG được quên: bắt đầu từ đợt 0 của đặc tả (V1–V5), rồi B1–B8; E1 (tách máy trạng thái) trước khi
  nhận thêm fix cùng họ trạng thái/khoá/event cũ.

## Phiên B5 — supervisor đếm nợ kiến trúc treo (ADR-0032)
- Xong: PR `feat/supervisor-no-kien-truc` — `Supervisor._count_debt`/`debt_table`, `Orchestrator._check_debt`
  (gate escalation subject = project_id, không pause), audit `debt.escalated`/`debt.decided`, ngưỡng `llm.yaml
  debt_reviews`, supervisor v12 + eval case mới, `tests/test_no_kien_truc_adr0032.py` (đo hai chiều).
- Dở: đề xuất 2 (`open_decisions` trong plan) và B6 chưa làm — nợ mới được đếm, chưa được ngăn từ lúc lập kế hoạch.
- Người sau KHÔNG được quên: "liên tiếp" đo theo (dự án, nguồn review); hai mã cùng chạm ngưỡng một lúc thì gate mở
  từng mã một (subject trùng), mã sau tự mở khi gate trước đóng.

## Phiên đánh giá sâu + tầm nhìn
- Xong: `docs/DANH-GIA-VA-TAM-NHIN-2026-09.md` (nhánh `claude/danh-gia-tam-nhin-phat-trien-521u5w`). Khảo sát cả bốn package; kết luận chính: E1 và package lõi chung `xagents-core` phải đi trước đợt 2/3/4; D2 sandbox lên P0 và mở rộng sang `smoke`/`stacks`.
- Dở: không. Phát hiện nhỏ chưa sửa: `gateway/.env.example` còn cổng 8100 ở 4 chỗ; root README ghi ADR 0001–0028 và 20 agent trong `software-company/pyproject.toml` (thực tế 32 ADR, 21 agent).
- Người sau KHÔNG được quên: bốn quyết định ở §7 của tài liệu cần người chủ ký trước khi mở phiên song song.
