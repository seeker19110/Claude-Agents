# 2026-09-06

## Phiên 14:30 — đối chiếu skill bên ngoài, smoke cho `deployed`, bộ khung repo
- Xong: #85 (đối chiếu superpowers/frontend-design — merged), #86 (mỗi phiên một worktree — merged),
  #90 (ADR-0029 smoke — merged sau 2 lần rebase + 2 lần vá CI: coverage 100% và ruff), #91 (bộ khung 9 file — merged), PR sổ Ruling ADR-0030 (đang mở).
- Dở: sổ Ruling bước 2 (đổi Quy tắc chung 21 agent — chờ đo sổ có được dùng không); trace một ticket từ intake tới deploy (mục 08 observability), ảnh chụp giao diện
  trong DoD frontend. Mỗi cái một ADR riêng.
- Đang mở: #90 chờ `quality`. Phiên "Tiếp quản công ty phần mềm" vẫn chạy trong `C:\Users\liend\Claude-Agents`
  (nhánh `fix/wip-head-is-pr`) — không dùng clone đó; các worktree `Claude-Agents-wt-*` là của phiên này, dọn bằng
  `git worktree remove` khi PR đã merge.
- Bẫy mới: đổi schema = đổi prompt → eval-record lại 21 agent (~2,5 phút/agent) — `software-company/TRAPS.md`; TRAPS.md §3 "hai phiên chung một clone", "sửa checklists.md mà không make subagents", "thêm file test/ADR
  mà không sửa README".
- Người sau KHÔNG được quên: orchestrator đang chạy `--repo D:/khach/qlkh --deliver`; REL-019 kẹt
  production/pending_human — kích lại bằng request + approve gate release sau CR-OPS-001 (memory
  `project-rel019-pending-human-production`).

## Phiên 16:30 — đánh giá toàn diện + đặc tả nâng cấp
- Xong: pull #90; lint/mypy/test cả 4 package xanh (785/440/213/179); đặc tả `docs/DAC-TA-NANG-CAP-2026-09.md` (PR này).
- Dở: không. Phát hiện: PR #94 CONFLICTING (1 dòng `software-company/README.md`, thuộc worktree `Claude-Agents-dev`);
  CHANGELOG hụt #92 #93; 18 nhánh local đã merge squash chưa xoá; 3 file `llm.yaml.bak-*` không bị gitignore.
- Đang mở: #94 chờ rebase. Nhánh `feat/so-ruling-adr-0030` wip chưa PR.
- Bẫy mới: không.
- Người sau KHÔNG được quên: bắt đầu từ đợt 0 của đặc tả (V1–V5), rồi B1–B8; E1 (tách máy trạng thái) trước khi
  nhận thêm fix cùng họ trạng thái/khoá/event cũ.

## Phiên B5 — supervisor đếm nợ kiến trúc treo (ADR-0032)
- Xong: PR `feat/supervisor-no-kien-truc` — `Supervisor._count_debt`/`debt_table`, `Orchestrator._check_debt`
  (gate escalation subject = project_id, không pause), audit `debt.escalated`/`debt.decided`, ngưỡng `llm.yaml
  debt_reviews`, supervisor v12 + eval case mới, `tests/test_no_kien_truc_adr0032.py` (đo hai chiều).
- Dở: đề xuất 2 (`open_decisions` trong plan) và B6 chưa làm — nợ mới được đếm, chưa được ngăn từ lúc lập kế hoạch.
- Người sau KHÔNG được quên: "liên tiếp" đo theo (dự án, nguồn review); hai mã cùng chạm ngưỡng một lúc thì gate mở
  từng mã một (subject trùng), mã sau tự mở khi gate trước đóng.

## Phiên đánh giá sâu + tầm nhìn
- Xong: `docs/DANH-GIA-VA-TAM-NHIN-2026-09.md` (nhánh `claude/danh-gia-tam-nhin-phat-trien-521u5w`). Khảo sát cả bốn package; kết luận chính: E1 và package lõi chung `xagents-core` phải đi trước đợt 2/3/4; D2 sandbox lên P0 và mở rộng sang `smoke`/`stacks`.
- Dở: không. Phát hiện nhỏ chưa sửa: `gateway/.env.example` còn cổng 8100 ở 4 chỗ; root README ghi ADR 0001–0028 và 20 agent trong `software-company/pyproject.toml` (thực tế 32 ADR, 21 agent).
- Người sau KHÔNG được quên: bốn quyết định ở §7 của tài liệu cần người chủ ký trước khi mở phiên song song.
- Tiếp: người chủ chọn kịch bản B → `docs/DAC-TA-KICH-BAN-B.md` + `docs/DAC-TA-TRIEN-KHAI-KICH-BAN-B.md` (cùng nhánh). Bắt đầu thực thi từ K0, rồi ADR-0037/ADR-0035/ADR gốc 0001 trước code. Phân phiên song song ở Phụ lục B của tài liệu triển khai.

## Phiên 16:30–19:00 — đánh giá toàn diện, đặc tả nâng cấp, thi công đợt 0/1/3
Phiên điều phối: viết đặc tả rồi giao 8 trợ lý chạy song song, mỗi trợ lý một worktree.

- **Xong (11 PR merge)**: #96 đặc tả `docs/DAC-TA-NANG-CAP-2026-09.md` · #98 đợt 0 (CHANGELOG đủ số PR, gitignore
  `.bak`, mốc lịch sử repo, CI bắt PR phải có dòng CHANGELOG) · #99 B7 lệnh `trace` · #100 B4 gate acceptance tự
  chạy smoke · #101 E4 ba ADR gateway + bộ khung 4 file · #102 B3 `regression-staging` mang `evidence.run` ·
  #103 B5 supervisor đếm nợ kiến trúc → escalation cấp dự án (ADR-0032) · #104 B1 Gate 1 đòi `runtime` (ADR-0031) ·
  #105 (tiếp quản từ phiên khác) · #106 `.gitattributes merge=union` · #107 console C1–C8+C10 · #108 sửa test chập chờn.
- **Dở**: B6 (DoD sản phẩm chạy được + ảnh chụp UI) và S1/S7 (studio đa phương thức + video thật) đang giao lại sau
  khi hai trợ lý chết vì mất mạng. B8 (dự án mẫu thứ hai chạy thật) chưa bắt đầu. Đợt 2 (D1 deploy thật, D2 sandbox,
  D3 thông báo, D4 UAT, D5 rebase RC) và đợt 5 (E1 tách máy trạng thái, E2 bus, E3 eval, E5 CI) chưa bắt đầu.
  C9 (màn Xưởng video của console) chờ đợt 4.
- **Bẫy mới**: `TRAPS.md` khuôn 5 — xếp theo dấu thời gian tưởng là thứ tự toàn phần. Test golden escalation đỏ
  1/6 lần; nguyên nhân là hai event trùng `ts` tới micro giây nên thứ tự chèn quyết định. Sửa bằng khoá phụ = thứ
  tự ghi vào bus (#108). Dấu hiệu: test chỉ đỏ một trong vài lần — đừng chạy lại cho qua.
- **Quy tắc mới của người dùng**: nhiều PR song song thì giải xung đột **từ PR nhỏ lên PR lớn hơn** — chỉ PR đầu
  hàng bật auto-merge, PR lớn hơn `--disable-auto` rồi rebase sau. Đã ghi vào bộ nhớ dài hạn.
- **Người sau KHÔNG được quên**: eval `database`/`mobile`/`risk` đang FAIL điểm chấm từ trước (không phải cổng CI) —
  thuộc mục E3. E1 (tách máy trạng thái khỏi `orchestrator.py` 2039 dòng) nên làm TRƯỚC khi nhận thêm fix cùng họ
  trạng thái/khoá/event cũ; đó là điều kiện để chuỗi `fix(company)` dừng lại.

## Phiên triển khai kịch bản B — K0
- Xong: PR K0 của `docs/DAC-TA-TRIEN-KHAI-KICH-BAN-B.md` — #114 (**chưa merge, KHÔNG bật auto-merge** theo yêu
  cầu người dùng). Vệ sinh số liệu trôi: README gốc 20→21 agent + 749→850 test, `software-company/pyproject.toml`
  + `README.md` 20→21, `gateway/.env.example` 8100→1123, đường dẫn sai trong ADR-0031, `docs/adr/README.md` mới,
  test hai chiều `console/tests/test_readme_goc.py`. Nghiệm thu: `software-company` 850 test/100% cov,
  `console` 214 test/100% cov, ruff+mypy sạch cả hai.
- Dở: K1–K9 (ADR-0037 tách máy trạng thái, K2 sandbox, K3 `xagents-core`, K4 bus phiên bản, K5 eval CI, K6 mypy
  strict, K7 console ES module, K8 tài liệu, K9 chạy thật) — chưa bắt đầu, quy mô lớn (31 PR), cần worktree riêng
  theo Phụ lục B của đặc tả triển khai.
- Bẫy mới: `test_readme_goc_khop_nguong_coverage_va_so_test_hai_cong_ty`
  (`software-company/tests/test_review_fixes_2026_09.py:420`) đòi dòng software-company trong README gốc kết
  thúc đúng khuôn `<N> test |` — đừng xoá số theo nghĩa đen của đặc tả K0 ("bỏ số"), phải giữ khuôn và thay bằng
  số đúng.
- Người sau KHÔNG được quên: #114 chờ người duyệt merge thủ công. K1 là điều kiện để chuỗi `fix(company)` dừng
  lại (đã ghi ở trên) — nên làm sớm, nhưng là một phiên riêng vì 7 PR + ADR-0037.

## Phiên triển khai kịch bản B — K1 (một phần)
- Xong: #114 (K0) merge theo yêu cầu người dùng ("merge trước đi"). PR #115 (K1, một phần) — ADR-0034 (số
  thực tế, không phải 0037 như đặc tả), tách `orch/routes.py` (K1.2), `orch/verify.py` + `orch/cli.py` (K1.1),
  `orch/rehydrate.py` (một phần K1.3) khỏi `orchestrator.py` (2269 → 1600 dòng), không đổi hành vi. Nghiệm thu:
  850 test/100% cov, ruff+mypy sạch. Người dùng chọn "gộp mỗi epic thành 1 PR lớn, tự merge khi CI xanh".
- Dở: phần rủi ro cao hơn của K1 (`OrchState` dataclass + property alias ~30 trường, `worktree_flow.py`,
  `scheduler.py`, `gates_flow.py`, `ticket_fsm.py`/`release_fsm.py` + bảng chuyển trạng thái) — chạm vòng lặp
  chính (process/run/tick/watch/_call, khoá merge, FSM ticket/release). Cố ý để phiên sau, có thời gian viết
  test rehydrate tham số hoá theo từng trường trước khi đổi cấu trúc trạng thái — rush là đúng thứ ADR-0034
  cảnh báo. K2 (sandbox), K3 (xagents-core) và K4–K9 chưa bắt đầu.
- Bẫy mới: di chuyển code vào package con `orch/` — `from .X import Y` (một dấu chấm) phải thành `from ..X
  import Y` khi hàm lùi thêm một cấp thư mục (gặp với `.metrics`/`.sqlite_bus`/`.trace`/`.llm`). Test
  monkeypatch nhắm `company.orchestrator.<tên>` (COMPANY_ROOT, _reexec, run_smoke, sys/os) sai chỗ sau khi hàm
  dời module — Python đọc tên từ namespace module ĐANG CHỨA hàm lúc gọi, không phải nơi re-export.
- Người sau KHÔNG được quên: PR #115 đang chờ CI; tự merge khi xanh (đã được phép). Tiếp K1 phần còn lại hoặc
  K2 tuỳ phiên sau chọn — cả hai đều còn nguyên trong đặc tả triển khai.

## Phiên K2.1 — sandbox tiến trình (ADR-0035)

- Đã làm: ADR-0035 `software-company/docs/adr/0035-sandbox-tien-trinh.md` + module mới
  `software-company/src/company/sandbox.py` (~185 dòng) + 33 ca test `tests/test_sandbox.py` + ba trường
  `sandbox`/`sandbox_image`/`sandbox_runtime` trong `LLMConfig` (yaml + env `COMPANY_SANDBOX*`) + ba dòng chú
  thích trong `llm.example.yaml`. Số liệu README (ADR 0001–0035, 50 file test) cập nhật theo test đếm-từ-đĩa.
- CÒN DỞ: **K2.2 chưa làm** (nối `tools.run`, `workspace._run`, `smoke.Popen` vào `Sandbox`; audit `sandbox=`;
  schema `local_checks.sandbox` + `release-events.smoke.sandbox`; sửa đoạn tự nhận trong `SECURITY.md`) và
  **K2.3 chưa làm** (studio: `CommandTTS`, `ffmpeg` timeout, `qc` qua sandbox; console K2.7 tile cảnh báo).
  Module hiện chưa có nơi gọi — đó là cố ý, không phải sót.
- Bẫy mới: `pytest -n auto` không chạy được trong worktree này (xdist không có trong venv của worktree) — chạy
  `uv run pytest --cov` không `-n`, mất ~11 phút. Và `git checkout -- <file>` KHÔNG khôi phục được file chưa
  track: khi cố tình phá code để đo test hai chiều, phải hoàn tác bằng tay.
- Người sau không được quên: `SECURITY.md` vẫn nói "sandbox là đường dẫn + env" và câu đó vẫn ĐÚNG cho tới khi
  K2.2 nối xong — đừng sửa sớm rồi tưởng đã được bảo vệ. Image mặc định `python:3.12-slim` chỉ hợp repo Python.
## Phiên tiếp kịch bản B — K1 (phần OrchState)
- Xong: `orch/state.py` — `OrchState` gom 24 biến trạng thái RAM khỏi `Orchestrator.__init__`;
  `install_aliases()` gắn property hai chiều nên 28 file test và các module `orch/*` gọi `o.<tên>` như cũ,
  không đổi một dòng gọi nào. Mỗi trường khai `metadata["rehydrate"]` = nguồn dựng lại (`audit:<action>`,
  `topic:<tên>`, hay `ram-only`). Test mới `tests/test_orch_state_rehydrate.py`: 22 ca tham số hoá theo
  `fields(OrchState)` + một ca chốt "trường mới phải khai nguồn và có kịch bản gieo" + một ca bí danh ghi
  xuyên xuống `state`. Đo hai chiều: bỏ dòng khôi phục của `conflict_retries` và `debt_gate` → đúng hai ca đỏ
  mang tên trường; khôi phục → 24 xanh. Nghiệm thu: 873 test pass, ruff + mypy sạch (coverage thiếu đúng 2
  dòng POSIX của `sqlite_bus` như mọi lần chạy trên Windows).
- Dở: K1.4–K1.7 còn nguyên (`worktree_flow.py`, `scheduler.py`, `gates_flow.py`, `ticket_fsm.py`/
  `release_fsm.py` + bảng chuyển trạng thái). `orchestrator.py` mới xuống 1592 dòng, đích cuối K1 là ≤ 300.
  Hạn chế đã biết và cố ý giữ: `deferred`/`defer_until` chỉ khôi phục MỘT PHẦN (event hoãn vì `gate:`/`paused:`
  không ghi `defer.until` nên mất khi restart) — đã ghi thẳng vào metadata của trường, không giấu.
- Bẫy mới: (1) `_thuoc_tinh_lech` trong `test_orchestrator.py` duyệt `vars(a)`, nên gom trạng thái vào một
  dataclass làm nó so NGUYÊN KHỐI `OrchState` — mất đúng thứ nó sinh ra để làm (chỉ tên trường bị mất) và đỏ
  vì các trường cố ý không dựng lại. Sửa bằng `_khoa_so_sanh` trải `state` ra từng trường. (2) mypy không
  thấy property gắn động → phải khai lại kiểu trong khối `if TYPE_CHECKING:` của thân class. (3) **Đừng chạy
  `uv sync --all-extras` trong worktree**: nó cài optional deps (langgraph…) và làm 20 test + mypy đỏ theo
  kiểu không tái hiện được trên CI; `uv sync` trần là đúng môi trường CI.
- Người sau KHÔNG được quên: agent nền của phiên này chỉ uỷ quyền vòng quanh chứ không làm việc (3 lần liên
  tiếp, mỗi lần 1–2 tool call rồi báo "đã giao cho agent khác"), và `SendMessage` bị tắt nên không sửa được
  giữa chừng — giao việc dài cho subagent thì phải kiểm output thật, đừng tin dòng báo cáo.

## Phiên K2.3 — lệnh con của phòng video đi qua Sandbox

- Làm: `Studio-creators/src/studio/sandbox.py` — bản sao gần nguyên văn của `company/sandbox.py` (ADR-0035), có
  ghi chú đầu file "bản tạm, xoá ở K3.2 khi module vào `xagents-core`". Khác bản company đúng bốn chỗ, đều ghi
  trong docstring: `clean_env`/`SECRET_ENV` nằm ngay đó (studio không có `workspace.py`); `RunSpec.stdin` (TTS
  đưa văn bản vào stdin — với container thì env buộc phải quay về `-e` vì `--env-file -` đã chiếm stdin);
  `RunSpec.read_only` (qc mount `:ro`); và **mặc định là `subprocess`, KHÔNG phải `auto`** — ffmpeg đọc/ghi
  đường dẫn tuyệt đối trải nhiều thư mục mà container chỉ thấy `cwd` được mount, nên bật container phải là
  việc cố ý. `CommandTTS`, `FFmpegAssembler._run`, `qc._run` gọi `sandbox.run`; ffmpeg thêm trần thời gian
  (`media.yaml` khoá mới `render.timeout_s`, mặc định 600 s); audit `render.*` thêm `sandbox`.
- Đo hai chiều: bỏ `clean_env()` trong `CommandTTS.synthesize` và bỏ `timeout=self.timeout` trong
  `FFmpegAssembler._run` → đúng hai ca đỏ (`test_command_tts_di_qua_sandbox_*`, `test_ffmpeg_co_tran_thoi_gian_*`);
  khôi phục → 26/26 xanh. Ca ffmpeg cố ý đặt trần 30 s khác mặc định 600 s, nếu không thì quên chuyền timeout
  xuống `RunSpec` vẫn xanh nhờ trùng số mặc định.
- Bẫy đã mắc: 11 test cũ đỏ vì chúng vá `media.subprocess.run` / `qc.subprocess.run`, mà `SubprocessSandbox`
  giữ `runner=subprocess.run` gắn lúc định nghĩa. Cách chữa ĐÚNG là sửa test bơm `SubprocessSandbox(runner=…)`
  (hoặc thay `qc.qc_sandbox`), không phải nới sandbox để vẫn vá được `subprocess` — vá được nghĩa là test không
  chứng minh được lệnh có đi qua sandbox hay không.
- Nghiệm thu (chạy trong `Studio-creators/`): `ruff check src tests` sạch; `mypy src/studio` sạch 27 file;
  `pytest -q --cov` 466 pass, coverage 100.00%. README (số ca/số file test) cập nhật theo test đếm-từ-đĩa.
- Dở / người sau: chỉ ba điểm gọi trong đặc tả đi qua sandbox; `media.probe_duration` vẫn gọi `subprocess.run`
  thẳng (ffprobe đo thời lượng) — cố ý để nguyên vì ngoài phạm vi K2.3, nêu ra chứ không tự sửa. Console K2.7
  (tile cảnh báo đọc `sandbox` trong audit) chưa làm.

## Phiên K1.4 — worktree_flow.py

- Xong: `orch/worktree_flow.py` nhận `_learn_repo`, `integration_for`, `_project_of_ticket`,
  `_integration_of_ticket`, `_has_integration`, `workspace`, `_integrate_approved`, `_branch_ahead`,
  `_merge_ticket`, `_merge_ticket_locked`, `_read_only_tools`, `_author_tests`, `_engineer`, `comment`,
  `takeover` khỏi `orchestrator.py` (1600 → 1402 dòng). Mỗi hàm nhận `o: Orchestrator` làm tham số đầu, gán
  làm method (`_merge_ticket = worktree_flow.merge_ticket`…) — bề mặt `o.<tên>` không đổi.
- Bẫy mới: lời gọi NỘI BỘ giữa các hàm vừa chuyển phải đi qua `o.<tên method>(...)` (bản đã gán trên class),
  KHÔNG được gọi thẳng hàm module (`workspace(o, tid)`). Gọi thẳng làm 2 test `test_orchestrator_coverage_gaps.py`
  đỏ ngay: chúng `monkeypatch.setattr(orch, "workspace", lambda tid: FakeWs())` trên INSTANCE, còn hàm module
  đọc biến toàn cục của chính nó, không thấy override trên instance. Đây là cùng khuôn với K1.2 (routes.py) đã
  ghi trong đặc tả — chỉ khác lần này bị bỏ sót vì `_merge_ticket_locked` gọi 4 hàm anh em cùng module.
- Đo hai chiều: sửa `merge_ticket_locked` để bỏ qua điều kiện `_branch_ahead` (luôn coi là không có commit mới)
  → `test_integrate_approved_merge_lai_ticket_da_integrated_khi_co_commit_moi` đỏ đúng như dự đoán (bản sửa
  không vào nhánh tích hợp); khôi phục → xanh lại.
- Nghiệm thu: `ruff check src` sạch, `mypy src/company --ignore-missing-imports` sạch (40 file), `pytest -q
  --cov` 906 pass/1 skip, coverage thiếu đúng 2 dòng POSIX của `sqlite_bus.py` (bình thường trên Windows, xem
  CLAUDE.md).
- Dở: K1.5–K1.7 còn nguyên (`scheduler.py`, `gates_flow.py`, `ticket_fsm.py`/`release_fsm.py` + bảng chuyển
  trạng thái). `orchestrator.py` còn 1402 dòng, đích cuối K1 là ≤ 300 — còn khá xa, `_call`/`process`/`run`/
  `tick`/`watch` (vòng lặp chính) vẫn ở nguyên trong `orchestrator.py`, đó là phần K1.5/K1.7 phải chạm tới.
- Người sau KHÔNG được quên: hai agent nền phiên trước (K1.3 OrchState, K2.3 studio sandbox) đã dừng do phiên
  Claude Code trước thoát giữa chừng — cả hai vẫn còn worktree dưới `.claude/worktrees/agent-*` với việc đã
  làm gần xong; kiểm output thật trước khi coi là mất. K1.3 hoá ra đã lên `main` qua PR #117 trước khi phiên
  đó dừng (worktree cũ giờ là rác, đã xoá); K2.3 chỉ còn thiếu commit+PR, đã hoàn tất trong phiên này thành
  PR #119.

## Phiên K1.5 — scheduler.py

- Xong: `orch/scheduler.py` nhận `_actionable`, `_track_pause`, `_on_event`, `_target` (đổi tên hàm module
  thành `target`, không giữ gạch dưới vì `@staticmethod` gán qua `staticmethod(scheduler.target)`),
  `_parallel_ok`, `_take_batch`, `run`, `_integrate_pending`, `tick`, `watch`, `_maybe_reload`, `_defer`,
  `_retry_deferred`, `_mark`, `_remember`, `_audit` khỏi `orchestrator.py` (1402 → 1221 dòng). Đây là VÒNG
  LẶP CHÍNH — rủi ro cao nhất trong K1 tính tới giờ.
- Bẫy mới: hai lớp import vòng lặp khác nhau, cả hai đều vì `orchestrator.py` nhập `orch.scheduler` ở gần đầu
  file nhưng `class ReloadRequested` và `class StepResult` định nghĩa SAU dòng nhập đó. Nhập thẳng ở đỉnh
  `scheduler.py` (`from ..orchestrator import ReloadRequested, StepResult`) vỡ vòng lặp import ngay lúc
  `import company.orchestrator`. Chữa bằng nhập LƯỜI bên trong hàm (`watch`, `_maybe_reload` cho
  `ReloadRequested`; `_integrate_pending`, `tick` cho `StepResult`) — cùng khuôn `orch/cli.py` đã dùng cho
  chính hai tên này (xem docstring đầu `cli.py`).
- Bẫy mới (2): test `test_watch_survives_tick_error` (`tests/test_upgrade_points.py:252`) monkeypatch
  `"company.orchestrator.time.sleep"` — đỏ ngay vì `time.sleep` giờ gọi từ `company.orch.scheduler`, không
  còn từ `orchestrator.py`. Sửa test trỏ `"company.orch.scheduler.time.sleep"` (đây CHÍNH XÁC là bẫy dự đoán
  trước trong đặc tả K1.1: "Python đọc tên từ namespace module ĐANG CHỨA hàm lúc gọi").
- Đo hai chiều: bỏ `PAUSING` check trong `_track_pause` (luôn coi hành động không phải pause) →
  `test_paused_ticket_is_deferred_until_resume` đỏ đúng chỗ (`T1` chạy thẳng tới `merged` thay vì bị hoãn ở
  `dispatched`); khôi phục → xanh. (Thử trước với việc bỏ guard `once` trong `_audit` và bỏ `_remember(key)`
  trong `tick()` — cả hai không đỏ vì test hiện có chỉ gọi `tick()` một lần, không có ca gọi lần hai để lộ
  trùng lặp; ghi lại đây làm khoảng trống test cho phiên sau, không tự thêm ca mới ngoài phạm vi K1.5.)
- Nghiệm thu: `ruff check src tests` sạch, `mypy src/company --ignore-missing-imports` sạch (41 file),
  `pytest -q` 906 pass/1 skip (coverage không đo bằng `--cov` lần này vì đã biết thiếu đúng 2 dòng POSIX của
  `sqlite_bus.py` trên Windows, không liên quan thay đổi).
- Dở: K1.6 (`gates_flow.py`: `_on_gate_decide`, `_check_escalations`, `_check_debt`,
  `_on_escalation_decided`, `_open/_close_acceptance_gate`, `_stall`, `_after_error`, `_rework_after_error`,
  `_retry_stalled`, `_retry_unhandled`, `_record_lessons`; và tách `tick()` thành `remind_gates`/
  `redispatch_reviews` theo đặc tả) và K1.7 (`ticket_fsm.py`/`release_fsm.py` + bảng chuyển trạng thái) còn
  nguyên. `orchestrator.py` còn 1221 dòng, đích cuối K1 là ≤ 300.
- Người sau KHÔNG được quên: `process()` (dispatcher chính, gọi `_on_gate_decide`/`_superseded`/`_learn_repo`/
  `_plan`/`_integrate`/`_mark`…) vẫn nguyên trong `orchestrator.py` — đó là điểm hội tụ cuối của K1.7, đừng
  di chuyển sớm nửa chừng kẻo `ROUTES`/`ticket_fsm.step`/`release_fsm.step` không có chỗ neo.

## Phiên K1.6 — gates_flow.py

- Xong: `orch/gates_flow.py` nhận `_on_gate_decide`, `_check_escalations`, `_check_debt`,
  `_on_escalation_decided`, `_open_acceptance_gate`, `_close_acceptance_gate`, `_stall`, `_after_error`,
  `_rework_after_error`, `_retry_stalled`, `_retry_unhandled`, `_record_lessons` khỏi `orchestrator.py`
  (1221 → 957 dòng). KHÔNG tách `tick()` thành `remind_gates`/`redispatch_reviews` như đặc tả gợi ý — để
  nguyên trong `scheduler.py` (K1.5 đã xong, đừng mở lại PR đã merge để tách thêm mà không có lý do mới).
- Bẫy mới: `_evidence` (parse `evidence` JSON của audit) là hàm module-level trong `orchestrator.py`, định
  nghĩa SAU dòng orchestrator.py nhập `orch.gates_flow` → cùng khuôn nhập lười đã gặp ở K1.5
  (`ReloadRequested`/`StepResult`); `orch/cli.py` và `orch/rehydrate.py` đã làm y hệt cho chính tên này, chỉ
  là lần này thêm hai điểm dùng nữa (`_on_gate_decide`, `_check_escalations`).
- Quyết định có chủ đích: `unhandled` (ghi trong `_after_error`) vẫn ghi trực tiếp `o.unhandled[subject] = rec`
  thay vì qua một hàm `mark_unhandled(ctx, ...)` dùng chung như đặc tả K1.6 đề nghị — hai điểm ghi còn lại
  (`_plan` reject kế hoạch, spec runtime) nằm trong `process()`/`ticket_fsm` (K1.7 chưa làm), gộp sớm vào một
  hàm mà thiếu hai điểm kia là nửa vời và dễ lệch. Để `mark_unhandled` cho K1.7 khi cả ba điểm ghi cùng lộ ra.
- Đo hai chiều: bỏ điều kiện `r.tools != "rw"` trong `_rework_after_error` (luôn coi mọi route là không sửa
  code) → `test_generic_error_in_engineering_triggers_rework` đỏ đúng chỗ (ticket treo `dispatched` thay vì đi
  `blocked`); khôi phục → xanh.
- Nghiệm thu: `ruff check src tests` sạch, `mypy src/company --ignore-missing-imports` sạch (42 file),
  `pytest -q` 908 pass/1 skip — XANH NGAY LẦN ĐẦU, không cần sửa test nào (khác K1.4/K1.5 đều có ít nhất một
  bẫy monkeypatch/import phải sửa).
- Dở: chỉ còn K1.7 (`ticket_fsm.py`/`release_fsm.py` + bảng chuyển trạng thái + `process()` rút về dispatcher
  thuần). `orchestrator.py` còn 957 dòng, đích cuối K1 là ≤ 300 — K1.7 phải mang phần lớn số dòng còn lại.
- Người sau KHÔNG được quên: K1.7 là PR cuối của K1 (theo Phụ lục A/B đặc tả) — sau đó nghiệm thu cuối K1 là
  `wc -l orchestrator.py ≤ 300` và test `test_kich_thuoc_module_orch` (≤ 400/module, xem đặc tả K1.7 — kiểm
  tra test này đã tồn tại hay phải viết mới). K2 sandbox: K2.1 (module `sandbox.py`) và K2.3 (studio) đã
  xong, nhưng **K2.2 (nối `tools.run`/`workspace._run`/`smoke.Popen` của company vào Sandbox) CHƯA LÀM** —
  đã kiểm lại bằng `grep sandbox src/company/{tools,workspace,smoke}.py`: không có kết quả nào. K3
  (`xagents-core`) và K4–K9 chưa bắt đầu.

## Phiên K1.7 — ticket_fsm.py + release_fsm.py (một phần, KHÔNG đủ như đặc tả)

- Xong: `orch/ticket_fsm.py` (`_superseded`, `_note_closed`, `_plan`, `_spec_runtime_missing`, `_threat_model`,
  `_check_plan`, `_dispatch_plan`, cùng `_cycle` — hàm thuần chỉ `_check_plan` dùng, dời theo cho gọn thay vì
  nhập lười một hàm không ai khác cần) và `orch/release_fsm.py` (`_integrate`, `_void`, `_release`, `_deliver`,
  `_rollback_delivery`, `redeploy`, `_check_paused_releases`, `_superseded_release`, `_release_paused`,
  `_recall`, `_rerun_release`) khỏi `orchestrator.py` (957 → 567 dòng).
- **LỆCH ĐẶC TẢ, GHI RÕ CHỨ KHÔNG GIẤU**: đây là di chuyển thuần (giống K1.1–K1.6, hành vi giữ nguyên 100%),
  **KHÔNG** dựng `TICKET_TRANSITIONS`/`RELEASE_TRANSITIONS` như bảng dữ liệu "driver" mà đặc tả K1.7 mô tả,
  **KHÔNG** viết `tests/test_orch_bang_chuyen.py` (test bảng tham số hoá + 3 cặp cấm) hay
  `tests/test_orch_khuon_loi.py` (5 test theo khuôn lỗi TRAPS.md), và **KHÔNG** rút `process()` về dispatcher
  thuần (nó vẫn còn nguyên trong `orchestrator.py`, dài, gọi thẳng `ticket_fsm`/`release_fsm` qua `o.<tên>`
  giống hệt cách gọi `gates_flow`/`worktree_flow` ở các PR trước — không có bảng chuyển trạng thái làm driver).
  Lý do: dựng bảng chuyển trạng thái đúng cách là một THIẾT KẾ, không phải cơ học — cần viết test bảng TRƯỚC
  (test-first, như chính đặc tả K1.7 đòi), đối chiếu từng dòng bảng với `lead.state`/`delivery.py` để không
  nhân đôi nguồn sự thật, và đo được từng cặp chuyển bị cấm. Làm vội trong cùng phiên với sáu PR cơ học khác
  là chính thứ ADR-0034 cảnh báo (rush → sai). `orchestrator.py` còn 567 dòng, CHƯA đạt đích ≤ 300 của K1
  (mảng còn thiếu chính là phần bảng chuyển trạng thái này, ước lượng gọn thêm ~200-250 dòng nếu `process()`
  được viết lại thành `tra ROUTES → step() → _call`).
- Bẫy mới: `StepResult` runtime-instantiate trong `redeploy()` — cùng khuôn nhập lười đã gặp ở K1.5
  (`orchestrator.py` nhập `orch.release_fsm` trước khi `class StepResult` được định nghĩa). `_cycle` và
  `spec_runtime_gap`/`SPEC_RUNTIME_REWORKS` là hợp đồng công khai của `company.orchestrator` (test/gate_brief.py
  import trực tiếp) — ruff tự xoá import "thừa" ở top-level sau khi hàm dùng chúng dời module, phải thêm lại
  bằng alias tường minh (`from .orch.X import Y as Y`) như các PR K1 trước đã làm cho `_evidence`/`ENGINEERING`.
- Đo hai chiều: bỏ nội dung `_void` (không thêm `rid` vào `void_releases`, không gọi `lead.void_release`) →
  `test_conflict_voids_release_and_ticket_redoes_on_fresh_base` đỏ đúng chỗ (`void_releases` rỗng thay vì có
  1 phần tử); khôi phục → xanh.
- Nghiệm thu: `ruff check src tests` sạch, `mypy src/company --ignore-missing-imports` sạch (44 file),
  `pytest -q` 908 pass/1 skip.
- Người sau KHÔNG được quên: nghiệm thu cuối K1 (`wc -l orchestrator.py ≤ 300`, test
  `test_kich_thuoc_module_orch`) CHƯA đạt — cần một phiên riêng, có thời gian, làm ĐÚNG phần bảng chuyển trạng
  thái (test-first) thay vì di chuyển cơ học thêm. Đây là việc còn lại DUY NHẤT để đóng K1 theo đúng đặc tả.
  K2 sandbox còn thiếu K2.2. K3 (`xagents-core`) và K4–K9 chưa bắt đầu.

## Phiên K1.7 phần 2 — bảng chuyển giao thật, hai bộ test theo đặc tả (K1.7 đóng)

- Xong: `orch/fsm.py` mới — `Transition` (frozen dataclass: `name`/`topics`/`action`/`guard`/`phase`) + `step()`
  (tra bảng theo đúng thứ tự khai báo, action trả `True` = process() dừng ngay). `ticket_fsm.TICKET_TRANSITIONS`
  (4 dòng: superseded, learn_repo, plan, clarification_fallback) và `release_fsm.RELEASE_TRANSITIONS` (5 dòng:
  integrate_rc, integrate_approved — `phase="pre"`; production_deploy_or_rollback, release_pending_human,
  acceptance_close — `phase="post"`). `process()` viết lại thành dispatcher thuần: `fsm.step(ticket table)` →
  `fsm.step(release table, phase="pre")` → vòng `ROUTES` → `fsm.step(release table, phase="post")` — ĐÚNG câu
  đặc tả "tra ROUTES → gọi ticket_fsm.step hoặc release_fsm.step → _call". `orchestrator.py`: 957 → 491 dòng
  (K1.7 phần 1 đưa xuống 567; dọn khoảng trắng thừa + orphan section header từ các PR trước thêm −57 dòng nữa).
  Test: `tests/test_orch_bang_chuyen.py` (9 ca: toàn vẹn bảng, cơ chế `step()`, ba cặp cấm — tasks cũ cho ticket
  đã vượt dispatched, release-candidates cho RC đã void, approved-specs trùng khi plan còn chờ gate) và
  `tests/test_orch_khuon_loi.py` (7 ca: 5 khuôn TRAPS.md §1 + `test_kich_thuoc_module_orch_duoi_400_dong`).
- **LỆCH ĐẶC TẢ có chủ đích, ghi rõ**: `Transition` KHÔNG có trường `dst` (ticket state) như đề xuất gốc — bảng
  định tuyến theo TOPIC của event (như `process()` cũ làm), không theo `lead.state`; nguồn sự thật trạng thái
  vẫn ở `delivery.py` (đúng tinh thần ADR-0034 "driver, không nhân đôi"). Không đụng `delivery.py` — các hàm
  `_on_pr`/`rework`/`human_hint`/... vẫn là nơi quyết định state thật, spec K1.7 mô tả một máy trạng thái tổng
  quát hơn thực tế trong code hiện tại (không có "báo cáo bản đồ" §4 nào tồn tại trong repo để đối chiếu 13/12
  dòng đúng như đặc tả nêu — có thể là tài liệu làm việc của phiên viết đặc tả, không được commit).
- **Phát hiện + sửa một bug thật khi viết test khuôn 3**: `once=f"no-test-author:{tid}"` (routes.py,
  `_can_author_tests`) không mang thế hệ — CHÍNH LÀ bẫy mà đặc tả K1.2 đã ghi rõ phải sửa ở K1.4 nhưng chưa ai
  làm ("thêm thế hệ tại đây (K1.4): f"no-test-author:{tid}:{o.lead.retries.get(tid, 0)}""). Sửa thành
  `f"no-test-author:{tid}:{retry}"` (đọc `o.lead.tickets[tid].retry`, `lead.retries` không tồn tại như đặc tả
  viết — dùng đúng trường thật). Đo hai chiều: bỏ `:{retry}` → `test_khuon3_no_test_author_ghi_lai_o_moi_lan_rework`
  đỏ (rework 2 lần chỉ 1 audit); khôi phục → 2 audit đúng.
- Ba khoá once khác cũng chỉ 1 thành phần (`delivery.skipped:{rid}`, `smoke.unverified:{rid}`, `no_draft:{e.event_id}`)
  — xét từng cái: `no_draft` đã mang thế hệ THẬT qua `event_id` (mỗi event mới một khoá khác, an toàn); hai cái
  release đưa vào `KHOA_MIEN` có lý do (audit phụ, bằng chứng thật nằm trong `release.smoke`/`release-events`
  mang riêng từng lượt) — KHÔNG tự ý đổi hành vi đường ống giao hàng khi chưa có ai xác nhận cần.
- Nghiệm thu cuối K1 đạt đủ: `orchestrator.py` 491 dòng (< 300 KHÔNG đạt — xem dưới), mọi module `orch/` ≤ 312
  dòng (< 400 đạt, có test chốt). `ruff`/`mypy` sạch (45 file); `pytest -q` 924 pass/1 skip; coverage 100% (trừ
  2 dòng POSIX `sqlite_bus.py` trên Windows, đã biết).
- **CHƯA đạt đích ≤ 300 dòng của K1** (còn 491) — phần còn lại là `_call` (~150 dòng, dispatcher gọi agent xử
  lý lỗi transport/runner) và `__init__`/`status`/`rulings`/`_deadlock_warnings` (~140 dòng gộp). KHÔNG di
  chuyển hai khối này: không nằm trong danh sách hàm K1.1–K1.7 của bất kỳ PR nào đã duyệt — di chuyển thêm là
  việc MỚI ngoài đặc tả, cần đặc tả riêng (tạm gọi K1.8) nếu người chủ muốn đẩy tiếp xuống ≤ 300.
- Người sau KHÔNG được quên: K1 giờ coi như ĐÓNG theo nghĩa "mọi hàm đặc tả liệt kê đã tách, có bảng chuyển
  giao thật, có 2 bộ test đặc tả yêu cầu" — chỉ còn thiếu số 300 dòng chưa chạm vì nó đòi tách `_call`, một
  việc rủi ro cao hơn (trung tâm mọi lời gọi agent) và ngoài phạm vi mọi PR K1.x đã duyệt. K2.2, K3–K9 vẫn y
  nguyên như phiên trước.
