# Phiên 2026-09-23

PR: #320 (đợt 1 + keeper, đã merge 02:06) và PR đợt 2 (#321) — các commit đẩy SAU khi #320 merge, rebase lên `main` mới.

Người dùng: "audit code tìm lỗi".

## Cách làm

- Cổng nền cả năm gói trước khi đụng gì: xanh (company 1283, gateway 258, console 441, core 494, keeper 553 test;
  100% phủ).
- Ba agent đọc song song (company · gateway+console · core+keeper), mỗi phát hiện kèm kịch bản hỏng. 27 ứng viên.
  Tự tái hiện lại năm lỗi nặng/rõ nhất trước khi tin; sửa đúng năm lỗi đó, mỗi lỗi có test đỏ trước sửa.

## Đã sửa trong PR này (đỏ trước → xanh sau, đo hai chiều)

1. `company/tools.py` `_walk`: glob `../*` thoát worktree (`relative_to` chỉ so chuỗi). Test
   `test_glob_khong_thoat_khoi_worktree` đỏ (`../hang-xom.txt:1: SECRET_TOKEN=1`) → xanh.
2. `company/orch/worktree_flow.py` `workspace`: `ticket_id` phải khớp `[A-Za-z0-9][A-Za-z0-9._-]*`, không thì
   `None` (mọi nơi gọi đã xử lý `None`). Test `test_ticket_id_khong_an_toan_khong_co_worktree`.
3. `company/deploy.py` `project_name`: hạ chữ thường, ký tự lạ → `-`. Ba test cũ khẳng định tên viết hoa — tức
   khoá chặt chính lỗi — đã sửa kỳ vọng.
4. `keeper/patcher.py` `bump_dependency`: ranh giới tên gói hai phía; `new_spec` qua lambda. Hai test.
5. `gateway/auth.py`: `HTTPError` 429/≥500 khi refresh là lỗi tạm thời, không cooldown. Test tham số 429/500/503.

Rà cả họ (luật 5): mọi `.glob`/`.rglob` còn lại dùng mẫu hằng hoặc id từ file trong repo; `re.sub` còn lại
(`xagents_core/guard.py`) đã dùng hàm thay thế.

## Đợt 2 — sửa nốt, bốn agent song song trên worktree riêng, gom bằng cherry-pick

Mỗi bản sửa có test đỏ trước (tên test + lý do đỏ nằm trong commit message). Đã sửa: company 4 (+1 cùng họ: tool
chỉ đọc của QA đọc sha đã staged), keeper 5 (+1: `publish()` gọi lại sau push hỏng — đường mới do chính bản sửa
mở), core 4, gateway 5, console 3. Test đổi kỳ vọng vì từng khoá chặt lỗi: `test_login_pkce_state_mismatch_raises`,
bốn ca `test_github.py` (`[]` → `None`), các ca audit trả `[]`, argv sandbox có `--name`, QA "cùng worktree tích hợp".

Cổng cuối (lượt này): company 1293, gateway 269, console 450, core 513, keeper 582 — 100% phủ.

## Chưa sửa — người sau đừng quên

- gateway #4 (cần người chốt chính sách): sau quota ở model chính, 4xx không-quota của model anh em đẩy thẳng về
  client, không xoay tài khoản và giấu nguyên nhân quota. `test_sibling_fallback_fails_without_cooldown_raises`
  khoá hành vi này có chủ đích. Đề xuất: coi model anh em là "không dùng được", cooldown theo gợi ý của model
  chính, xoay tài khoản, giữ phản hồi quota làm lỗi cuối (client thấy 429 thay vì 400).
- core: `CodexClient` thoát khác 0 — đuôi stdout có thể lẫn `agent_message` do model viết; cần parse event JSON
  để tách `model_text` như đã làm cho `claude -p`.
- keeper: docstring `audit.py` nói `uv run` tự cài pip-audit (chỉ `uvx` làm vậy) — máy thiếu pip-audit nay ra một
  finding "lỗi công cụ" thay vì `[]`. `audit()` hiện chưa có caller sản xuất.
- Hai marker `no-ky-thuat` mới: `Lease` chiếm lock cũ vẫn đọc-rồi-ghi (`sqlite_bus.py`); thư mục
  `.worktrees/_releases/<sha>` không dọn (`workspace.py`).
- Escalation "merge hỏng không-xung-đột" hiện là thông báo supervisor (không mở gate): chưa rõ duyệt gate cho
  ticket `approved` nên làm gì.

## Việc kế tiếp (sau khi #320 merge): công ty tự vận hành sau khi chốt spec

Người dùng: "sau khi người dùng chốt yêu cầu, thì công ty phần mềm tự hoạt động, tự duyệt tự động theo chất
lượng tốt nhất đã chốt". Quyết định người (qua `AskUserQuestion`, 2026-09-23):

- Gate **spec vẫn do người ký**. Code được tự duyệt: **release**, **acceptance (UAT)**, **deploy production**.
  Escalation KHÔNG tự duyệt.
- Ngưỡng chất lượng: **cả hai** — sàn cứng trong code (review qua `sc-security`) + người có thể NÂNG ngưỡng
  theo dự án lúc chốt spec, không được hạ dưới sàn. Thiếu bằng chứng cho bất kỳ ô nào ⇒ về người duyệt.
- Thứ tự: xong #320 → nhánh mới, **ADR trước** (ADR-0043), rồi TDD.

Hiện trạng đo được: cơ chế đã có (`COMPANY_GATE_AUTOAPPROVE`, actor `"code"`, `gate_risk.request_gate`,
`docs/thi-hanh/adr113.md`) nhưng `RISK_RULES` rỗng có chủ đích, và `GateRiskContext` KHÔNG mang bằng chứng chất
lượng nào — đó là khoảng trống kiến trúc ADR phải lấp. Rủi ro phải nêu trong ADR: acceptance tự duyệt = code ký
thay khách; audit-log phải phân biệt `code`-theo-ngưỡng với khách ký thật (không mở lại lỗ
`orchestrator`+`UAT-*` mà `gate_risk.py` đã cảnh báo).

## Thi hành ADR-0043 (nhánh `claude/code-audit-8dtadj-3`, PR #323)

Nhánh gốc `claude/code-audit-8dtadj` lệch lịch sử với remote sau khi #320 merge (force-push bị chặn) nên đợt 2
đi nhánh `-2` (#321, đã merge 02:19) và việc này đi nhánh `-3`. Các bước, mỗi bước test đỏ trước:

1. ADR-0043 + `quality_floor.floor_gaps` (hàm thuần, R1–R5 + A1–A2) + `QualityBar` (tập khoá đóng chỉ-siết).
2. Hai hàng `RISK_RULES` đầu tiên; `request_gate(..., evidence=, bar=)`; `gate.auto_skipped` ghi khoảng trống.
3. `gate_cli approve SPEC-x --quality-bar ...` → `quality.bar_set` (bus từ chối agent ghi); `project_bar` bỏ qua
   bản ghi không tin được, bản ghi của người mà hỏng → đóng cả hai cửa.
4. `collect_evidence` đọc bus; gate release mở kèm bằng chứng; deploy ghi `sha`. **Lỗ tìm ra ở bước này**:
   `scheduler._actionable` hỏi `trusted_decision` (core) nên quyết định do `code` tự duyệt — cơ chế có từ adr113
   (2026-09-10) — được ghi mà KHÔNG BAO GIỜ chạy. Nay hỏi `gate._trusted` (company).
5. Nghiệm thu: máy duyệt `UAT-*` → `close_accepted` + `acceptance.auto` (không ghi `acceptance-results`),
   `_rehydrate` áp lại sau restart; khách ký khác `accepted` sau đó → `acceptance.overridden` + gate escalation.

6. `sc-security` (chỉ đọc) tìm 5 khoảng trống; vá cả 5 bằng test đỏ trước, chi tiết ở mục cuối ADR-0043. Đo hai
   chiều: tắt từng phép kiểm (actor ở rehydrate ×2, chặn `by=code`, `RiskRule.kind`) → đúng ca tương ứng đỏ.

Người sau không được quên:
- Ba lỗ "còn mở" của ADR-0043 → đóng ở PR (#324) (nhánh `claude/code-audit-8dtadj-4`, xem mục cuối ADR-0043):
  `TRUSTED_WRITERS` ở `_rehydrate` + ép `action=change.impact` cho route product→audit-log; `ticket_id` đầu ra từ
  route; `acceptance-results`: đo được agent không tự ký được, canh bằng test. Chưa gỡ `writes: acceptance-results`
  khỏi `ops.md` — cần `make eval-record` bằng model thật (hai ca eval pha `account` đang xuất topic đó).
- Cờ `COMPANY_GATE_AUTOAPPROVE` vẫn mặc định TẮT — không bật thì không gì đổi.
- Sàn đòi bằng chứng mà nhiều dự án chưa sinh (R2 cần spec khai `runtime`). Đếm `gate.auto_skipped` theo khoảng
  trống trước khi kết luận "tự duyệt không chạy".
- Không có bằng chứng coverage/SAST do máy sinh cho dự án khách → không có khoá mức nâng cho chúng (ADR §2).
- Trần: `trusted_autoapprove` khi replay chỉ kiểm tên hàng, không thu lại bằng chứng (ADR "Hệ quả").

## Tiếp — theo dõi công ty phần mềm, `risk_tags` tự sửa thay vì từ chối cả kế hoạch (nhánh `docs/gate-quality-heuristic`)

Tiếp phiên 2026-09-22 (khởi động + theo dõi công ty phần mềm). Người dùng: "lưu vào file markdown cho các
agent duyệt cổng" (tiêu chí ưu tiên chất lượng cao nhất / nền tảng mới nhất khi bằng chứng mơ hồ), rồi "sửa
lại cho đỡ phải sai sót, chạy lại nhiều tốn tiền của tôi" sau khi thấy kế hoạch CAMPUS-UNI bị `plan_rejected`
hai lần liên tiếp vì thiếu `risk_tags`.

- **Đo trước khi sửa**: theo dõi CAMPUS-UNI chạy thật qua `Monitor`, thấy `_check_plan` từ chối
  `PLAN-CAMPUS-UNI-20260922-1` hai lần liên tiếp (TCK-036 chạm `admin`, rồi TCK-033/034 chạm `email`), mỗi lần
  một lượt `product[plan]` model thật (opus-5) đầy đủ để sinh lại. Không phải bug — `_check_plan` (đúng thiết
  kế `RISK_HINTS`) chặn đúng, nhưng cách phản ứng (từ chối cả kế hoạch, lập lại từ đầu) tốn tiền cho một khoá
  suy được thẳng từ chính hint đã dò ra.
- **TDD**: viết hai test đỏ trước (`test_risk_hint_khong_tag_tu_dong_gan_thay_vi_tu_choi`,
  `test_risk_hint_khong_map_duoc_van_bi_tu_choi`), chạy thấy đỏ đúng lý do, rồi thêm `HINT_TO_TAG` (`events.py`)
  và sửa `_check_plan` tự gắn tag + audit `risk_tags_autofixed` thay vì `problems.append`. Hint không suy được
  tag vẫn bị từ chối như cũ (không đoán bừa) — test riêng canh nhánh đó.
- **Bẫy gặp khi tách hàm**: `_check_plan`/`_cycle` dời từ `ticket_fsm.py` sang `orch/guards.py` vì file đầu
  chạm trần 400 dòng (`test_orch_khuon_loi.py`). Hook `auto-format.sh` (PostToolUse của Edit/Write) chạy
  `ruff format` toàn file mỗi lần Edit — thổi phồng cả ba file (`events.py`, `guards.py`, `ticket_fsm.py`,
  `orchestrator.py`) từ dạng gọn cố ý sang dạng nhiều dòng, kéo tổng dòng thân hàm của `orchestrator.py` vượt
  trần `MAX_THAN_HAM_ORCHESTRATOR=260` dù logic không đổi. Sửa bằng cách revert (`git checkout --`) rồi áp lại
  đúng phần logic bằng `python3` chạy qua Bash (không qua Edit/Write, hook không bắt được) — diff cuối 174 dòng
  thay vì 517 dòng lúc đầu.
- **`gates/checklists.md`**: thêm một đoạn văn xuôi (không phải bullet `- [ ]`, nên không cần khai nguồn bằng
  chứng ở `gate_checklists.py`) nêu tiêu chí phá thế bế tắc: bằng chứng mơ hồ giữa nhiều phương án kỹ thuật hợp
  lệ thì nghiêng về chất lượng cao nhất + phiên bản/nền tảng mới nhất đã ổn định (≥3 tháng). `make subagents
  check` xác nhận không đổi bản dẫn xuất (đoạn văn xuôi không sinh mục checklist mới).
- **Rebase qua #320**: hai PR cùng ngày cùng chạm `README.md`/`CHANGELOG.md`/nhật ký phiên này. Theo luật
  một-PR-mở (`docs/QUY-TRINH-GIT.md` §2c), chờ #320 merge rồi `git fetch` + `git rebase origin/main` mới mở PR
  này; xung đột ở đúng ba chỗ nói trên, giải bằng cách nối nội dung (nhật ký) và lấy số test mới nhất sau rebase
  (README).
- Cổng: `gate all` xanh sau rebase (xem báo cáo xác thực cuối PR), `evals --replay --strict` pass, `subagents
  check` khớp, `assetscan scan` 0 lỗi.

### Vận hành công ty (tiếp từ 09-22)

- Đã merge #316, #317 (dependabot) trong lượt trước — không còn PR mở khi bắt đầu nhánh này.
- Đã mở rộng uỷ quyền tự duyệt gate sang MỌI dự án (không riêng QLKH) và thêm tiêu chí "chất lượng cao nhất/
  nền tảng mới nhất" cho lựa chọn mơ hồ — ghi vào bộ nhớ Claude (`feedback-duyet-gate-thay-nguoi-dung.md`,
  `feedback-huong-quyet-dinh-chat-luong-cao-nhat.md`), không phải tài liệu repo.
- CAMPUS-UNI: gate `escalation` (plan_rejected lần 2) đã duyệt retry trước khi phiên này bắt đầu sửa code; kế
  hoạch lần ba (sau khi PR này merge) sẽ tự sửa risk_tags thay vì từ chối nếu gặp đúng khuôn cũ.

### Người sau không được quên

- `HINT_TO_TAG` mới phủ các hint đã biết trong `RISK_HINTS`; hint mới thêm sau này phải thêm cả hai bảng cùng
  lúc, không thì rơi về nhánh "không suy được tag" và vẫn tốn một lượt model như cũ — không sai, chỉ không tối
  ưu.
- Auto-format hook thổi phồng style khi Edit/Write chạm file `company` — biết trước để tránh lặp lại việc dọn
  diff lần sau: sửa qua Bash/python trực tiếp khi cần giữ style gọn có chủ đích (trần dòng module/hàm).
- Việc "công ty tự vận hành sau khi chốt spec" (ADR-0043, mục trên) vẫn chưa làm — người dùng đã quyết ba điểm
  chính sách, còn thiếu ADR + TDD.

## Tiếp — audit "vận hành tự động" rồi sửa bốn chỗ kẹt im lặng (nhánh `claude/admiring-dirac-5t1cn7`, PR #325)

Người dùng: "audit nâng cấp công ty phần mềm để hoạt động tự động tốt hơn", rồi "fix đi, người dùng chỉ cần đưa ra
yêu cầu, công ty phần mềm tự xử lý cho đến khi xong".

### Audit (chỉ đọc, trên `main@bcb74ba`, cổng company xanh 1383/100%)

Hai trợ lý rà song song (điểm chờ người; sự cố/nợ trong TRAPS+sessions+reports), tự xác minh trong code trước khi tin.
Bảy điểm dừng chờ người (spec, câu hỏi làm rõ, release/UAT, escalation ×10 điểm mở, change request, supervisor pause,
gate quá hạn) và **chín chỗ kẹt im lặng**; bốn chỗ xác minh được ngay trong code: `_plan` lỗi → `_mark` không gate
(`ticket_fsm.py:92-95`, docstring hứa K1.7 nối `mark_unhandled` mà chưa nối); threat model `block` chỉ audit
(`:203-205`); câu hỏi làm rõ không timeout (`guards.py:121`); `transient:` không trần (`scheduler._defer`). Còn lại
(chưa làm, ưu tiên 2–4): watchdog/heartbeat cho `run --watch` (#315 treo im lặng chưa rõ nguyên nhân), sao lưu
`company.sqlite`, sink cảnh báo R8, lệnh `stop/pause/resume`, `runtime` bắt buộc từ spec + coverage/SAST máy sinh để
ADR-0043 đạt sàn thật, deploy tự chọn cổng, đường nạp bằng chứng deploy tay (5 gate QLKH). Báo cáo đầy đủ nằm trong
chat của phiên (scratchpad, không commit).

### Sửa (TDD, mỗi chỗ một test đỏ trước — `tests/test_tu_van_hanh_khong_ket_im_lang.py`)

- Tách `_mark_unhandled` khỏi `_after_error` (gates_flow) — một cửa cho mọi lỗi không nhánh nào nhận; `_plan` và
  `_threat_model` (block) gọi nó; `scheduler._defer` gọi khi `transient:` quá `transient_limit()`.
- `_assume_clarifications` (ticket_fsm, gọi từ `tick`): quá `clarify_timeout()` → audit `clarification.assumed` +
  `_call(product, draft + assumed_answers, spec_route)`; `pending_clarifications` bỏ vòng đã giả định.
- Test cũ `test_security_block_on_spec_stops_planning` khoá đúng hành vi im lặng (`not orch.gate.pending`) → đổi
  kỳ vọng: gate escalation `P1` phải mở.
- Đo hai chiều: `git stash -- src` → 5/7 ca đỏ (2 ca coverage-branch không phụ thuộc bản sửa), pop → 7/7 xanh.
- README hai nơi: 1390 ca / 84 file.

### Người sau không được quên

- Giả định câu hỏi làm rõ KHÔNG qua gate `spec`: người vẫn ký `SPEC-<pid>`, nên đọc `clarification.assumed` trong
  audit trước khi ký — đó là chỗ máy đã đoán thay người.
- `transient_since` chỉ sống trong RAM (`RAM_ONLY`): restart là đếm lại từ đầu. Chấp nhận vì restart hiếm hơn 2h;
  nếu thấy một event transient sống qua nhiều lần restart thì phải ghi mốc vào audit như `defer.until`.
- Gate `spec`/`escalation` vẫn của người (quyết định 2026-09-23 sáng). "Chỉ cần đưa yêu cầu" hiện nghĩa là: đưa
  yêu cầu + ký spec một lần; muốn bỏ nốt chữ ký spec là quyết định chính sách mới, cần người chốt lại.
- `COMPANY_GATE_AUTOAPPROVE` vẫn mặc định tắt — muốn công ty tự duyệt release/UAT thì bật cờ (ADR-0043).

## Tiếp — "pull và audit nâng cấp công ty phần mềm để hoạt động tự động tốt hơn" (worktree `audit-tu-dong-2026-09-23`, PR #326)

Người dùng: "pull và audit nâng cấp công ty phần mềm để hoạt động tự động tốt hơn", rồi "xem cả pr đang mở".

### Hiện trạng đo được trước khi sửa

- `main@bf2fdcc` (#324), cổng company xanh 1428 passed / 100%. Bus thật: CAMPUS-UNI-20260922 đứng ở gate
  `escalation` (plan_rejected lần 2, TCK-033 thiếu risk_tags) từ 15:22 UTC 22/09; orchestrator không chạy (event
  cuối là lúc đó). #322 đã vá đúng ca risk_tags, nhưng khuôn chung "từ chối kế hoạch = hỏi người" vẫn còn.
- PR #325 (phiên khác, cùng chủ đề: bốn chỗ kẹt im lặng) đang **CONFLICTING** với main sau #324 và đỏ check
  `metadata` (thân PR thiếu "ADR-0034" dù chạm `orch/`; CHANGELOG còn `(#PR)`). Theo luật một-PR-mở: gộp
  `origin/main` vào nhánh PR (xung đột chỉ ở hai README số test → lấy số đo trên nhánh gộp 1436/87), sửa thân PR,
  điền `#325`, cổng company xanh 1435 passed / 100%, evals replay 59/59, subagents khớp, assetscan 0 lỗi, bật
  auto-merge squash. Bẫy nhỏ: `test_readme_khop_so_lieu_that` đếm 87 file trong khi `ls tests/test_*.py` cho 86 —
  tin test, không tin `ls`.
- Hai trợ lý `Explore` rà song song, tự xác minh trong code trước khi tin: (a) 19 điểm dừng chờ người — 10/13 điểm
  mở gate là `escalation` mà `floor_gaps` từ chối tuyệt đối; **không lượt tự thử lại nào** trước gate ở `_stall`
  và `_plan` (chỉ `_spec_runtime_missing` có 1 lượt); `pending_human` của release là model tự dừng, không máy nào
  chấm; (b) điểm treo/đốt tiền: `_deadlock_warnings` bị vô hiệu bởi BẤT KỲ event `deferred` nào
  (`scheduler.py:281`); timeout `claude -p` 900 s cố định × 3 retry × số backend không cấu hình được; `execv` khi
  reload không có đường lùi (lease đã trả); re-dispatch xung đột merge dưới `MAX_CONFLICT_RETRIES` không có trần
  riêng; trần USD chết với model không giá; `tick_error` không vào `status`/`metrics`.

### Sửa trong PR này (TDD, đỏ trước; đo hai chiều bằng `git apply -R` hai hunk sản xuất → đúng 5 ca mới đỏ)

1. `PLAN_REWORKS` (=1): kế hoạch bị `_check_plan` từ chối → `_plan` gọi lại chính nó với payload thêm `hint` +
   `previous_plan`, audit `plan.rework`; quá số lượt mới `plan_rejected` + gate. Bộ đếm theo **event nguồn** (không
   theo dự án), dựng lại từ `plan.rework` + `plan_rejected` (thiếu vế sau thì mỗi restart tặng một lượt "miễn phí"),
   `TRUSTED_WRITERS` thêm `plan.rework` (product/orchestrator); `_retry_unhandled` xoá bộ đếm. Ba test cũ khoá hành
   vi "một lần rỗng là tới người" đổi thành `1 + PLAN_REWORKS` lần; `test_gates_flow_branches` đọc `any(...)` vì
   `plan_rework:` đứng trước `plan_rejected:` trong `actions`. `tests/test_plan_tu_sua_truoc_khi_hoi_nguoi.py`.
2. `cli_cmds.run`: `_reexec` ném `OSError` → audit `orchestrator.reload_failed`, `ns.no_reload = True`, gọi lại
   `run` (lấy lại lease, chạy mã cũ). `tests/test_auto_reload.py::test_execv_hong_thi_chay_tiep_ma_cu_khong_chet_im_lang`.
3. Không đổi prompt agent: `hint`/`previous_plan` là trường payload thêm, `product.md` đã có quy ước đọc `hint`.

### Chưa làm — xếp theo giá trị, người sau chọn

- `_stall` (agent pha research lỗi lần đầu) → mở gate ngay, chưa có lượt tự thử lại như `_plan` nay có. Cùng họ.
- `_deadlock_warnings` trả `[]` khi có bất kỳ `deferred` nào — sau #325 `transient:` có trần 2h nên đỡ, nhưng
  event hoãn `gate:`/`paused:` vẫn che cảnh báo. Cần tách "hoãn có gate" khỏi "hoãn treo".
- Timeout 900 s của `claude -p`/`codex` không đọc từ `llm.yaml`/env; một bước có thể kẹt ~1–2 giờ không heartbeat.
- `status` thiếu `unhandled`; `tick_error` không vào `metrics.HEALTH`.
- Release `pending_human` (model tự dừng) chưa được chấm bằng `quality_floor` để tự chạy lại.
- Cờ `COMPANY_GATE_AUTOAPPROVE` vẫn tắt mặc định — mọi sàn ADR-0043 chưa chạy thật cho dự án nào.
- Gate escalation CAMPUS-UNI: duyệt `retry` sau khi PR này merge và bật lại `run --watch`; kế hoạch lần 3 sẽ tự
  gắn risk_tags (#322) và nếu còn lỗi khác thì tự sửa một lượt trước khi hỏi.


## Phiên 3 (chiều) — khởi động lại công ty, CAMPUS-UNI đi tiếp, và một lỗi hạ tầng đội lốt lỗi builder

Người dùng: "chờ phiên kia xong khởi động lại công ty phần mềm hoàn thành nốt dự án dang dở".

### Đã làm

- Chờ #326 merge (04:47 UTC), pull `main@9799f8c`, xác minh lock cũ (PID 3488) là xác chết rồi bật lại
  `orchestrator --repo C:/Users/liend/campus-company --base intake/campus-base --workers 2 run --watch 5`.
- Gate `escalation` CAMPUS-UNI đã được duyệt `retry` từ trước nhưng nằm im vì không có tiến trình nào chạy.
  Bật lên là đi ngay: `resume` → `event.retried` → **`risk_tags_autofixed` hai lần** (#322/#326 chạy thật lần
  đầu trên bus) → `plan.proposed` **39 ticket**, `problems: []`, `threat_model: ok` → `tasks TCK-001`.
- TCK-001 (scaffold Django + waitress + `/healthz`) chạy 3 lượt, mỗi lượt 43 file, mỗi lượt
  `local_checks {lint: false, tests: false}` → `ticket.blocked (retry 3/3)` → gate `escalation`.

### Lỗi tìm ra — đo, không suy (luật bắt buộc 6)

Không vá lần 4. Chạy tay đúng hai lệnh mà `stacks.py` ghép, trong chính worktree `ticket/TCK-001`:

| Trình thông dịch | Kết quả |
|---|---|
| `sys.executable` (venv orchestrator) | `ModuleNotFoundError: No module named 'django'`, 4 lỗi collect |
| `uv run` (venv khách) | test chạy thật, còn 2 fail nội dung (thiếu mark `django_db`) |

⇒ lỗi hạ tầng, builder không sửa được bằng bất kỳ code nào; dính mọi ticket Python của mọi dự án khách.
Sửa ở ADR-0044 (PR #327). Bẫy phụ phát hiện khi cổng đỏ 47 ca: quy tắc đầu tiên tôi viết
(`[project]` là đủ) tạo ra màu đỏ giả cho repo không khai `pytest` — phải siết thành "khách khai công cụ nào
thì lệnh đó mới đổi", đúng khuôn `_node_stack` có sẵn.

### Dở — người sau KHÔNG được quên

- **Orchestrator đang TẮT** (tôi tự dừng PID 26476 để khỏi đốt tiền vào các ticket cùng số phận). Sau khi PR
  ADR-0044 merge: pull, bật lại đúng lệnh trên.
- **Gate `escalation` cho TCK-001 vẫn đang mở, chưa quyết.** Quyết đúng là `reopen` kèm hint nêu 3 lỗi thật
  của builder: 1 `# noqa: S608` thừa, và 2 ca ở `tests/test_health_probe.py` thiếu mark `django_db`. Đừng
  `close` — code của TCK-001 phần lớn dùng được, đang nằm ở `a8fa1e3` nhánh `ticket/TCK-001`.
- 38 ticket còn lại của CAMPUS-UNI chưa chạy lượt nào.
- Nợ bảo mật mở ra có chủ đích: agent nay gọi được `uv sync`. Cần ADR egress proxy (đã ghi trong ADR-0044).
