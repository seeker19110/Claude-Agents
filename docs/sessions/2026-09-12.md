# 2026-09-12

## Hạng mục 3/5: phiên audit toàn dự án đầu tiên chạy đủ A1–A8 (#272)

- Chạy đúng khuôn `docs/TASK-PACK.md` (mở từ nghi thức tự kiểm ở #264, lần đầu chạy đủ 8 phép đo).
- A1+A2: README gốc/con lệch đĩa — software-company 1100→1152 test, console ADR 0001–0003→0001–0004, gateway
  216→251 ca. Phát hiện `companies/software-company/README.md` **tự mâu thuẫn** hai dòng khác nhau (1151 vs 1097
  ca) trong cùng file — cả hai đều sai. Đóng dòng treo từ 2026-09-07: mở rộng `test_readme_goc.py` canh thêm dãy
  ADR console+gateway (trước chỉ canh software-company, console lệch 5 ngày không gì đỏ).
- A3: dọn **38 chỗ** hoá thạch `Studio-creators` (35 ở `HUONG-DAN-VAN-HANH.md` gồm cả §3.3 34 dòng + §6 85 dòng
  mô tả quy trình một công ty đã tách repo ở #259 hai tuần trước; 3 ở `DIEU-PHOI-MODEL.md`). Xoá 2 mục lớn,
  đánh lại số §7–§12 → §6–§11 kèm 2 tham chiếu chéo trong chính tài liệu. Nhận ra: grep chuẩn của A3 gốc
  (đếm "package") **không bắt được** loại hoá thạch này vì nó nhắc tên riêng công ty, không nhắc số — ghi đề
  xuất mở rộng phạm vi A3 vào báo cáo.
- A4–A8: đo, không sửa (đúng luật khuôn — phiên đo không phải phiên sửa). A4 đọc 2/11 ADR gốc (khớp mã thật),
  9 ADR gốc + 39 ADR software-company còn lại vào sổ treo — phép này chậm, không tự động hoá được. A5 đếm lại
  4 lối thoát khỏi `fail_under=100`, khớp trần #271 (đúng kỳ vọng, cổng mới 2 giờ tuổi). A6/A7 xác nhận số đã
  ghi trong sổ treo từ 2026-09-10, không đổi gì thêm.
- Viết `docs/reports/2026-09-12-audit.md` theo đúng khuôn báo cáo cũ (bảng claim·đo được·chênh·lệnh), cập nhật
  sổ "Việc để lại đang treo": đóng 2 dòng, mở 1 dòng (A4).
- Còn lại: hạng mục 4 (**bớt** luật trùng lặp — regex PR ở 4 nơi, 7 bước ở 5 nơi, "mỗi PR một gói" vi phạm 100%,
  `AGENTS.md:74` trỏ sai §TRAPS) · 5 (chốt quy trình mới + ADR-0012).

## Hạng mục 2/5: hồi sinh cổng chết, biến luật thủ công thành cổng cứng (#271)

- Chủ dự án chốt: **cổng cứng** (đỏ CI), không phải cảnh báo.
- Hai cổng đã chết im lặng từ cải tổ #262, không gì đỏ suốt từ đó: `.pre-commit-config.yaml` (`files:` khớp
  `^software-company/` không bao giờ khớp nữa, `--project software-company` trỏ package không tồn tại) ⇒ hook
  `subagents-check` **không bao giờ chạy**; `.github/CODEOWNERS` 7/9 pattern chết ⇒ luật sở hữu rút về dòng `*`.
  Bài học: cổng chết im lặng nguy hiểm hơn không có cổng, vì người ta vẫn tin nó canh.
- Không chỉ vá đường dẫn mà dựng `platform/console/tests/test_cong_repo.py` (17 ca) chặn tái phát — 5 cổng cứng:
  đường dẫn cấu hình cấp gốc phải có thật · mọi package có golden phải trong matrix `golden-check` (bắt được
  `companies/keeper` đứng ngoài) · `quality.needs` phủ mọi job · **trần** cho `pragma`/`skip`/`omit` · câu khai
  của `AGENTS.md:7` phải khớp đĩa.
- **Số nền lối thoát khỏi `fail_under=100`**: pragma 21 · skip/xfail 6 · omit 2. Sổ bắt khớp CHÍNH XÁC hai
  chiều: thêm phải sửa số (đi qua review), bớt cũng phải sửa — sổ chỉ có giá trị khi nó đúng.
- Bẫy đo: `grep 'pragma: no cover'` ra 7 cho `xagents-core`, số đúng là **6** — `llm.py:509` là văn xuôi nhắc
  tới `` `pragma: no cover` `` trong backtick, không phải directive. Cổng dùng regex phân biệt được, grep thì
  không. Nếu chốt sổ bằng grep thì cổng sai ngay từ ngày đầu.
- Quyết định có chủ ý: `xagents-core` và `keeper` thiếu cả bốn file khung → **sửa câu khai** ở `AGENTS.md`/
  `README.md`, KHÔNG đẻ 8 file stub để qua cổng (đó đúng là khuôn "tài liệu phình" đang phải chữa). Việc viết
  bốn file có nội dung thật vào sổ "Việc để lại đang treo".
- Còn lại: hạng mục 3 (phiên audit A1–A8 — `HUONG-DAN-VAN-HANH.md` còn 35 chỗ Studio-creators) · 4 (**bớt** luật
  trùng lặp) · 5 (chốt quy trình mới + ADR-0012).

## Rà soát toàn bộ quy trình repo — hạng mục 1/5: động cơ giao hàng (#270)

- **Cách rà**: 5 subagent chỉ đọc chạy song song, mỗi mũi một cụm quy trình (git/PR/CI · thi hành/giao việc ·
  chất lượng/TDD · vận hành công ty · tài liệu/ADR), phiên chính tự đo số liệu thực tế 60 PR gần nhất rồi tổng
  hợp. Báo cáo subagent là **lời khai** (luật cấm 8) — mọi phát hiện dùng tới đều được phiên chính kiểm lại
  trực tiếp trước khi tin. Việc kiểm lại đã lật một nửa chẩn đoán, xem dưới.
- **Số đo thực tế 60 PR gần nhất** (2026-09-09 → 09-12): PR trung vị 6 file / 151 dòng / 2 commit, merge sau
  **7,5 phút** (51/60 dưới 15 phút); 22% là `fix`; **luật 2c "chỉ một PR mở" bị vi phạm 8/60 lần**, cặp
  #225/#226 chồng nhau 65 phút.
- **Luật bị vi phạm có hệ thống** (đo, không đoán): "mỗi PR một gói" (`KHUON-THI-HANH:57`) vi phạm ở **mọi** bản
  thi hành thật — 6 mã → 1 PR (#198), 3 mã → 1 PR (#266); task pack "7 mục" thì 3/4 bản thật chỉ viết 4 mục,
  bỏ đúng ba mục mà `TASK-PACK:197` gọi là "đắt nhất khi bỏ". Kết luận: quy trình PR-theo-hạng-mục-lớn mà chủ
  dự án yêu cầu hôm nay **hợp thức hoá cái đang chạy**, không phải đổi cái đang chạy.
- **Chẩn đoán bị lật khi đo** (bài học, `TRAPS.md` §2): subagent báo console thiếu cả `--repo` lẫn `--deliver`.
  Đọc ADR-0025 thì `--repo` **đã thành per-project** từ lâu (`research-requests.repo`, console gửi kèm lúc giao
  việc) ⇒ console không truyền là ĐÚNG THIẾT KẾ. Chỉ `--deliver`/`--push-remote` là thật: `Orchestrator.deliver`
  (`orchestrator.py:180`) là cờ toàn tiến trình, không override theo dự án. Nếu tin báo cáo mà sửa luôn thì đã
  phá một quyết định kiến trúc đúng.
- Làm trong #270 (3 task, mỗi task một commit): `--deliver-remote` cho console (cần `--allow-engine`, thiếu thì
  dừng mã 2) · sửa `HUONG-DAN-VAN-HANH.md:872` đang khẳng định sai · gate `release` thêm mục tự kiểm
  `release.giao-hang-duoc` đọc trạng thái tiến trình thật.
- **Quy trình mới chạy thử ngay trên chính PR này**: một nhánh cho cả hạng mục, mở **PR nháp sau task đầu**,
  mỗi task sau là một commit + push. Đo được hai điều: CI **có** chạy trên PR nháp (`pull_request.synchronize`,
  không job nào lọc draft) ⇒ mỗi task vẫn có CI thật; và vì số PR có ngay từ đầu nên dòng CHANGELOG mang
  `(#270)` từ commit thứ hai — **hết cảnh vá số PR sau khi merge** (luật bắt buộc 10).
- Bẫy thao tác: `UPDATE_GOLDEN=1` và `make subagents` trên Windows làm 10 file golden hiện `modified` mà
  `git diff` rỗng — chỉ đổi CRLF. Phải `git restore` chúng, chỉ giữ file đổi thật (`sc-gate-release.md`,
  `golden/gate_brief/release.json`). Không kiểm thì commit rác 10 file.
- Còn lại: hạng mục 2–5 (hồi sinh cổng chết · phiên audit thật A1–A8 · **bớt** luật trùng lặp · chốt quy trình
  mới + ADR-0012).

## Phiên 09:xx — pull + đánh giá hiện trạng, sửa số liệu README gốc (#269)

- Xong: `git pull --ff-only` main lên #268 (11 commit — ADR-0011 hoàn tất cả 5/5 giai đoạn). Đánh giá hiện trạng
  cho người dùng: chỉ 1 PR mở (#246 dependabot), sổ "Việc để lại đang treo" của `docs/TASK-PACK.md` còn 5 dòng.
- Xong: sửa hai dòng đầu của sổ treo — README gốc ghi keeper "8 agent" (đĩa có 10) và "cả sáu package" sót ở
  `AGENTS.md`/`ci.yml` sau khi `Studio-creators` rời workspace ở #259 (`members` nay có 5). PR #269, thuần tài
  liệu, auto-merge squash đã bật.
- Bẫy thao tác (không phải bug repo): sửa nhầm trực tiếp trên `main` checkout (bỏ qua luật cấm 2) trước khi nhớ
  ra phải vào worktree — phải `git stash push -u -m <tag>` các file đã sửa, `EnterWorktree`, tìm lại đúng
  `stash@{n}` qua tag (`git stash list --format='%gd %H %gs'`), `apply` bằng SHA rồi `drop`, không dùng `stash pop`
  trần (stash stack dùng chung mọi phiên). Ghi lại vì đây là quy trình đúng, chưa có sẵn ví dụ trong TRAPS.md.
- Người dùng hỏi thêm về repo `X-Studio` (studio tách ra ở #259) — trả lời: chỉ là bản subtree-split, không có
  gì mới để học ngoài quy ước đã có sẵn ở đây.
- Đang mở: PR #269 (auto-merge, chờ CI). Còn 3 dòng khác trong sổ treo chưa đụng: bù `branch = true` cho
  `companies/software-company` (86 nhánh chưa đi), `companies/keeper` chưa qua canary BT8, và mở rộng
  `test_readme_goc.py` canh thêm số liệu (là sửa test — cần PR riêng theo luật cấm 7).
- Bẫy mới: không — quy trình worktree/stash ở trên là kỹ thuật đã biết, chỉ chưa viết ra, không thêm mục TRAPS.md.
- Người sau KHÔNG được quên: nếu lỡ sửa trực tiếp trên `main` checkout của phiên khác, dùng đúng chuỗi
  stash-with-tag ở trên — đừng `git stash pop` trần.
