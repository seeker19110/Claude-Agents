# 2026-09-07

## Phiên tiếp kịch bản B — đóng K1, cập nhật tài liệu theo dõi

- Xong (5 PR merge, không code mới, chỉ tài liệu): #120 (`worktree_flow.py`), #121 (`no_changes_reason`),
  #122 (`scheduler.py`), #123 (`gates_flow.py`), #124 (`ticket_fsm.py`/`release_fsm.py` di chuyển thuần),
  #125 (`orch/fsm.py` + bảng `TICKET_TRANSITIONS`/`RELEASE_TRANSITIONS` thật, đóng K1 ở mức chức năng).
  `orchestrator.py` 2269 → 491 dòng.
- Phiên này (không code): đối chiếu lại `docs/DAC-TA-KICH-BAN-B.md` (bảng theo dõi §6) và
  `docs/DAC-TA-TRIEN-KHAI-KICH-BAN-B.md` (trạng thái K1) với thực tế 12 PR đã merge (#114–#125), bổ sung số PR
  còn thiếu trong `CHANGELOG.md` (#116–#120, #122–#125 — nội dung đã có sẵn từ các phiên trước, chỉ thiếu số).
- Phát hiện khi đối chiếu (đừng lặp lại nhầm lẫn này): hai file đặc tả dùng **hai cách đánh số K1.x/K2.x khác
  nhau** — `DAC-TA-KICH-BAN-B.md` đánh theo **tiêu chí nghiệm thu** (K1.4 = 3 khoá `once` thiếu thế hệ, K1.5 =
  guard `smoke.unverified`…), còn `DAC-TA-TRIEN-KHAI-KICH-BAN-B.md` và tiêu đề PR thật đánh theo **PR-theo-PR**
  (K1.4 = `worktree_flow.py`, K1.5 = `scheduler.py`…). Lần đầu cập nhật bảng theo dõi, mình đã map nhầm PR
  #120–125 thẳng vào K1.4–K1.7 của bảng nghiệm thu — sai, phải đọc lại định nghĩa từng dòng ở §"Yêu cầu" trước
  khi điền "xong". Tương tự với K2 (K2.1–K2.3 acceptance = interface/argv/backend, đều xong trong #116; K2.4
  acceptance = phạm vi bắt buộc qua Sandbox, chỉ studio xong ở #119, company vẫn dở).
- Trạng thái K1 (theo bảng nghiệm thu, không phải theo tên PR): K1.1/K1.2/K1.3/K1.6/K1.7 xong; **K1.4 một
  phần** (chỉ `no-test-author:{tid}` được thêm thế hệ ở #125; ba khoá `gate.escalate:{sid}`
  (`orch/scheduler.py:119`), `smoke.unverified:{rid}` (`orch/verify.py:47,52`),
  `delivery.skipped:{rid}` (`orch/release_fsm.py:97`) chưa có — **đã sửa cuối phiên, #127**); K1.5 hoá ra đã có sẵn từ trước
  (không tính công cho phiên K1 nào); **K1.8 chưa đạt** — `orchestrator.py` còn 491 dòng, đích ≤ 300.
- Trạng thái K2 (theo bảng nghiệm thu): K2.1/K2.2/K2.3 xong (#116); **K2.4/K2.5 một phần** — studio xong
  (#119), company (`tools.run`, `TicketWorkspace._run`, `run_smoke`) vẫn gọi `subprocess` thẳng; K2.6 xong
  (#119); K2.7 (console) và K2.8 (`SECURITY.md`/README) chưa làm.
- Người sau KHÔNG được quên:
  - Trước khi tự "đóng" một epic vì thấy PR title nói vậy, đọc lại đúng bảng "Yêu cầu" của epic đó trong
    `DAC-TA-KICH-BAN-B.md` — tên PR theo đặc tả triển khai không phải là danh sách tiêu chí nghiệm thu.
  - ~~K1 còn 3 khoá `once` thiếu thế hệ~~ — **đã xử lý trong chính phiên này, xem mục dưới (#127)**.
  - K1.8 (đích dòng) và phần công ty của K2.4/K2.5 (nối `tools.run`/`workspace._run`/`smoke.Popen` vào
    `Sandbox`) là hai việc rõ nhất còn lại trước khi mở K3 (`xagents-core`).
  - `SECURITY.md` vẫn đúng khi nói "sandbox là đường dẫn + env" cho company — đừng sửa tới khi K2.4/K2.5 phần
    company xong (nhắc lại từ phiên K2.3, vẫn còn hiệu lực).

## Phiên (tiếp) — đóng K1.4: ba khoá `once` còn thiếu thế hệ (#127)

- Làm ngay trong phiên sau khi đối chiếu xong (việc nhỏ, rõ, an toàn — không phải việc "để phiên riêng" như
  K1.8 hay K2.4/K2.5 phần company). Merge `5fbe799`, CI 15/15 xanh.
- Sửa **bốn** khoá chứ không phải ba: `gate.escalate:{sid}` → `:{GateRequest.created_at}`,
  `gate:{sid}:{pha}` → `:{created_at}` (cùng họ, cùng file, cùng nguyên nhân — AGENTS.md luật bắt buộc 5),
  `smoke.unverified:{rid}` → `:{rc.event_id}` (2 nhánh), `delivery.skipped:{rid}` → `:{env.event_id}`.
- **Đặc tả K1.4 sai ở một chỗ, đã ghi lại vào bảng theo dõi:** nó viết `gate.escalate:{sid}:{lần quá hạn}`.
  "Số lần quá hạn" không tồn tại ở đâu trong hệ và cũng không sống sót qua restart (khuôn 2). Thế hệ đúng là
  `GateRequest.created_at`: `HumanGate.pending` khoá theo `subject_id` nên gate mới **ghi đè** gate cũ dưới
  đúng cái tên đó, `created_at` là thứ duy nhất phân biệt được hai thế hệ.
- **Bài học đắt nhất của phiên — test canh quy ước cũng có lỗ, và lỗ của nó im lặng:**
  `test_khuon3_khoa_once_mang_the_he…` (thêm ở #125) đáng lẽ phải bắt `gate.escalate:{sid}` ngay từ đầu.
  Nó bỏ sót vì mẫu chỉ bắt `once=`, mà chuỗi `"once_key="` **không** chứa chuỗi con `"once="`
  (`supervisor.escalate_gate` nhận tham số tên `once_key`, không phải `once`). Một test quy ước xanh không
  chứng minh quy ước được giữ — nó chỉ chứng minh những gì mẫu của nó nhìn thấy được là sạch. Lần sau viết
  test kiểu grep: **kiểm chính cái mẫu** bằng cách chạy nó trên một vi phạm đã biết trước khi tin nó.
  Mẫu nay là `once(?:_key)?=`; tiêu chí "có thế hệ" tách ra `_co_the_he()` (≥ 2 thành phần **hoặc** thành
  phần duy nhất là `event_id`), nhờ đó `no_draft`/`plan_rejected`/`spec_runtime` không cần miễn nữa.
- `KHOA_MIEN` co lại còn `{uat, lesson, closed, review.escalate}`. Lý do miễn cũ của
  `delivery.skipped`/`smoke.unverified` — "audit phụ, đường ống đã có audit chính mang bằng chứng thật cho
  mỗi lần thử" — **sai**: nó chỉ đúng cho nhánh *có* chạy được smoke, mà đúng hai nhánh dùng khoá này là hai
  nhánh **không** chạy được (thiếu `runtime`, thiếu worktree), ở đó audit phụ là bản ghi DUY NHẤT nói lượt
  ấy chưa kiểm. Danh sách miễn phải kiểm lại lý do, không kế thừa.
- Đo hai chiều: bỏ ba hậu tố thế hệ khỏi mã nguồn → 4 test đỏ; khôi phục → 10 xanh. Toàn bộ
  `uv run pytest -q --cov`: 927 pass / 1 skip, coverage 99.97% (thiếu đúng 2 dòng POSIX
  `sqlite_bus.py:126,129`, bình thường trên Windows).
- Trạng thái K1 sau PR này: **K1.1–K1.7 xong**, chỉ còn **K1.8 chưa đạt** (`orchestrator.py` 491 dòng,
  đích ≤ 300).
- Người sau KHÔNG được quên (cập nhật lại danh sách ở mục trên):
  - Còn lại của kế hoạch B, theo thứ tự rủi ro tăng dần: K1.8 (tách tiếp `_call`/`__init__`/`status` khỏi
    `orchestrator.py`, hoặc quyết định hạ đích và ghi lý do) → phần company của K2.4/K2.5 (nối `tools.run`,
    `TicketWorkspace._run`, `run_smoke` vào `Sandbox`) → K2.7/K2.8 → K3 (`xagents-core`).
  - `SECURITY.md` vẫn giữ nguyên tới khi K2.4/K2.5 phần company xong (nhắc lại lần thứ ba).

## Phiên (tiếp) — K2.4/K2.5 + K2.8, K8.3/K8.4, K1.8: đóng K1 và phần company của K2

Ba PR merge liên tiếp sau #128. Trạng thái kế hoạch B sau phiên: **K0, K1 (đủ K1.1–K1.8), K2 (trừ K2.7), K8.3,
K8.4 xong**; còn K2.7, K3–K7, K8.1/K8.2/K8.5/K8.6, K9.

### #129 — K2.4/K2.5 phần company (và K2.8 theo sau)

- Ba điểm chạy mã của khách qua `Sandbox` (ADR-0035): `WorkspaceTools.run`, `TicketWorkspace._run`,
  `smoke.run_smoke`. `SECURITY.md` viết lại mục sandbox, kèm **ba giới hạn còn lại** (git, CLI model,
  `subprocess` vẫn thấy `HOME`).
- **Ba quyết định thiết kế đáng nhớ hơn bản thân việc nối:**
  1. **Sandbox đi theo WORKTREE, không theo nơi gọi** (`TicketWorkspace.sandbox` → `WorkspaceTools`). Nhờ vậy
     `runner.py` — nơi dựng tool cho mọi lượt agent kỹ thuật — không phải đổi dòng nào. Nếu đi theo nơi gọi thì
     mỗi chỗ dựng tool là một chỗ có thể quên, và quên thì lặng lẽ tụt về subprocess.
  2. **`Orchestrator` mặc định `SubprocessSandbox`, KHÔNG gọi `sandbox_from_config`.** `auto` chọn container
     ngay khi máy có docker, mà CI ubuntu có → hành vi test đổi theo máy chạy. Cấu hình chỉ đọc ở `orch/cli.py`
     cho đúng hai lệnh chạy mã khách (`run`, `redeploy`); fail-closed vẫn ném trước khi có lượt agent nào.
     CI ubuntu xanh là bằng chứng quyết định này đúng.
  3. **Smoke phải `network=True` + cổng loopback**, khác lint/test. `--network none` cho smoke là mọi lượt đều
     "không trả lời" — xanh vỏ đỏ lòng, đúng khuôn lỗi mà ADR-0029 sinh ra để chặn.
- Lý do miễn cũ trong test khuôn 3 (`delivery.skipped`/`smoke.unverified` là "audit phụ") hoá ra sai — xem #127,
  cùng bài học: **danh sách miễn phải kiểm lại LÝ DO, không kế thừa**.
- Lệch đặc tả có chủ ý: K2.5 viết `calls[].sandbox`; lặp chuỗi đó vào cả `read_file`/`search` đọc lên như thể
  chúng cũng chạy lệnh → đặt ở BẢNG (`ToolBox.sandbox`, như `root`), in trong `dump_calls`.

### #130 — K8.3/K8.4: hai luật thành cổng CI

- K8.3 (**đỏ**): PR `fix(` chạm `orchestrator.py`/`orch/` phải dẫn `ADR-0034` trong thân.
- K8.4 (**cảnh báo**): thiếu `docs/sessions/<ngày UTC>.md` → `::warning`, không chặn merge.
- **Đặc tả viết "ADR-0037" — repo không có file đó.** ADR tách máy trạng thái là 0034, và chính ADR-0034
  §"Hệ quả" đã ghi lại chỗ lệch số này. Người sau gặp số ADR trong đặc tả kịch bản B: **kiểm trên đĩa trước**.
- Chỉ soi `fix(`, không soi `refactor(`: tách module là làm ĐÚNG theo ADR; `fix(` mới là sửa hành vi máy trạng thái.
- Đã chạy thử logic shell ngoài Actions (5 ca K8.3 + pathspec đối chiếu commit #129 vs #128 + K8.4 hai chiều)
  TRƯỚC khi push. Cổng CI mà chỉ kiểm được bằng cách mở PR sau là cổng chưa ai đo.

### #131 — K1.8: tách subcommand CLI, và **sửa tiêu chí**

- `main` 162 → **29 dòng**; 13 subcommand sang `orch/cli_cmds.py`, hai bảng dispatch. Ranh giới hai bảng là chỗ
  dựng `Orchestrator`: `BUS_CMDS` chạy trước nên `metrics`/`trace`/`publish` không đòi SDK/API key.
- **Tiêu chí K1.8 gốc (`wc -l orchestrator.py ≤ 300`) mâu thuẫn với K1.7 của chính epic đó.** Đo trên 498 dòng:
  250 dòng là import + re-export + bảng gán `x = module.fn` — tức chính bề mặt shim mà K1.7/K1.1 CỐ Ý tạo ra;
  248 dòng còn lại là thân hàm, trong đó `_call` (78) + `process` (31) là hai thứ K1.7 yêu cầu ở lại. Cắt để
  xuống 300 là đảo ngược K1.7.
- Đổi thành **thân hàm ≤ 260** (nay 245) + `main` ≤ 60 + module `orch/` ≤ 400. Lý do ghi ở CẢ bảng Yêu cầu lẫn
  bảng theo dõi của `DAC-TA-KICH-BAN-B.md` và trong chính file test.
- **Bài học rộng hơn một con số:** một tiêu chí nghiệm thu cũng có thể là proxy sai. Mốc "tổng số dòng" đếm cả
  `import`, nên nó *phạt đúng cái refactor đã làm đúng*. Gặp tiêu chí không đạt được mà không phá một tiêu chí
  khác của cùng epic → đó là dấu hiệu tiêu chí sai, không phải việc chưa xong. Nhưng phải **ghi lý do ở nơi
  người sau đọc**, không lặng lẽ đổi số (khác hẳn hạ `fail_under` — luật cấm 6).

### Người sau KHÔNG được quên

- Còn lại của kế hoạch B, thứ tự rủi ro tăng dần: **K2.7** (console tile "sandbox=none" — nay đọc thẳng được từ
  `local_checks.sandbox`/`smoke.sandbox`, không cần thêm trường) → **K5** (ghi eval từ CI, độc lập, gỡ nút
  "máy cá nhân là đường duy nhất") → **K7.3** (danh tính người duyệt — hiện four-eyes chỉ dựa vào chữ trong body)
  → **K4** (phải sau K3.5) → **K3** (`xagents-core`, 8 PR, có 5 ngày chờ ổn định sau K3.5 — phiên riêng).
- K8.1/K8.2/K8.5/K8.6 (gateway README §rủi ro tài khoản, `make llm` một tài khoản, "ngày đầu của người thứ hai",
  gateway `stop` kiểm cmdline) vẫn chưa — rẻ, làm xen kẽ được.
- `SECURITY.md` nay đã đúng cho company; **đừng sửa lại** trừ khi K2.7 hoặc K3 đổi đường sandbox.

## Phiên (tiếp) — K2.7 và K5: đóng K2, mở cửa ghi eval cho người thứ hai

Hai PR nữa sau #132. **Kế hoạch B sau phiên: K0, K1, K2, K5, K8.3, K8.4 xong.** Còn K3, K4, K6, K7,
K8.1/K8.2/K8.5/K8.6, K9.

### #133 — K2.7 (đóng K2) + nửa còn lại của K2.8

- `truth.sandbox()` đếm lượt chạy mã của khách trong 24h theo tên sandbox, từ ba chỗ CODE điền bằng chứng ở
  #129. **Không đọc cấu hình** — cấu hình lúc người trực mở trang có thể đã khác cấu hình lúc lượt đó chạy.
- Ô chỉ sáng khi CẢ HAI đúng: có lượt ngoài container **và** máy có docker/podman. Máy không có runtime thì im:
  nhắc một việc người không làm được ngay là nhiễu, và **cảnh báo không hành động được thì lần sau người bỏ qua
  cả những cảnh báo hành động được**. Khi sáng thì nói thẳng lệnh cần gõ.
- Cửa sổ 24h = một ca trực. Dài hơn thì một lượt cũ kéo cảnh báo sáng mãi sau khi đã sửa — cảnh báo không tắt
  được là cảnh báo vô dụng.
- `software-company/README.md` mục "Chưa có" vẫn nói sandbox chỉ là allowlist + env: **sai từ #129**. Đã sửa.
- Test frontend của repo là khẳng định trên MÃ NGUỒN (không có JS runtime trong CI), nên ngoài test mình chạy
  chính `renderSandbox` trích từ `index.html` qua node, năm ca — cộng `node --check` có đối chứng.

### #134 — K5 (đủ K5.1–K5.5)

- Workflow `eval-record.yml`: `workflow_dispatch` (package/agents/provider/jobs, 45'), key từ Secrets, tên model
  từ Variables, kết quả **mở PR** `chore(<package>): ghi lại eval <agents>` nhãn `no-changelog`.
- `--jobs N` ở **cả hai** package. Thứ tự IN vẫn theo id: dòng gom lại rồi in sau vòng chạy, vì in thẳng trong
  luồng là log cài răng lược và `FAIL` không biết thuộc về ai.
- `concurrency` theo (package, provider): `save()` gộp chứ không ghi đè, nhưng hai lượt gộp lên hai BẢN SAO
  khác nhau của cùng file thì bản sau vẫn đè — mất một nửa kết quả vừa tốn tiền.
- Bảng điểm vào `$GITHUB_STEP_SUMMARY`, cổng vẫn chỉ là `gate_ok`.
- **Lệch đặc tả:** bảng theo dõi ghi K5 "sau K3.3" — sai, K5 không phụ thuộc K3.
- **Chưa chạy thật:** workflow cần `ANTHROPIC_API_KEY` trong Secrets và mỗi lượt là tiền thật. Lượt đầu tiên nên
  gõ `agents: <một agent>` để xác nhận đường đi trước khi chạy `all`.

### Bẫy thao tác mất ~20 phút (đã đưa vào `TRAPS.md` §3)

PR #134 mở ra mà **không có check nào chạy** — `statusCheckRollup` rỗng, `actions/runs?branch=…` rỗng, trong khi
các PR khác cùng lúc chạy bình thường. Mình nghi file workflow mới bị GitHub từ chối, và làm phép thử đối chứng
(bỏ file đó ra, đẩy lại) — **vẫn không chạy**, tức là loại được giả thuyết đó. Nguyên nhân thật:
`mergeStateStatus = DIRTY`, PR xung đột với `main` (#133 vừa merge, cùng chạm `CHANGELOG.md`). **GitHub chạy
check trên commit MERGE, nên PR không merge được thì không có check nào để chạy.** Rebase là xong.

Bài học ghi lại: phép thử đối chứng lần này *không* tìm ra nguyên nhân, nhưng nó loại được một giả thuyết sai
mà mình đã sắp bỏ nửa tiếng đi sửa — đó vẫn là lãi.

### Người sau KHÔNG được quên

- Còn lại: **K7.3** (danh tính người duyệt — four-eyes hiện chỉ dựa vào chữ `by` trong body) → **K6** (mypy
  strict, rải theo module) → **K4** (phải sau K3.5) → **K3** (`xagents-core`, 8 PR, có mốc chờ 5 ngày lịch giữa
  K3.5 và K3.6 — phiên riêng, đừng nhét vào phiên có việc khác kẻo quên mốc).
- K8.1/K8.2/K8.5/K8.6 rẻ, xen kẽ được. K9 là thước đo, cần K8.5 trước cho K9.3.
- Muốn dùng `eval-record` thật thì phải đặt Secrets `ANTHROPIC_API_KEY` (hoặc `OPENAI_API_KEY`) và Variables
  `COMPANY_MODEL_STRONG/STANDARD/LIGHT` trong Settings của repo — chưa ai đặt.
