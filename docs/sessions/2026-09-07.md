# 2026-09-07

## Phiên tiếp kịch bản B — đóng K1, cập nhật tài liệu theo dõi

- Xong (5 PR merge, không code mới, chỉ tài liệu): #120 (`worktree_flow.py`), #121 (`no_changes_reason`),
  #122 (`scheduler.py`), #123 (`gates_flow.py`), #124 (`ticket_fsm.py`/`release_fsm.py` di chuyển thuần),
  #125 (`orch/fsm.py` + bảng `TICKET_TRANSITIONS`/`RELEASE_TRANSITIONS` thật, đóng K1 ở mức chức năng).
  `orchestrator.py` 2269 → 491 dòng.
- Phiên này (không code): đối chiếu lại `docs/DAC-TA-KICH-BAN-B.md` (bảng theo dõi §6) và
  `docs/DAC-TA-TRIEN-KHAI-KICH-BAN-B.md` (trạng thái K1) với thực tế 12 PR đã merge (#114–#125), bổ sung số PR
  còn thiếu trong `CHANGELOG.md` (#116–#120, #122–#125 — nội dung đã có sẵn từ các phiên trước, chỉ thiếu số).
- Phát hiện khi đối chiếu (đừng lặp lại nhầm lẫn này): hai file đặc tả dùng **hai cách đánh số K1.x/K2.x khác
  nhau** — `DAC-TA-KICH-BAN-B.md` đánh theo **tiêu chí nghiệm thu** (K1.4 = 3 khoá `once` thiếu thế hệ, K1.5 =
  guard `smoke.unverified`…), còn `DAC-TA-TRIEN-KHAI-KICH-BAN-B.md` và tiêu đề PR thật đánh theo **PR-theo-PR**
  (K1.4 = `worktree_flow.py`, K1.5 = `scheduler.py`…). Lần đầu cập nhật bảng theo dõi, mình đã map nhầm PR
  #120–125 thẳng vào K1.4–K1.7 của bảng nghiệm thu — sai, phải đọc lại định nghĩa từng dòng ở §"Yêu cầu" trước
  khi điền "xong". Tương tự với K2 (K2.1–K2.3 acceptance = interface/argv/backend, đều xong trong #116; K2.4
  acceptance = phạm vi bắt buộc qua Sandbox, chỉ studio xong ở #119, company vẫn dở).
- Trạng thái K1 (theo bảng nghiệm thu, không phải theo tên PR): K1.1/K1.2/K1.3/K1.6/K1.7 xong; **K1.4 một
  phần** (chỉ `no-test-author:{tid}` được thêm thế hệ ở #125; ba khoá `gate.escalate:{sid}`
  (`orch/scheduler.py:119`), `smoke.unverified:{rid}` (`orch/verify.py:47,52`),
  `delivery.skipped:{rid}` (`orch/release_fsm.py:97`) chưa có — **đã sửa cuối phiên, #127**); K1.5 hoá ra đã có sẵn từ trước
  (không tính công cho phiên K1 nào); **K1.8 chưa đạt** — `orchestrator.py` còn 491 dòng, đích ≤ 300.
- Trạng thái K2 (theo bảng nghiệm thu): K2.1/K2.2/K2.3 xong (#116); **K2.4/K2.5 một phần** — studio xong
  (#119), company (`tools.run`, `TicketWorkspace._run`, `run_smoke`) vẫn gọi `subprocess` thẳng; K2.6 xong
  (#119); K2.7 (console) và K2.8 (`SECURITY.md`/README) chưa làm.
- Người sau KHÔNG được quên:
  - Trước khi tự "đóng" một epic vì thấy PR title nói vậy, đọc lại đúng bảng "Yêu cầu" của epic đó trong
    `DAC-TA-KICH-BAN-B.md` — tên PR theo đặc tả triển khai không phải là danh sách tiêu chí nghiệm thu.
  - ~~K1 còn 3 khoá `once` thiếu thế hệ~~ — **đã xử lý trong chính phiên này, xem mục dưới (#127)**.
  - K1.8 (đích dòng) và phần công ty của K2.4/K2.5 (nối `tools.run`/`workspace._run`/`smoke.Popen` vào
    `Sandbox`) là hai việc rõ nhất còn lại trước khi mở K3 (`xagents-core`).
  - `SECURITY.md` vẫn đúng khi nói "sandbox là đường dẫn + env" cho company — đừng sửa tới khi K2.4/K2.5 phần
    company xong (nhắc lại từ phiên K2.3, vẫn còn hiệu lực).

## Phiên (tiếp) — đóng K1.4: ba khoá `once` còn thiếu thế hệ (#127)

- Làm ngay trong phiên sau khi đối chiếu xong (việc nhỏ, rõ, an toàn — không phải việc "để phiên riêng" như
  K1.8 hay K2.4/K2.5 phần company). Merge `5fbe799`, CI 15/15 xanh.
- Sửa **bốn** khoá chứ không phải ba: `gate.escalate:{sid}` → `:{GateRequest.created_at}`,
  `gate:{sid}:{pha}` → `:{created_at}` (cùng họ, cùng file, cùng nguyên nhân — AGENTS.md luật bắt buộc 5),
  `smoke.unverified:{rid}` → `:{rc.event_id}` (2 nhánh), `delivery.skipped:{rid}` → `:{env.event_id}`.
- **Đặc tả K1.4 sai ở một chỗ, đã ghi lại vào bảng theo dõi:** nó viết `gate.escalate:{sid}:{lần quá hạn}`.
  "Số lần quá hạn" không tồn tại ở đâu trong hệ và cũng không sống sót qua restart (khuôn 2). Thế hệ đúng là
  `GateRequest.created_at`: `HumanGate.pending` khoá theo `subject_id` nên gate mới **ghi đè** gate cũ dưới
  đúng cái tên đó, `created_at` là thứ duy nhất phân biệt được hai thế hệ.
- **Bài học đắt nhất của phiên — test canh quy ước cũng có lỗ, và lỗ của nó im lặng:**
  `test_khuon3_khoa_once_mang_the_he…` (thêm ở #125) đáng lẽ phải bắt `gate.escalate:{sid}` ngay từ đầu.
  Nó bỏ sót vì mẫu chỉ bắt `once=`, mà chuỗi `"once_key="` **không** chứa chuỗi con `"once="`
  (`supervisor.escalate_gate` nhận tham số tên `once_key`, không phải `once`). Một test quy ước xanh không
  chứng minh quy ước được giữ — nó chỉ chứng minh những gì mẫu của nó nhìn thấy được là sạch. Lần sau viết
  test kiểu grep: **kiểm chính cái mẫu** bằng cách chạy nó trên một vi phạm đã biết trước khi tin nó.
  Mẫu nay là `once(?:_key)?=`; tiêu chí "có thế hệ" tách ra `_co_the_he()` (≥ 2 thành phần **hoặc** thành
  phần duy nhất là `event_id`), nhờ đó `no_draft`/`plan_rejected`/`spec_runtime` không cần miễn nữa.
- `KHOA_MIEN` co lại còn `{uat, lesson, closed, review.escalate}`. Lý do miễn cũ của
  `delivery.skipped`/`smoke.unverified` — "audit phụ, đường ống đã có audit chính mang bằng chứng thật cho
  mỗi lần thử" — **sai**: nó chỉ đúng cho nhánh *có* chạy được smoke, mà đúng hai nhánh dùng khoá này là hai
  nhánh **không** chạy được (thiếu `runtime`, thiếu worktree), ở đó audit phụ là bản ghi DUY NHẤT nói lượt
  ấy chưa kiểm. Danh sách miễn phải kiểm lại lý do, không kế thừa.
- Đo hai chiều: bỏ ba hậu tố thế hệ khỏi mã nguồn → 4 test đỏ; khôi phục → 10 xanh. Toàn bộ
  `uv run pytest -q --cov`: 927 pass / 1 skip, coverage 99.97% (thiếu đúng 2 dòng POSIX
  `sqlite_bus.py:126,129`, bình thường trên Windows).
- Trạng thái K1 sau PR này: **K1.1–K1.7 xong**, chỉ còn **K1.8 chưa đạt** (`orchestrator.py` 491 dòng,
  đích ≤ 300).
- Người sau KHÔNG được quên (cập nhật lại danh sách ở mục trên):
  - Còn lại của kế hoạch B, theo thứ tự rủi ro tăng dần: K1.8 (tách tiếp `_call`/`__init__`/`status` khỏi
    `orchestrator.py`, hoặc quyết định hạ đích và ghi lý do) → phần company của K2.4/K2.5 (nối `tools.run`,
    `TicketWorkspace._run`, `run_smoke` vào `Sandbox`) → K2.7/K2.8 → K3 (`xagents-core`).
  - `SECURITY.md` vẫn giữ nguyên tới khi K2.4/K2.5 phần company xong (nhắc lại lần thứ ba).

## Phiên (tiếp) — K2.4/K2.5 + K2.8, K8.3/K8.4, K1.8: đóng K1 và phần company của K2

Ba PR merge liên tiếp sau #128. Trạng thái kế hoạch B sau phiên: **K0, K1 (đủ K1.1–K1.8), K2 (trừ K2.7), K8.3,
K8.4 xong**; còn K2.7, K3–K7, K8.1/K8.2/K8.5/K8.6, K9.

### #129 — K2.4/K2.5 phần company (và K2.8 theo sau)

- Ba điểm chạy mã của khách qua `Sandbox` (ADR-0035): `WorkspaceTools.run`, `TicketWorkspace._run`,
  `smoke.run_smoke`. `SECURITY.md` viết lại mục sandbox, kèm **ba giới hạn còn lại** (git, CLI model,
  `subprocess` vẫn thấy `HOME`).
- **Ba quyết định thiết kế đáng nhớ hơn bản thân việc nối:**
  1. **Sandbox đi theo WORKTREE, không theo nơi gọi** (`TicketWorkspace.sandbox` → `WorkspaceTools`). Nhờ vậy
     `runner.py` — nơi dựng tool cho mọi lượt agent kỹ thuật — không phải đổi dòng nào. Nếu đi theo nơi gọi thì
     mỗi chỗ dựng tool là một chỗ có thể quên, và quên thì lặng lẽ tụt về subprocess.
  2. **`Orchestrator` mặc định `SubprocessSandbox`, KHÔNG gọi `sandbox_from_config`.** `auto` chọn container
     ngay khi máy có docker, mà CI ubuntu có → hành vi test đổi theo máy chạy. Cấu hình chỉ đọc ở `orch/cli.py`
     cho đúng hai lệnh chạy mã khách (`run`, `redeploy`); fail-closed vẫn ném trước khi có lượt agent nào.
     CI ubuntu xanh là bằng chứng quyết định này đúng.
  3. **Smoke phải `network=True` + cổng loopback**, khác lint/test. `--network none` cho smoke là mọi lượt đều
     "không trả lời" — xanh vỏ đỏ lòng, đúng khuôn lỗi mà ADR-0029 sinh ra để chặn.
- Lý do miễn cũ trong test khuôn 3 (`delivery.skipped`/`smoke.unverified` là "audit phụ") hoá ra sai — xem #127,
  cùng bài học: **danh sách miễn phải kiểm lại LÝ DO, không kế thừa**.
- Lệch đặc tả có chủ ý: K2.5 viết `calls[].sandbox`; lặp chuỗi đó vào cả `read_file`/`search` đọc lên như thể
  chúng cũng chạy lệnh → đặt ở BẢNG (`ToolBox.sandbox`, như `root`), in trong `dump_calls`.

### #130 — K8.3/K8.4: hai luật thành cổng CI

- K8.3 (**đỏ**): PR `fix(` chạm `orchestrator.py`/`orch/` phải dẫn `ADR-0034` trong thân.
- K8.4 (**cảnh báo**): thiếu `docs/sessions/<ngày UTC>.md` → `::warning`, không chặn merge.
- **Đặc tả viết "ADR-0037" — repo không có file đó.** ADR tách máy trạng thái là 0034, và chính ADR-0034
  §"Hệ quả" đã ghi lại chỗ lệch số này. Người sau gặp số ADR trong đặc tả kịch bản B: **kiểm trên đĩa trước**.
- Chỉ soi `fix(`, không soi `refactor(`: tách module là làm ĐÚNG theo ADR; `fix(` mới là sửa hành vi máy trạng thái.
- Đã chạy thử logic shell ngoài Actions (5 ca K8.3 + pathspec đối chiếu commit #129 vs #128 + K8.4 hai chiều)
  TRƯỚC khi push. Cổng CI mà chỉ kiểm được bằng cách mở PR sau là cổng chưa ai đo.

### #131 — K1.8: tách subcommand CLI, và **sửa tiêu chí**

- `main` 162 → **29 dòng**; 13 subcommand sang `orch/cli_cmds.py`, hai bảng dispatch. Ranh giới hai bảng là chỗ
  dựng `Orchestrator`: `BUS_CMDS` chạy trước nên `metrics`/`trace`/`publish` không đòi SDK/API key.
- **Tiêu chí K1.8 gốc (`wc -l orchestrator.py ≤ 300`) mâu thuẫn với K1.7 của chính epic đó.** Đo trên 498 dòng:
  250 dòng là import + re-export + bảng gán `x = module.fn` — tức chính bề mặt shim mà K1.7/K1.1 CỐ Ý tạo ra;
  248 dòng còn lại là thân hàm, trong đó `_call` (78) + `process` (31) là hai thứ K1.7 yêu cầu ở lại. Cắt để
  xuống 300 là đảo ngược K1.7.
- Đổi thành **thân hàm ≤ 260** (nay 245) + `main` ≤ 60 + module `orch/` ≤ 400. Lý do ghi ở CẢ bảng Yêu cầu lẫn
  bảng theo dõi của `DAC-TA-KICH-BAN-B.md` và trong chính file test.
- **Bài học rộng hơn một con số:** một tiêu chí nghiệm thu cũng có thể là proxy sai. Mốc "tổng số dòng" đếm cả
  `import`, nên nó *phạt đúng cái refactor đã làm đúng*. Gặp tiêu chí không đạt được mà không phá một tiêu chí
  khác của cùng epic → đó là dấu hiệu tiêu chí sai, không phải việc chưa xong. Nhưng phải **ghi lý do ở nơi
  người sau đọc**, không lặng lẽ đổi số (khác hẳn hạ `fail_under` — luật cấm 6).

### Người sau KHÔNG được quên

- Còn lại của kế hoạch B, thứ tự rủi ro tăng dần: **K2.7** (console tile "sandbox=none" — nay đọc thẳng được từ
  `local_checks.sandbox`/`smoke.sandbox`, không cần thêm trường) → **K5** (ghi eval từ CI, độc lập, gỡ nút
  "máy cá nhân là đường duy nhất") → **K7.3** (danh tính người duyệt — hiện four-eyes chỉ dựa vào chữ trong body)
  → **K4** (phải sau K3.5) → **K3** (`xagents-core`, 8 PR, có 5 ngày chờ ổn định sau K3.5 — phiên riêng).
- K8.1/K8.2/K8.5/K8.6 (gateway README §rủi ro tài khoản, `make llm` một tài khoản, "ngày đầu của người thứ hai",
  gateway `stop` kiểm cmdline) vẫn chưa — rẻ, làm xen kẽ được.
- `SECURITY.md` nay đã đúng cho company; **đừng sửa lại** trừ khi K2.7 hoặc K3 đổi đường sandbox.

## Phiên (tiếp) — K2.7 và K5: đóng K2, mở cửa ghi eval cho người thứ hai

Hai PR nữa sau #132. **Kế hoạch B sau phiên: K0, K1, K2, K5, K8.3, K8.4 xong.** Còn K3, K4, K6, K7,
K8.1/K8.2/K8.5/K8.6, K9.

### #133 — K2.7 (đóng K2) + nửa còn lại của K2.8

- `truth.sandbox()` đếm lượt chạy mã của khách trong 24h theo tên sandbox, từ ba chỗ CODE điền bằng chứng ở
  #129. **Không đọc cấu hình** — cấu hình lúc người trực mở trang có thể đã khác cấu hình lúc lượt đó chạy.
- Ô chỉ sáng khi CẢ HAI đúng: có lượt ngoài container **và** máy có docker/podman. Máy không có runtime thì im:
  nhắc một việc người không làm được ngay là nhiễu, và **cảnh báo không hành động được thì lần sau người bỏ qua
  cả những cảnh báo hành động được**. Khi sáng thì nói thẳng lệnh cần gõ.
- Cửa sổ 24h = một ca trực. Dài hơn thì một lượt cũ kéo cảnh báo sáng mãi sau khi đã sửa — cảnh báo không tắt
  được là cảnh báo vô dụng.
- `software-company/README.md` mục "Chưa có" vẫn nói sandbox chỉ là allowlist + env: **sai từ #129**. Đã sửa.
- Test frontend của repo là khẳng định trên MÃ NGUỒN (không có JS runtime trong CI), nên ngoài test mình chạy
  chính `renderSandbox` trích từ `index.html` qua node, năm ca — cộng `node --check` có đối chứng.

### #134 — K5 (đủ K5.1–K5.5)

- Workflow `eval-record.yml`: `workflow_dispatch` (package/agents/provider/jobs, 45'), key từ Secrets, tên model
  từ Variables, kết quả **mở PR** `chore(<package>): ghi lại eval <agents>` nhãn `no-changelog`.
- `--jobs N` ở **cả hai** package. Thứ tự IN vẫn theo id: dòng gom lại rồi in sau vòng chạy, vì in thẳng trong
  luồng là log cài răng lược và `FAIL` không biết thuộc về ai.
- `concurrency` theo (package, provider): `save()` gộp chứ không ghi đè, nhưng hai lượt gộp lên hai BẢN SAO
  khác nhau của cùng file thì bản sau vẫn đè — mất một nửa kết quả vừa tốn tiền.
- Bảng điểm vào `$GITHUB_STEP_SUMMARY`, cổng vẫn chỉ là `gate_ok`.
- **Lệch đặc tả:** bảng theo dõi ghi K5 "sau K3.3" — sai, K5 không phụ thuộc K3.
- **Chưa chạy thật:** workflow cần `ANTHROPIC_API_KEY` trong Secrets và mỗi lượt là tiền thật. Lượt đầu tiên nên
  gõ `agents: <một agent>` để xác nhận đường đi trước khi chạy `all`.

### Bẫy thao tác mất ~20 phút (đã đưa vào `TRAPS.md` §3)

PR #134 mở ra mà **không có check nào chạy** — `statusCheckRollup` rỗng, `actions/runs?branch=…` rỗng, trong khi
các PR khác cùng lúc chạy bình thường. Mình nghi file workflow mới bị GitHub từ chối, và làm phép thử đối chứng
(bỏ file đó ra, đẩy lại) — **vẫn không chạy**, tức là loại được giả thuyết đó. Nguyên nhân thật:
`mergeStateStatus = DIRTY`, PR xung đột với `main` (#133 vừa merge, cùng chạm `CHANGELOG.md`). **GitHub chạy
check trên commit MERGE, nên PR không merge được thì không có check nào để chạy.** Rebase là xong.

Bài học ghi lại: phép thử đối chứng lần này *không* tìm ra nguyên nhân, nhưng nó loại được một giả thuyết sai
mà mình đã sắp bỏ nửa tiếng đi sửa — đó vẫn là lãi.

### Người sau KHÔNG được quên

- Còn lại: **K7.3** (danh tính người duyệt — four-eyes hiện chỉ dựa vào chữ `by` trong body) → **K6** (mypy
  strict, rải theo module) → **K4** (phải sau K3.5) → **K3** (`xagents-core`, 8 PR, có mốc chờ 5 ngày lịch giữa
  K3.5 và K3.6 — phiên riêng, đừng nhét vào phiên có việc khác kẻo quên mốc).
- K8.1/K8.2/K8.5/K8.6 rẻ, xen kẽ được. K9 là thước đo, cần K8.5 trước cho K9.3.
- Muốn dùng `eval-record` thật thì phải đặt Secrets `ANTHROPIC_API_KEY` (hoặc `OPENAI_API_KEY`) và Variables
  `COMPANY_MODEL_STRONG/STANDARD/LIGHT` trong Settings của repo — chưa ai đặt.

---

## Phiên chiều 2026-09-07 — #143, #144, K3.0

### Sai lầm đắt nhất của phiên: kết luận hiện trạng trên mốc cũ

Mình mở bảng theo dõi trong một `git worktree` cũ (`main` ở #125), thấy K1.4 ghi "một phần", và **làm lại toàn
bộ**: sửa ba khoá `once`, viết test hai chiều, đo cả hai chiều. Xong mới `git fetch` — `origin/main` đã ở **#142**,
và K1.4 chính là **#127**, merge từ lâu. #127 còn tìm ra đúng cả lỗ hổng regex `once_key=` mà mình tưởng mình mới
phát hiện. Toàn bộ công bỏ đi, worktree xoá.

**Luật rút ra, làm trước mọi thứ khác ở đầu phiên:** `git fetch origin` → đọc `CHANGELOG.md` mục "Chưa phát hành"
→ `gh pr list --state merged --limit 20` → *rồi mới* mở bảng theo dõi. Bảng theo dõi là thứ trôi nhanh nhất trong
repo vì nó được cập nhật bằng tay.

### Chiều ngược lại: quét kỹ rồi thì đừng tin bảng

Cùng phiên, bảng ghi **K1.5 "xong (từ trước)"** và dẫn `verify.py:109-111` làm bằng chứng. Đó là hàm
`verdict_with_run` — đường **QA hồi quy**. Yêu cầu K1.5 nói về `smoke()` — đường **deploy staging**, nơi không hề
đọc `kind`; và cờ `legacy` chưa từng tồn tại (`grep -rn legacy src/` rỗng). Người viết bảng đối chiếu nhầm hàm.

Lần theo thì lỗ hổng còn xấu hơn đặc tả mô tả (→ #144, ADR-0036):

```
smoke unverified -> status giữ `deployed` -> QA hạ `fail` -> gate escalation
  -> người duyệt escalation `approve`
  -> waive_release_findings waive MỌI nguồn chưa pass, KỂ CẢ `qa`
  -> Gate 3 mở -> production
```

Cơ chế waive sinh ra cho finding không có code để sửa (DPIA, license). Nó **không phân biệt được** với "chưa bao
giờ kiểm sản phẩm có chạy không". Tức là hình dạng QLKH 2026-09-06 thoát ra được bằng **một chữ ký của người**,
không phải bằng một lỗi của máy. Đó là lý do K1.5 chặn ở `smoke` chứ không để QA chặn: RC `failed` không có route
đi tiếp nên waive không cứu được.

**Còn nợ:** `waive_release_findings` vẫn waive theo *nguồn*, không theo từng *finding*. #144 chỉ chặn loại finding
nguy hiểm nhất không đi vào đường waive. Có loại thứ hai "không được waive" thì phải phân loại finding thật sự,
đừng thêm case-by-case.

### Bánh cóc K6.3 làm đúng việc của nó

Bản K1.5 đầu tiên của mình tách một hàm phụ `_chua_kiem(o, rc, rid, p, pid, kind, ly_do)`. Hàm đó nhận và trả
payload → thêm 2 `dict[str, Any]` → `test_k63_banh_coc_dict_any_trong_orch_khong_duoc_tang` đỏ ở 27 > trần 25.

Trần đó vừa đặt ở #141 với luật **"chỉ được giảm"**. Nâng nó ngay ở PR kế tiếp là làm hỏng chính cơ chế, mà
"kiểu hoá chỗ khác để hạ trần" bằng một `TypeAlias` cosmetic thì là gian lận số đo. Cách đúng hoá ra cũng là cách
gọn hơn: **gộp hai nhánh không-smoke-được vào một khối** thay vì tách hàm — một chỗ quyết, không thêm dòng kiểu
nào, đếm giữ nguyên 25.

### K3.0 (PR đang mở)

Khung `xagents-core` + `CoreConfig`/`TopicACL`, hai job CI, workspace/Makefile lên năm member, `strict = true`
(K6.1). Hai điều đặc tả để ngỏ mà phiên này chốt:

- **Ruleset không phải sửa.** Required status check chỉ có `quality` + `metadata`, nên nối hai job vào
  `quality.needs` là đủ. Đặc tả viết "thêm 2 context (nếu required check là `quality` tổng thể thì chỉ cần
  `needs`; đọc `protection-guard` để chắc)" — đã đọc, đúng là chỉ cần `needs`.
- **`xagents-core` là phụ thuộc khai báo của cả hai công ty ngay từ K3.0**, dù chưa import dòng nào. Để `uv.lock`
  chốt một lần, và K3.1 là bước chuyển mã thuần.

### Bẫy thao tác của phiên này

- **`cmd | tail` nuốt mã thoát.** Chạy `pytest ... 2>&1 | tail -12` báo "exit code 0" trong khi pytest **lỗi**
  (`unrecognized arguments: -n`). Ghi ra file rồi `echo EXIT=$?` mới thấy. Đừng đọc mã thoát qua ống dẫn.
- **Worktree mới có `.venv` riêng, thiếu nhóm `dev`.** `uv run pytest -n auto` trong worktree vừa tạo báo
  `unrecognized arguments: -n` vì thiếu `pytest-xdist`. Chạy `uv sync` ở **gốc worktree** trước, không phải trong
  thư mục package.
- **`make` không có trong Git Bash trên máy này** (exit 127). Muốn chạy fan-out thì lặp tay từng thư mục.
- **`test_readme_goc_khop_nguong_coverage_va_so_test_hai_cong_ty` lấy dòng ĐẦU TIÊN chứa `` `fail_under` ``.**
  Thêm một dòng bảng README có nhắc chữ đó ở phía trên là test đỏ với thông điệp khó hiểu. Dòng bảng của
  `xagents-core` phải viết "phủ 100% dòng" thay vì tên biến.

### Người sau KHÔNG được quên (cập nhật)

- Còn lại thật sự: **K3.1–K3.7** (mốc chờ **≥ 5 ngày lịch** giữa K3.5 và K3.6 — không rút ngắn được), **K4**
  (sau K3.5), **K9** (cần docker + dự án mẫu; provider TTS/ảnh thật + kênh YouTube; và **một người thứ hai** —
  ba thứ không tự làm được).
- **K7.3 hoãn có chủ ý**, điều kiện kích hoạt lại đã ghi ở bảng theo dõi: có người thứ hai chạm console.

---

## Phiên tối 2026-09-07 — K3.3b: cấu hình LLM lên core, `CORE` đầu tiên của hai công ty

Vào phiên đúng thứ tự đã rút ra ở phiên trước: `git fetch origin` → `CHANGELOG.md` mục "Chưa phát hành" →
PR đang mở (không có) → *rồi mới* mở bảng theo dõi. `origin/main` ở #150 (K3.3a), việc tiếp theo là **K3.3b**.

### Việc đã làm

- `xagents_core.llm` nhận `LLMConfig` (13 trường hai bên khai giống nhau) + `load_config(core, path, cls=…)`.
  `company.LLMConfig` và `studio.LLMConfig` nay là **lớp con**, ghi đè ba móc: `apply_backend_yaml` (khoá đọc
  được ở CẢ hai cấp — gốc `llm.yaml` và từng phần tử `backends:`), `apply_yaml` (chỉ ở gốc), `apply_env`.
- **`company/core.py` + `studio/core.py`** — `CORE` đầu tiên, nguồn duy nhất của (`prefix`, `root`, `db_name`).
  K3.4–K3.7 sẽ điền nốt `topic_acl`/`payload_models`/`transitions`; cố ý **chưa** khai bảng rỗng nào ở đây, khai
  trước một bảng rỗng là mời người sau tin vào nó.
- `types-PyYAML` vào nhóm `dev` của core. Bốn package kia chạy mypy với `--ignore-missing-imports` nên `yaml`
  im lặng thành `Any`; core cố ý không có cờ đó (ADR gốc 0001 §6), nên nó phải khai stub thật.

### Ba quyết định đáng nhớ hơn bản thân việc chuyển mã

1. **Kế thừa, không phải cờ.** 14 trường company có mà studio không (CLI/MCP, retry, giá, ngân sách, nợ, sandbox)
   là *dữ liệu có kiểu*. Gom chúng vào một `dict[str, Any]` chung ở core cho "trung lập" là đổi 14 trường có kiểu
   lấy một túi `Any` — mất đúng thứ `strict` của core sinh ra để giữ.
2. **`PREFIX` là `ClassVar`, không phải trường.** `backend_config` sao chép `self.__dict__` xuống từng backend;
   tiền tố công ty không có lý do gì khác nhau giữa hai backend của cùng công ty. Và một `LLMConfig()` dựng tay
   trong test vẫn phải báo `COMPANY_MODEL_STRONG`, không phải chuỗi rỗng vì người dựng quên truyền tiền tố.
3. **`select_backends` tách khỏi `apply_env` và chạy sau cùng.** `<PREFIX>_LLM_PROVIDER` xoá sạch `backends`, nên
   thứ tự hai bước này là *hành vi*, không phải khẩu vị: đặt ngược lại thì hai biến cùng đặt cho ra một cấu hình
   không tồn tại ở đâu cả. Có ca đo riêng cho đúng thứ tự đó.

### Đo hai chiều: 5 đột biến, mỗi cái đỏ đúng một ca

`type(self)` → `LLMConfig` trần; `max_input_chars` đọc ở cấp backend; đảo thứ tự `select_backends`/`apply_env`;
`PREFIX` rỗng ở company; studio mọc lại trường `retries`. Không cái nào làm đỏ nhiều hơn một ca — tức là các ca
đo những thứ khác nhau chứ không cùng đo một thứ.

### Bẫy mất ~25 phút, đã vào `TRAPS.md` §3

Sau khi khôi phục file bằng `cp file.bak file`, ca vừa xanh **đỏ vĩnh viễn**: `inspect.getsource` in ra mã ĐÚNG
nhưng chương trình chạy mã CŨ. Đột biến chỉ đảo chỗ hai dòng nên file khôi phục **cùng kích thước**, và cả hai
thao tác nằm trong **cùng một giây** — `__pycache__` khoá theo (mtime, size) nên `.pyc` của bản đột biến vẫn được
coi là hợp lệ. Xoá `__pycache__` là xong. Đáng nhớ vì kịch bản đo hai chiều ở repo này *luôn* có bước khôi phục
file, tức là bẫy này chờ sẵn ở mọi lần đo sau. Bài học rộng hơn: khi mã trên đĩa và hành vi lệch nhau, nghi lớp
đệm giữa hai thứ đó trước khi nghi mã.

### Người sau KHÔNG được quên

- **K3.3c** là bước rủi ro nhất của K3.3: bốn adapter + `Completion.json()` (hai thuật toán bóc JSON khác nhau).
  Đặc tả nói phải đo bằng **eval replay**, không đo bằng đọc diff. Rồi **K3.3d** (`routing.py` + studio hoãn
  `TransientError` thay vì dừng) — K5 ghi "mở khoá sau d" nhưng K5 đã xong từ #134 và không phụ thuộc K3.
- Sau K3.3d mới tới K3.4–K3.7; **mốc chờ ≥ 5 ngày lịch giữa K3.5 và K3.6** vẫn còn nguyên, đừng nhét K3.5 vào
  một phiên có việc khác kẻo quên mốc.
- `SECURITY.md` không đổi ở phiên này (K3.3b không đụng đường sandbox).

## Phiên tối (tiếp) — K3.3c1: hợp nhất `Completion` và đường bóc JSON

#151 (K3.3b) merge lúc 10:41Z, CI 24/24. Việc tiếp: K3.3c. Đo `difflib` lại từng symbol trước khi bắt tay
(`AnthropicClient` 0.90 · `CodexClient` 0.93 · `FakeClient` 0.96 · `ClaudeCodeClient` 0.32 ·
`OpenAICompatClient` 0.28 · `Completion` 0.23) → **tách c thành ba bước theo mức lệch**, và làm bước lệch nhất
trước vì nó là điều kiện của hai bước kia (adapter nào cũng trả `Completion`).

### Quyết định hợp nhất — chỗ hai bản mâu thuẫn THẬT

Không phải "trôi khỏi nhau" như `LLMConfig`, mà là hai chính sách trái nhau:

- **Company**: bóc fence bao ngoài rồi đòi JSON đứng ngay từ ký tự đầu. JSON hỏng **giữa cấu trúc** (model đóng
  object sớm rồi viết tiếp `,{...}`) thì **ĐỎ**, cố ý — cứu chỗ đó là đoán ý model, đoán sai thì ticket đi tiếp
  với một nửa dữ liệu mà không ai biết.
- **Studio**: đi TÌM object trong văn xuôi (fence bất kỳ, rồi quét `{`…`}` lùi dần). Cứu được lượt model kể
  chuyện trước rồi mới trả JSON — hay gặp sau khi model chạy tool.

Union thẳng thì nuốt mất nửa nghiêm của company. Ranh giới chọn: **chỗ JSON bắt đầu**. Có văn xuôi đứng trước
thì đi tìm; đầu ra tự nhận là JSON ngay từ ký tự đầu mà hỏng thì vẫn đỏ. Cả hai bộ test cũ (company: fence
trong chuỗi, fixture researcher 8 fence, thông điệp có vị trí lỗi; studio: văn xuôi + fence, fence hỏng, quét
lùi) xanh nguyên mà không sửa một dòng test nào của hai package.

### Bằng chứng đúng loại đặc tả đòi

Đặc tả ghi K3.3c "phải đo bằng eval replay". Đã chạy `evals all --replay --strict` **trên bản trước và bản sau**,
cả hai công ty, rồi `diff` danh sách PASS/FAIL: **giống hệt từng dòng** (company 42 PASS + 3 FAIL có sẵn từ
trước; studio 28 PASS). Tức là hợp nhất không đổi cách đọc bất kỳ đầu ra model đã ghi nào. Đây là loại bằng
chứng mà test đơn vị không thay được: test đơn vị đo ca mình nghĩ ra, eval replay đo cái model đã thật sự viết.

### Bẫy đắt nhất phiên: `import` trong thân hàm

Xoá `_strip_code_fence` khỏi `company/llm.py` → **73 test đỏ**, triệu chứng hoàn toàn khác: đường ống dừng ở
`research-findings` thay vì `clarification-questions`. Không lời báo lỗi nào nhắc cái tên vừa mất, vì
`runner._co_ve_la_json` nhập nó **bên trong hàm** — ruff và mypy đều không thấy, ImportError chỉ nổ lúc chạy và
bị nuốt ở tầng trên. Đã vào `TRAPS.md` §2. Sửa: helper thành tên công khai `strip_code_fence`, re-export ở cả
hai package, `runner.py` gọi tên công khai.

### Người sau KHÔNG được quên

- **K3.3c2** (rẻ): `AnthropicClient`/`CodexClient`/`FakeClient` — 0.90/0.93/0.96, company là bản trên ở mọi
  điểm lệch (timeout mặc định, phân loại `TransientError` theo mã HTTP, `cache_write_tokens`, `check_argv`,
  prompt qua **stdin** thay vì argv). Studio được nâng, nhưng đường prompt của codex đổi thật → test studio nào
  chèn `runner` sẽ phải đổi chữ ký; đo hai chiều chỗ đó.
- **K3.3c3** (đắt): `ClaudeCodeClient` 0.32 + `OpenAICompatClient` 0.28 — mỗi cái một quyết định hợp nhất
  riêng, đừng gộp một PR. Rồi mới tới **K3.3d** (`routing.py`, studio hoãn `TransientError` thay vì dừng).
- `anthropic_input_tokens` vẫn còn hai bản (3-tuple company / 2-tuple studio) — thống nhất trong c2, đúng như
  đặc tả K3.3 đã ghi.

---

## Phiên riêng — ADR-0037 PR-1: `_check_plan` chặn đủ khoá của gate plan

Track khác, không liên quan kế hoạch B ở trên: triển khai `software-company/docs/DAC-TA-TRIEN-KHAI-ADR-0037.md`
§2, đúng một PR theo luật của chính đặc tả đó ("mỗi phiên agent nhận đúng một PR").

- **Việc:** `_check_plan` (`orch/ticket_fsm.py`) thêm tham số `project`, thêm bốn kiểm bằng code — ticket >
  1 ngày/200k token (`MAX_TICKET_TOKENS`), ticket chạm từ khoá nhạy cảm mà thiếu `risk_tags` (`RISK_HINTS`),
  thiếu threat model (`review-results` key `SPEC-<project>`), thiếu `architecture`/`api-contract` trên
  blackboard. Mục tiêu: để PR-2 (bỏ `GateKind` `plan`, chưa làm) không mất kiểm nào — mọi khoá "Code gửi kèm"
  của Gate 2 cũ nay là code, không còn chỗ nào chỉ người duyệt mới thấy. Test mới `test_check_plan_adr0037.py`
  (9 ca, mỗi ca có chiều tắt qua monkeypatch).
- **Hệ quả lộ ra ngay, không phải việc ngoài phạm vi:** luật mới chặn đúng vài kịch bản test cũ dựng thiếu điều
  kiện tiên quyết (ticket giả thiếu `context_writes` architecture/api-contract; hai test threat-model coi lỗi
  transient/vĩnh viễn là "không chặn plan" — nay đúng là **có** chặn, theo đúng đặc tả; một ticket giả tên
  "POST /payments" vô tình khớp từ khoá risk mới). Sửa cả năm chỗ va chạm cùng PR này thay vì mở PR riêng, vì
  chúng là hệ quả trực tiếp của đúng một thay đổi, không phải việc khác chen vào.
  README số file/ca test cập nhật theo đĩa (57→58, 974→988).
- **CI đỏ hai vòng trước khi xanh, cả hai đều đáng nhớ cho lần sau:**
  1. Job `metadata` đỏ vì thiếu dòng `CHANGELOG.md` — luật bắt buộc 9 của `AGENTS.md`, quên ở lượt tạo PR đầu.
  2. Cùng job, sau khi thêm CHANGELOG: đỏ tiếp vì PR `fix(` chạm `orch/` phải dẫn `ADR-0034` trong THÂN PR
     (K8.3, xem mục #130 ở trên) — không phải sửa code, chỉ cần sửa mô tả PR nói rõ **không** đụng
     `TICKET_TRANSITIONS`/`RELEASE_TRANSITIONS`. Sửa thân PR (không phải commit) đủ để job chạy lại và xanh —
     GitHub Actions bắt sự kiện `edited` trên PR, không cần push thêm.
- **Merge:** #154 (squash), auto-merge. `subscribe_pr_activity` + `send_later` hẹn kiểm lại 20 phút — huỷ ngay
  sau khi nhận webhook `pull_request.closed (merged)`, không cần đợi tới giờ hẹn.
- **Người sau KHÔNG được quên:** PR-1 xong, PR-2 (bỏ `GateKind` plan) chưa làm — đọc lại §3 của
  `DAC-TA-TRIEN-KHAI-ADR-0037.md` trước khi bắt tay, và nhớ luật "một phiên một PR, không làm trước PR có phụ
  thuộc chưa merge". PR-3 (skill theo pha) đã merge độc lập (#155, phiên khác) trong lúc PR-1 đang chạy CI —
  không phụ thuộc PR-1/2 nên không có xung đột thứ tự.

## Phiên riêng — ADR-0037 PR-2: bỏ `GateKind` `plan` (#158)

Tiếp track ADR-0037, đúng một PR theo §3 của `software-company/docs/DAC-TA-TRIEN-KHAI-ADR-0037.md`. Xác nhận
trước khi bắt đầu: PR-1 (#154) và PR-3 (#155) đã ở trong `origin/main`; PR-2 chỉ phụ thuộc PR-1.

- **Việc:** `GateKind` còn 4 giá trị (`spec|release|acceptance|escalation`). `_plan` gọi `_dispatch_plan` ngay
  sau `plan.proposed` thay vì mở gate; nguồn sự thật cho phép giao ticket chuyển từ `gate.is_approved(plan_id)`
  sang `lead.plans_ok` (orchestrator ghi vào ngay sau khi `_check_plan` không ra `problems`). Guard **không**
  biến mất — `DeliveryLead.dispatch` vẫn `PermissionError` với `plan_id` lạ, chỉ đổi thứ nó hỏi.
- **Chỗ dễ bỏ sót nhất là `rehydrate`:** trước đây ticket được dựng lại từ nhánh `gate.decide` approve. Bỏ gate
  mà không dời việc dựng lại sang nhánh `plan.proposed` thì mở lại bus là mọi ticket biến mất (khuôn 2
  `TRAPS.md`). Dựng ở `plan.proposed` còn đúng thứ tự thời gian hơn: mọi `tasks`/`ticket.blocked` của kế hoạch
  nằm SAU trong log nên vẫn ghi đè được trạng thái dựng ở đó.
- **Hệ quả trên test lớn hơn hẳn phần code (33 file test đổi):** `_drive_to_plan` cũ dừng ở gate plan, nên
  hàng chục test dùng nó như một *mốc dừng* để chen sự kiện vào giữa (pause ticket, `max_steps`, ký gate từ
  tiến trình khác). Nay ký gate spec là cả đường ống chạy hết trong một `run()`. Thêm `_drive_to_spec_gate`
  (mốc dừng cuối cùng còn lại) và chuyển những test cần chen việc sang dùng nó; số `max_steps` phải đo lại.
- **Console không phải "xoá nhánh plan cho gọn":** `truth.py` là bảng dùng chung hai công ty, và Studio **vẫn
  có** `GateKind` `plan`. Đã kiểm `collect.py` trước khi xoá: chỉ `CompanyView` override `gate_effect`/
  `gate_reject_effect`/`gate_next_agent`, `StudioView` dùng mặc định rỗng — nên xoá là an toàn. Fixture
  `tests/conftest.py` đổi gate quá hạn của company từ `PLAN-1` sang `REL-001`.
- **Hai mục người-tự-kiểm của gate plan cũ không bị vứt:** "Ước lượng có cơ sở" và "Ngân sách token" dời sang
  gate release, **giữ nguyên `id`** `plan.uoc-luong-co-so` / `plan.ngan-sach-token` để hồ sơ `gate_brief` đã
  ghi ra đĩa còn đọc lại được. `_brief_plan` xoá, logic thành `_brief_estimates` đo trên ticket của release.
  Mục thứ ba ("Phụ thuộc ngoài đã xác nhận") bỏ hẳn theo §12 của đặc tả.
- **Số gate đổi, chú thích trong mã thì chưa:** `checklists.md` nay là Gate 1 spec / Gate 2 release, nhưng
  "Gate 3" vẫn nằm trong system prompt của agent (`agents/*.md`) và trong nhiều chú thích. **Không sửa prompt
  ở PR này** — đổi `agents/` là 7 bước `CONTRIBUTING.md` §3, trong đó `make eval-record` cần model thật.
  Đã ghi mapping số cũ → số mới vào đầu `checklists.md` để người đọc không lạc; PR-5x sẽ viết lại prompt.
- **Đo hai chiều (3 đột biến):** (A) trả lại gate plan + bỏ `_dispatch_plan` trong `_plan` → 3 ca đỏ;
  (B) bỏ guard `plans_ok` trong `dispatch` → 2 ca đỏ; (C) `rehydrate` chỉ dựng `plans` mà không giao lại →
  1 ca đỏ. Khôi phục → xanh hết.
- **Người sau KHÔNG được quên:** PR-4 (gom hằng tên vai `ROLE` về một chỗ) là bước kế, phụ thuộc PR-2 + PR-3
  — cả hai nay đã có. `sc-gate-plan.md` đã xoá khỏi `.claude/agents/`; `make subagents` **không tự xoá** file
  dẫn xuất thừa, phải xoá tay khi bỏ một `GateKind`.

## Phiên ADR-0037 PR-4 — gom tên vai về `roles.py` (#159)

- **Việc:** `src/company/roles.py` mới; 121 literal id agent trong 15 file thay bằng `ROLE.*`/`SOURCE.*`/`LEAD_ACTOR`.
  Không đổi hành vi, không đổi agent — diff thuần cơ học, áp bằng script có đếm số lần khớp từng mẫu.
- **Ba quyết định đặc tả §5 không nói, đã nêu trong PR body:** (1) đặt tên hằng theo ĐÍCH gộp (`PRODUCT`, `QA`,
  `SECURITY`, `OPS`) chỉ cho agent sẽ giữ tên đó, các agent bị gộp giữ tên cũ (`INTAKE`, `REVIEWER`…) — PR-5x đổi
  giá trị chứ không đổi tên hằng; `BUILDER` chưa có vì không có agent cũ nào 1:1. (2) `reviewer|qa|security` làm
  KHOÁ của `REVIEW_AGENT` là nhãn `source`, không phải id agent → nhóm riêng `SOURCE.*`; `RISK_REVIEWS`/`BASE_REVIEWS`
  của `delivery.py` cũng dùng nó. (3) `Assignee`/`ReviewSource` là `Literal` nên phải viết chuỗi tay → dời vào
  `roles.py`, test khoá `get_args(Assignee) == ENGINEERING`.
- **Chỗ test grep sẽ bắt sai:** `payload.get("data")` ở `_with_intake` — `data` là trường schema. Miễn trừ theo
  nguyên văn dòng, không theo file; đổi dòng là `test_moi_dong_mien_tru_van_ton_tai` đỏ. `kind == "intake"` thì dùng
  `ROLE.INTAKE` vì enum `kind` của schema research-findings chính là id agent phát ra nó.
- **Người sau KHÔNG được quên:** đặc tả nói "31 file/179 chỗ" nhưng đo trên `main` hôm nay bằng grep chuỗi nguyên
  id chỉ ra 14 file + `truth.py`; số cũ có lẽ đếm cả substring/tests. `collect.py` của console còn `"supervisor"`
  làm KEY dict kết quả (không phải actor) — ngoài phạm vi, để PR-6. Sau PR-5x, `kind` của research-findings sẽ
  không còn trùng id agent (`_from_kind("intake")` theo §1.2) — lúc đó cần thêm hằng/miễn trừ cho giá trị `kind`.

## Phiên ADR-0037 PR-5a — agent `security`

- **Việc:** đổi tên `security-engineer` → `security` theo `software-company/docs/DAC-TA-TRIEN-KHAI-ADR-0037.md` §6
  dòng "5a". Gộp 1:1, thân bài prompt không đổi một chữ ngoài cái tên; không đụng route, không đụng `RISK_REVIEWS`
  (đặc tả nói giữ `{qa, security}` tới PR-5c).
- **Bẫy của chính PR này — id agent trùng nhãn `source`:** sau khi đổi tên, `"security"` vừa là id agent vừa là nhãn
  `source` của `review-results`. `test_khong_con_id_agent_dang_chuoi_trong_src` (PR-4) bắt ngay ba literal vốn hợp lệ
  ở `delivery.py:329`, `orch/ticket_fsm.py:187`, `orch/routes.py:91`. Không nới test: ba chỗ đó đổi sang
  `SOURCE.SECURITY` — đọc code là biết ngay đang nói tới *nhãn chấm* hay *agent*. PR-5c sẽ gặp y hệt với `"qa"`.
- **`test_hang_role_khop_front_matter_hai_chieu` phải sửa, và sửa thế nào mới đúng:** nó khoá
  `set(ROLE.*) == OLD_IDS` với lý do "PR-4 chưa đổi agent nào" — đúng ở PR-4, sai từ PR-5a. Thay bằng bảng
  `MIGRATED` (id cũ → id mới) lớn dần **đúng một dòng mỗi PR-5x**: `OLD_IDS` giữ nguyên 21 tên (để tên cũ không
  quay lại `src/`), còn `MIGRATED` là chỗ duy nhất test biết đợt gộp đã đi tới đâu. Quên cập nhật nó là đỏ.
- **Hai fixture eval của agent KHÁC cũng phải đổi:** `evals/delivery-lead.yaml` và `evals/platform.yaml` khai
  `context: [{actor: security-engineer, namespace: threat-model, …}]`. `NAMESPACE_OWNERS` nay là `{security}` nên
  bus từ chối quyền ghi lúc phát lại — `test_committed_recordings_match_current_prompts` đỏ với thông điệp
  `PermissionDenied`, không hề nhắc tới việc đổi tên. Đổi actor trong hai file đó **không** làm bản ghi nào lệch
  (khoá bản ghi tính prompt + ca, không tính context fixture), nên không phải ghi lại eval cho hai agent ấy.
- **`make golden` và `make subagents` không tự dọn file dẫn xuất thừa:** phải `git rm`
  `tests/golden/agents/security-engineer.md` và xoá `.claude/agents/sc-security-engineer.md` bằng tay, y như
  `sc-gate-plan.md` ở PR-2. Nhớ cho PR-5b..5e: mỗi agent bị xoá là hai file dẫn xuất phải xoá tay.
- **Bước 3 (eval-record model thật) chạy được ngay trong phiên:** máy không có `ANTHROPIC_API_KEY`/`OPENAI_API_KEY`,
  nhưng có CLI `claude` đã đăng nhập → `llm.yaml` (gitignore) dựng với `provider: claude-code`, tier strong =
  `claude-opus-5`. `make eval-record AGENT=security` → 2/2 pass, ~56k token, ~4 phút. Đây là đường rẻ nhất cho
  PR-5b..5e; không cần workflow Actions `eval-record` (workflow đó tới nay chưa từng chạy lần nào).
- **Số liệu không đổi ở PR này:** vẫn 21 agent, 26 file `sc-*` — 5a là đổi tên, không phải gộp. README/`docs/
  dac-ta-tro-ly-kiem-duyet.md` chỉ đổi chữ, không đổi số; các con số ấy bắt đầu giảm từ PR-5b.
- **Người sau KHÔNG được quên:** PR-5b (`ops`) là bước kế, phụ thuộc 5a đã merge. Ba comment ghi *kết quả đo ngày
  cụ thể* (`llm.py:547,736`, `workspace.py:229`) cố ý giữ tên `security-engineer` vì là biên bản sự việc đã xảy ra,
  như ADR và `TRAPS.md`; §9 của đặc tả ("không còn id cũ trong `src/`") tính ở PR-6, khi rà lần cuối.
  `evals/spec-writer.yaml:16` còn `owner: security-engineer` trong *nội dung* một risk — đổi nó sẽ làm lệch bản ghi
  của spec-writer mà không được gì; spec-writer bị gộp vào `product` ở PR-5e nên để đó.
