# Phiên 2026-09-15

Tiếp ngay sau phiên đêm 09-14 (dọn worktree → #298, sửa cổng Windows → #299, gom bài học TRAPS → #300, port hai
việc mồ côi → #301/#302, cài đặt ADR-0016 → #303). Phiên này bắt đầu bằng đúng một câu hỏi: "có gì để tạo PR
không".

## Số test README trôi, lần thứ hai — PR #304

- **Không tin ghi chú, đo lại.** Nhật ký 09-14 có để lại một dòng "README gốc ghi gateway 251 test, đo được 256".
  Thay vì sửa đúng dòng đó, quét **cả họ**: ba dòng bảng "Quy mô" đều lệch, và **đều lệch một chiều nói ít hơn
  thật** — company 1249→1256, gateway 251→256, core 479→488.
- **Đây là lần thứ hai cùng một khuôn**: bản tự kiểm 2026-09-07 đã bắt 5/10 dòng số liệu README lệch, tất cả
  cùng chiều ấy, tất cả ở đúng những dòng KHÔNG có test CI canh. Sửa số lần một, ba tháng sau lệch lại. Nên
  phần chính của PR không phải ba con số mà là **cổng**: `test_so_test_trong_readme_khop_dia`.
- **Một quyết định kỹ thuật đáng ghi**: cổng chạy `uv run --directory <pkg> pytest --collect-only`, KHÔNG
  `sys.executable -m pytest`. Đo mới biết: pytest của console chạy trong thư mục gateway thì 10 file lỗi thu
  thập vì thiếu `pytest-asyncio` (nhóm dev riêng của từng package) → hàm đếm trả 0. Một cổng "đếm được 0" là
  cổng **nói dối**, nguy hơn không có cổng. Giá phải trả: cổng phụ thuộc `uv` có trên PATH — chấp nhận được vì
  CI chạy test bằng chính `uv`.
- **Đo ba chiều**, không chỉ hai: trả README về số cũ → đỏ; sửa đúng → xanh; **thêm một ca test mới vào core**
  → đỏ ngay. Chiều thứ ba mới là chiều quan trọng, vì nó chứng minh cổng bắt được lệch TƯƠNG LAI chứ không chỉ
  lệch đang có.
- **Gói việc thường trực nói sai hiện trạng cổng**, sửa luôn trong PR này: `docs/TASK-PACK.md` A5 còn viết ba
  lối thoát khỏi cổng "không có hạn đáo, không có trần — số của chúng chỉ tăng nếu không ai đếm", trong khi
  `TRAN_PRAGMA`/`TRAN_SKIP`/`TRAN_OMIT` đã so **bằng đúng** từ 2026-09-12; A6 còn bảo grep tay trong khi
  `CHUA_PHU_NHANH` canh từ #297; A1 nay đã có cổng cho số test README. Tài liệu gói việc sai kiểu này tốn hai
  lần: người audit tiêu giờ làm lại việc máy đã làm, rồi bỏ qua đúng phần máy **không** làm được (đọc lý do ghi
  lúc thêm một miễn trừ xem còn đúng không — cổng không đọc được lý do).
- Cổng: `console` ruff sạch, mypy 11 file sạch, `pytest -q --cov` **425 passed, 2 skipped**, coverage
  **100.00%**.
- **Người sau**: cổng mới khiến mọi PR thêm/bớt test ở company, gateway hoặc core phải sửa `README.md` trong
  CÙNG PR. Đó là chủ ý (giống `test_so_agent_khop_dia`/`test_so_adr_khop_dia` đã có), không phải phiền toái
  ngoài ý muốn — nhưng nếu thấy nó chặn oan thì **sửa cổng kèm test**, đừng hạ sổ.
